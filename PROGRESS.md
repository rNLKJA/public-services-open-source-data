# Progress log

Working memory for the hourly dataset-discovery task. There's no dedicated "codebase memory" MCP wired up for this project (see chat for why), so this file is the substitute: each run reads it first, then appends an entry, so runs build on each other instead of repeating work or losing track of what's been checked.

## Covered so far (as at 2026-07-06, initial build)

- SA school locations (Dept for Education)
- SA suburb/council reference data (ABS ASGS, SA LGAs)
- SA expiation notices (SAPOL)
- SA Police use-of-force / oversight (documented as a genuine gap, not fabricated)
- Analysis module: school zone speed limits × expiation notice volume

## Candidate public-service domains not yet explored

Pick the domain nearest the top that isn't struck through each run. Strike through (`- [x]`) once checked, whether or not it produced a new dataset. Add more domains here as they come up.

**Ordering note (2026-07-06):** Rin asked for the pick order to factor in his own career trajectory (ASO7 Senior Data Analyst, SA Police — public-sector analytics track; see chat/Donna for detail). Per this project's standing scope rule, that's used only to reorder *which* domain gets picked next — it does not narrow how any dataset's own README is framed; every dataset here stays described as general-purpose reference infrastructure. Two domains were moved up / added on that basis: SA public sector workforce data (directly relevant to his own classification/pay trajectory) and courts/justice (relevant to his SAPOL analytics remit). Re-evaluate this ordering if the career context changes.

- [x] Health (SA Health open data: hospital performance, wait times, public health statistics)
- [x] Housing (SA Housing Authority: public housing stock, waitlists, homelessness services)
- [x] SA public sector workforce and employment (Office of the Commissioner for Public Sector Employment: workforce information, classification/FTE counts, gender pay gap reporting) — agency-level dataset added 2026-07-06 (Dept for Education); genuine current whole-of-sector dataset still doesn't exist as open structured data (see run log)
- [x] Courts and justice, beyond expiation notices (court lists, sentencing statistics)
- [x] Emergency services (SES, CFS, MFS: incident statistics, response times)
- [x] Public transport (Adelaide Metro: routes, timetables, patronage, GTFS feed)
- [x] Local council services (waste collection, development approvals, rates)
- [x] Libraries (SA public library membership, usage, branch locations)
- [x] Environment and water (SA Water quality, EPA monitoring, national parks)
- [x] Aged care and disability services (facility locations, quality ratings) — facility-locations dataset added 2026-07-06 (SA Health retirement villages register); quality ratings checked and excluded on licensing grounds (see run log)
- [x] Employment and training (Workforce Australia regional statistics, TAFE SA)
- [x] Planning and development (development application approvals, zoning) — statewide zoning dataset added 2026-07-06 (SA Planning and Design Code Zones); a genuine current statewide development-application-approvals dataset (beyond the land-division subset already covered) still doesn't exist as open data (see run log)
- [x] Road safety and traffic crash statistics (Dept for Infrastructure and Transport crash/TARS data — relevant to SAPOL traffic-enforcement analytics) — statewide crash/casualty/unit dataset added 2026-07-06 (DIT's Road Crash Data, 2020-2024 rolling extract, CC BY 4.0)
- [x] Environment — EPA ambient air/water quality monitoring and national parks (distinct from the drinking-water compliance angle already covered by `sa-water-quality`; flagged as unexplored in that dataset's run log entry) — EPA ambient air quality monitoring network dataset added 2026-07-06 (16 stations, CC BY 4.0); ambient/EPA water-quality monitoring and national parks visitor data left unexplored (see run log)
- [x] Consumer affairs and business regulation (Consumer and Business Services: complaints, licensing/registration registers) — checked 2026-07-06; every CBS-specific dataset on data.sa.gov.au is stale (frozen 2017-2019), no genuine current dataset exists; nothing added (see run log)
- [x] Corrections and rehabilitation (SA Department for Correctional Services: prisoner population and recidivism statistics) — checked 2026-07-06; no genuine SA-DCS-published dataset exists (org has 0 datasets; the sole SA-portal candidate is a stale 2016 ABS mirror); national ABS "Prisoners in Australia, 2025" dataset added instead, with a dedicated SA breakdown (see run log)
- [x] Agriculture and primary industries (PIRSA: crop/livestock statistics, water licensing) — statewide crop/livestock/forestry/horticulture/seafood/wine production statistics dataset added 2026-07-06 (PIRSA Primary Industries Scorecard, CC BY 4.0, stale to FY2020-21); water licensing/allocation checked and left unfulfilled — no genuine SA licence-register or extraction-volume dataset exists as open data (see run log)
- [x] Energy and utilities (SA Government renewable/solar uptake and energy consumption data) — checked 2026-07-06; no genuine current SA-specific renewable-uptake or energy-consumption dataset exists (see run log); national Clean Energy Regulator postcode-level solar/wind/hydro/heat-pump/battery installation-and-capacity dataset added instead, with a dedicated SA breakdown; energy *consumption* remains a real, undisclosed gap
- [x] Workplace health and safety (WorkSafe SA: workplace injury/incident claims statistics, dangerous occurrence notifications) — checked 2026-07-06; no genuine open SA-specific dataset exists (SafeWork SA publishes none, ReturnToWorkSA data is all-rights-reserved); Safe Work Australia's national Jurisdictional Comparison dataset added instead, with SA broken out in every table (see run log)
- [x] Heritage and cultural places (SA Heritage Register: state and local heritage-listed places, Dept for Environment and Water) — statewide dataset added 2026-07-07 (SA Heritage Places, DHUD/DEW, CC BY 3.0 AU per bundled licence, 24,479 point features)
- [x] Mining and resources tenements (PIRSA/Dept for Energy and Mining: mineral exploration licences and tenement register — distinct from the crop/livestock angle already covered by "Agriculture and primary industries") — statewide dataset added 2026-07-07 (SA Mineral Tenements, Dept for Energy and Mining/SARIG, CC BY 4.0, 2,913 current tenement/application/released-area records across 13 layers; individual-holder names redacted, see run log)
- [x] Elections and electoral statistics (SA Electoral Commission: enrolment figures, election/referendum results, turnout by district) — checked 2026-07-07; ECSA publishes no stated open licence (only a liability disclaimer), excluded on licensing grounds same as ReturnToWorkSA/SafeWork SA; AEC's national 2025 Federal Election Results dataset added instead (CC BY, all 10 SA divisions broken out) — see run log
- [x] Tourism and visitor statistics (South Australian Tourism Commission: visitor numbers, expenditure, regional breakdowns) — SA Tourism Commission's own dataset added (CC BY 4.0, statewide + Regional SA, but frozen at March 2017); paired with Tourism Research Australia's current State Tourism Satellite Account 2023-24 (CC BY 4.0 via site disclaimer, national with an explicit SA breakdown in every table) — see run log
- [x] Native vegetation, land clearance and biodiversity (Department for Environment and Water — distinct from the conservation-reserve boundary/national-parks data already flagged as unexplored in the EPA air quality run) — statewide dataset added 2026-07-07 (SA Native Vegetation Floristic Areas - NVIS - Statewide, DEW, CC BY 4.0, 1,046-row vegetation-type lookup table mirrored; multi-gigabyte spatial layers documented but not mirrored, see run log)
- [x] Aboriginal affairs and native title (Department for Aboriginal Affairs and Reconciliation / South Australian Native Title Services — native title determinations and Indigenous Land Use Agreements, distinct from the native-vegetation/biodiversity angle above) — no distinct SA-published dataset exists; national NNTT native title determinations/ILUA register added instead, with a dedicated SA breakdown and individual-claimant-name redaction (see run log)
- [x] Arts and cultural institutions (Arts South Australia / State Library, Art Gallery of SA, SA Museum, History Trust of SA — visitation and attendance statistics) — no genuine SA-published visitation dataset exists (every relevant SA org's data.sa.gov.au listing is mandatory governance disclosures, not attendance data); ABS's national Cultural and Creative Activities, 2021-22 survey added instead, with a dedicated SA breakdown in its state-level attendance table (see run log)
- [x] Child protection and family services (Department for Child Protection: notifications, investigations, out-of-home care and reunification statistics) — no genuine SA-DCP-published dataset exists (org has 8 datasets, all mandatory governance disclosures); Productivity Commission's Report on Government Services 2026 Child protection services data tables added instead (44 tables, CC BY 4.0, SA broken out in every table) — see run log
- [x] Veterans affairs and ex-service support (Office for Veterans' Support SA / Dept of Veterans' Affairs — veteran population statistics and support service usage) — no genuine SA-published dataset exists (Defence SA has 10 datasets, all mandatory governance disclosures); DVA's national Local Government Area (LGA) profile added instead (CC BY 4.0, 534 LGAs, dedicated SA breakdown of 69 LGAs) — see run log
- [x] Sport and recreation (Office for Recreation, Sport and Racing — participation statistics, facility and grant funding) — checked 2026-07-07; the only two ORSR-published datasets on data.sa.gov.au (sports vouchers) are frozen at 2017 and explicitly superseded by ORSR's own non-open Power BI dashboard; nothing added (see run log)
- [x] Multicultural affairs and migration settlement (SA multicultural affairs / humanitarian and skilled migration settlement statistics) — no genuine SA-published dataset exists (SA's current Multicultural Affairs unit has no CKAN footprint; its predecessor's only releases are one-off 2012-2015 items, one explicitly inactive); Department of Home Affairs' national Settlement Database extract added instead (CC BY 3.0 AU, Humanitarian/Family/Skilled streams, 2016-2025, dedicated SA breakdown to LGA level) — see run log
- [x] Crime and offence statistics (SAPOL recorded crime/reported offences by offence type and LGA — distinct from `sa-expiation-notices`, which covers only traffic infringements, and `sa-road-crash-data`, which covers only crashes; directly relevant to Rin's SAPOL analytics remit) — statewide suburb/postcode-level dataset added 2026-07-07 (SAPOL Crime statistics + Family & Domestic Abuse related-offences, CC BY 4.0, 2010-11 to 2025-26 Q1-Q3, 1.49M rows)
- [x] Mental health services (SA Health / Mental Health Commission of SA — community mental health service usage, involuntary treatment order statistics; distinct from the ED-performance and hospital-workforce angles already covered) — statewide acute mental health performance dataset added 2026-07-07 (SA Health Mental Health data, CC BY, average length of stay/28-day readmission/7-day community follow-up by ward type, frozen at 2017-18; SA Mental Health Commission's own datasets are governance disclosures only, excluded; see run log)
- [x] Domestic, family and sexual violence statistics (SA Attorney-General's Department / Office for Women — reported incidents, intervention orders; distinct from the general court-workload angle already covered by `sa-courts-at-a-glance`) — no genuine SA-published dataset exists (AG's Dept, Courts Administration Authority and "Office for Women" all checked directly, none exist as open data); AIHW's national Family, Domestic and Sexual Violence supplementary data-tables workbook added instead, 40 tables across 10 source collections, CC BY 4.0, jurisdiction/SA breakdown where the source publishes one (see run log)
- [x] Youth justice and youth offending statistics (Department of Human Services Youth Justice — youth detention, diversionary program and reoffending statistics; distinct from the adult-corrections angle already covered nationally via `au-prisoners-in-australia`) — no genuine SA-DHS-published dataset exists; AIHW's national Youth Justice National Minimum Data Set / "Youth justice in Australia 2024–25" added instead (290 tables, CC BY 4.0, current 12 May 2026 edition, dedicated SA breakdown in 182 tables plus two SA-only trend tables) — see run log
- [x] Firearms licensing and registration statistics (SAPOL Firearms Branch — firearm licence/registration counts by category and region; directly relevant to Rin's own agency) — checked 2026-07-07; no genuine open SA or national dataset exists (see run log)
- [x] Court sentencing outcomes (Courts Administration Authority / SA Sentencing Advisory Council — sentence type and length statistics, distinct from the workload/staffing angle already covered by `sa-courts-at-a-glance`) — no genuine CAA- or SA-Sentencing-Advisory-Council-published dataset exists; ABS's national Criminal Courts, Australia 2024-25 added instead, with a dedicated South Australia data cube (see run log)
- [x] Ambulance services and response times (SA Ambulance Service — call volume, response time performance and ramping statistics; distinct from the SES/CFS/MFS incident-statistics angle already covered under "Emergency services") — no genuine currently open SA-published dataset exists (the two SAAS response/incident-by-priority datasets that used to exist on data.sa.gov.au are now 404/unpublished); Productivity Commission's Report on Government Services 2026 Ambulance services data tables added instead (12 tables, CC BY 4.0, SA broken out in every table) — see run log
- [x] Threatened species and biodiversity conservation listings (Department for Environment and Water — threatened flora/fauna listings and conservation status; distinct from the native vegetation floristic-type angle already covered by `sa-native-vegetation-floristic-areas`) — no genuine current SA-published species-level conservation-status list exists (DEW's flora/fauna survey-site packages are stale 2016 ALA-biocache pointers, not a status listing); DCCEEW's national EPBC Act Threatened Species and Ecological Communities state lists added instead (CC BY 3.0 AU, current Feb/Mar 2026 editions, dedicated SA breakdown: 319 species, 18 ecological communities) — see run log
- [x] Building and construction approvals (ABS Building Approvals, Australia — number and value of residential/non-residential building approvals by state/LGA; distinct from the land-division and planning-zone angles already covered) — no genuine current SA-published dataset exists (the sole SA-portal candidate is a stale 2017 mirror pointing at a discontinued ABS catalogue number); ABS's national Building Approvals, Australia, May 2026 added instead (dwelling-unit counts and building values by state, plus a Greater Adelaide GCCSA breakdown) — see run log
- [x] Waste and resource recovery (Green Industries SA — statewide waste diversion, recycling and landfill statistics; distinct from local-council kerbside collection logistics already covered under "Local council services") — SA EPA Waste Levy Rates dataset added (CC BY 4.0, 1994-2023/24 solid/liquid levy rate schedule), paired with DCCEEW's national Waste and Resource Recovery Database 2024 for the tonnage/diversion/recycling statistics (CC BY 2.5 AU, dedicated SA breakdown, 2006-07 to 2022-23) — see run log
- [x] Communicable disease and public health surveillance (SA Health — notifiable disease/condition statistics; distinct from the ED-performance, mental health and hospital-workforce angles already covered) — Communicable Disease Control Branch weekly notifiable conditions surveillance dataset added 2026-07-07 (5-year/YTD comparison + last-8-weeks reports, ~63 diseases/conditions, CC BY 4.0 per SA Health's site-wide copyright page, genuinely current/weekly-refreshed as at 4/07/2026)
- [x] Disability services and NDIS participant statistics (NDIS Quality and Safeguards Commission / SA breakdown — participant counts and service usage; distinct from the facility-location angle already covered under "Aged care and disability services") — no genuine current SA-published dataset exists (the sole SA-portal candidate is a stale 2013-14 pre-NDIS National Disability Agreement mirror); NDIA's own national "Participants by LGA" dataset added instead (CC BY 4.0 via the NDIS Data Research site's own copyright page, 18 quarterly snapshots Dec 2021-Mar 2026, dedicated SA breakdown across all 70 SA LGAs) — see run log
- [x] Vehicle registration and driver licensing statistics (Department for Infrastructure and Transport — registered vehicle counts by type, fuel/energy source, age and region, and driver licence holder counts; distinct from the crash and expiation-notice angles already covered by `sa-road-crash-data` and `sa-expiation-notices`) — statewide dataset added 2026-07-07 (DIT's Registered Vehicles by Postcode + Drivers' Licences by Postcode Age and Sex, both CC BY 4.0; no fuel/energy-source breakdown exists in either source, see run log)
- [x] Government grants and funding transparency (SA Government Grants Register / GrantConnect — recipient, amount, purpose and administering agency for state government grant payments) — checked 2026-07-08; no genuine consolidated SA state grants register or open GrantConnect state-level equivalent exists; nothing added (see run log)
- [x] State government finances (SA Budget Papers / Department of Treasury and Finance — portfolio-level expenditure and revenue estimates, actual-vs-budget outturns) — checked 2026-07-08; no genuine current SA-published budget/finance dataset exists (Treasury's own CKAN datasets are stale 2015-2018, and the current 2026-27 Budget Papers are PDF-only, behind a Cloudflare block); ABS's national Government Finance Statistics, Annual 2024-25 added instead, with a dedicated South Australia general government data cube (see run log)
- [x] Public sector procurement and contracts transparency (SA Government Contracts and Tenders register — supplier, value, agency and category for state government contracts; distinct from the grants/GrantConnect angle above) — checked 2026-07-08; no genuine consolidated SA state contracts/tenders register exists as open data; nothing added (see run log)
- [x] Population and demographic projections (ABS / SA Government population estimates and projections by LGA — distinct from the static suburb/LGA boundary reference data already covered by `au-suburbs-councils`) — two datasets added 2026-07-08: `sa-population-projections` (DPTI/DHUD's 2016-Census-based projections to 2036/2041, CC BY 4.0, current 2021-2041 edition PDF-only behind a Cloudflare block) and `au-regional-population-estimates` (ABS's actual Estimated Resident Population by LGA, 2001-2025, CC BY 4.0, dedicated SA breakdown) — see run log
- [x] Weather, climate and rainfall statistics (Bureau of Meteorology — SA rainfall, temperature and climate station data; distinct from the EPA ambient air/water quality monitoring already covered) — dataset added 2026-07-08 via SILO (Queensland Government, CC BY 4.0), built from BOM station observations for 8 SA stations, 2016-2025, after bom.gov.au itself proved blocked to automated access (see run log)
- [x] Freedom of Information (FOI) request statistics (SA Ombudsman / individual agencies — requests received, granted, refused and review outcomes; a distinct transparency angle from the grants/procurement gaps already documented) — dataset added 2026-07-08: State Records SA's 19 FOI annual-reporting datasets (applications, determinations, refusal reasons, processing time, fee waivers, extensions, FTE, 1991-2024/25, CC BY 4.0) paired with Ombudsman SA's Annual Report 2024-25 FOI external-review tables (CC BY 4.0) — see run log
- [x] Fisheries and aquaculture statistics (PIRSA — wild-catch and aquaculture licence/production data; distinct from the crop/livestock/forestry angle already covered by `sa-primary-industries-scorecard`) — checked 2026-07-08; no genuine PIRSA-published production/licence-count dataset exists (only spatial zone/lease/sanctuary boundary layers); ABARES' national Australian fisheries and aquaculture statistics: Data to 2024-25 added instead (CC BY 4.0), with a dedicated South Australia production table sourced from PIRSA/SARDI's own data (see run log)
- [x] National parks and reserves visitation statistics (Department for Environment and Water — visitor numbers and park-level usage; distinct from the native vegetation and threatened species angles already covered) — checked 2026-07-08; no genuine open SA park-level visitation dataset exists (see run log)
- [x] Local government elections (SA Electoral Commission — council election results, turnout and ward representation; distinct from the state/federal election results already covered by `au-federal-election-results`) — statewide dataset added 2026-07-08 (ECSA's 2022 Council elections results and statistics, CC BY 3.0 AU per ECSA's own copyright page, all 68 SA councils across 8 tidy tables) — see run log
- [x] Cost of living and consumer price index (ABS Consumer Price Index / Selected Living Cost Indexes — Adelaide/South Australia breakdown; distinct from the general state finances/economic data already covered) — dataset added 2026-07-08 (ABS CPI, May 2026 release, CC BY 4.0, Adelaide's monthly index numbers/annual/monthly % change from headline to expenditure-class level, October 2017 to May 2026); SLCI checked and excluded — no state/city breakdown exists in that series at all (see run log)
- [x] Gender pay gap reporting (Workplace Gender Equality Agency — employer-level gender pay gap data covering SA-based organisations; distinct from the whole-of-SA-public-sector workforce composition data already covered by `sa-education-workforce`) — WGEA's national 2024-25 Employer Gender Pay Gaps dataset added (CC BY 4.0, 8,617 employers + 1,838 corporate groups, named); no state/location field exists in the source so it could not be cut to SA-based organisations specifically (see run log)
- [x] Local government finances (SA Local Government Grants Commission / council-level revenue, expenditure, rates and grants data — distinct from the state-government-finances angle already covered by `sa-state-government-finances`, and a natural companion to `sa-local-government-elections`) — checked 2026-07-08; genuine council-by-council Grants Commission data (Minister's Sheet, "Councils in Focus") exists but sits behind a live Cloudflare bot-challenge on `dit.sa.gov.au`/`councilsinfocus.sa.gov.au`; ABS's national Government Finance Statistics local-government-sector data cube added instead, with a dedicated South Australia (all-68-councils-combined) breakdown (see run log)
- [x] Working with Children Checks and screening statistics (Department of Human Services Screening Unit — application/clearance volumes; distinct from the general public-sector-workforce and child-protection angles already covered) — dataset added 2026-07-08 (DHS Screening Unit's Screening Clearance Processing Timeframes, CC BY 4.0, stale to FY2017-18, all clearance types combined not WWCC-specific; see run log)
- [x] Container deposit and recycling scheme statistics (Green Industries SA — container return rates and volumes under SA's container deposit scheme; distinct from the general waste-levy/resource-recovery angle already covered by `sa-waste-levy-rates`/`au-waste-resource-recovery`) — statewide dataset added 2026-07-08 (EPA SA's Container Deposit Legislation - Return Rates and Compliance, CC BY 4.0, containers returned/return rate by material/compliance, FY2005-06 to FY2019-20; a related current Green Industries SA PDF report was found but excluded, no open licence — see run log)
- [x] Dog and cat management and animal welfare statistics (Dog and Cat Management Board SA — registration counts, microchipping, seizure/rehoming/euthanasia statistics; a distinct local-government-adjacent regulatory domain not yet explored) — statewide dataset added 2026-07-08 (Dogs and Cats Online Data, CC BY 4.0, animal/owner counts by council, incident statistics, popular breeds/names, 4 editions 2021-22 to 2024-25); no genuine open seizure/rehoming/euthanasia dataset found (see run log)
- [x] SA Civil and Administrative Tribunal (SACAT) case statistics (guardianship/administration, residential tenancy, building work and equal opportunity case volumes and outcomes; distinct from the general court workload/sentencing angles already covered by `sa-courts-at-a-glance` and `au-criminal-courts-sentencing-outcomes`, since SACAT is a tribunal, not a court) — statewide dataset added 2026-07-08 (SACAT's own Annual Report data, CC BY 4.0, 10 tidy tables across Administrative and Disciplinary/ADR/Community/Housing/Internal Review lists, financial years 2020-21 to 2022-23, stale ~2.5 years — see run log)
- [x] Missing persons statistics (SAPOL — reported missing person counts, demographics and outcomes; directly relevant to Rin's own agency and distinct from the crime/expiation/road-crash angles already covered by `sa-crime-statistics`, `sa-expiation-notices` and `sa-road-crash-data`) — checked 2026-07-08; no genuine open SAPOL-published or national missing-persons dataset exists (see run log)
- [x] Public housing and homelessness services performance (SA Housing Authority / Specialist Homelessness Services — waitlist times, tenancy turnover, homelessness-service client outcomes; distinct from the private-rental-market angle already covered by `sa-private-rental-report` and the general Housing domain checked 2026-07-06) — two datasets added 2026-07-08: `sa-social-housing` (SA Housing Authority's public housing/SOMIH dwellings, households, new-households-housed, household-members, CC BY 4.0, stale to FY2020-21) and `au-specialist-homelessness-services` (AIHW's SHSC historical tables, CC BY 4.0, current 2024-25 edition, dedicated SA breakdown in 14 of 15 tables); no genuine waitlist-time or tenancy-turnover dataset found (see run log)
- [x] Coastal and marine management (Department for Environment and Water — coastal erosion/protection works, marine park zoning and compliance; distinct from the native vegetation, threatened species and ambient water-quality angles already covered) — statewide marine park zoning/boundaries dataset added 2026-07-08 (DEW's Marine Park Zoning + Marine Park Network Boundaries, CC BY 4.0, 19 parks/214 zones); no coastal-erosion/protection-works or compliance/enforcement dataset found (see run log)
- [x] Public sector executive remuneration and senior officer disclosures (SA Government — executive/senior-officer salary bands and headcount by agency; a distinct transparency angle from the whole-of-sector workforce composition data already covered by `sa-education-workforce` and the general public-sector-workforce domain checked 2026-07-06) — no whole-of-sector register exists, but a 37-agency merged dataset was assembled from individually-published per-agency disclosures added 2026-07-08 (headcount by classification only, no $ salary figures published anywhere — see run log)
- [x] Gambling and liquor licensing statistics (Consumer and Business Services / Office of Problem Gambling — liquor licence counts by category, gaming machine numbers, problem gambling support-service usage; distinct from the general CBS complaints/licensing-register check already done 2026-07-06, which did not dig into gambling/liquor specifically) — checked 2026-07-08; no genuine open, currently-live, CC-BY-licensed dataset exists for any of the 3 sub-angles (liquor licence counts by category, gaming machine numbers, problem gambling support-service usage); nothing added (see run log)
- [x] Boating and marine safety statistics (Department for Infrastructure and Transport / Marine Safety SA — recreational vessel registrations, boating safety incidents, mooring licences; distinct from the road vehicle/driver-licence data already covered by `sa-vehicle-registrations-and-licences` and from the environmental "Coastal and marine management" domain above) — statewide dataset added 2026-07-08 (DIT's Boat Registrations + Boat Licence Statistics, both CC BY 4.0; boating safety incident/fatality and mooring-licence sub-angles checked and found not to exist as open structured data, see run log)
- [x] Concessions and cost-of-living support statistics (Department of Human Services — Cost of Living Concession, Emergency Electricity and Gas Payment recipient counts/expenditure; distinct from the CPI price-index data already covered by `au-cost-of-living-cpi`, which measures prices, not concession usage) — statewide dataset added 2026-07-08 (DHS's Cost of Living Concessions + Emergency Electricity Payments Scheme, both CC BY 4.0, FY2015-16 to FY2017-18, stale since 2018; no expenditure/dollar figures published anywhere — see run log)
- [x] Coroners Court death investigation statistics (Courts Administration Authority — Coroners Court case numbers, cause-of-death category breakdowns, inquest counts and timeframes; distinct from the whole-of-courts lodgement/finalisation figures already covered by `sa-courts-at-a-glance`, which includes the Coroners Court only as one line in an aggregate statewide caseload table, not a dedicated cause-of-death breakdown) — no genuine SA-CAA-published dataset exists; Productivity Commission's Report on Government Services 2026 Courts chapter added instead (`au-coroners-court-statistics`, 17 tables isolating the Coroners' courts row, CC BY 4.0, SA broken out in every table); no cause-of-death category breakdown exists in any open source (NCIS is not open data) — see run log
- [x] Births, deaths and marriages registration statistics (Attorney-General's Department / South Australian Registry of Births, Deaths and Marriages — annual registration counts, birth/death/marriage volumes by year and region; a distinct civil-registration domain not yet explored, separate from the ABS population-estimate and projection datasets already covered by `sa-population-projections`/`au-regional-population-estimates`, which model population stock, not registration-event flow) — no genuine SA Registry-published registration-count dataset exists (Attorney-General's Dept's only BDM-derived dataset is "Popular Baby Names", top-100 names only); ABS's national `au-births-deaths-marriages-registrations` added instead (Births/Deaths Australia via ABS.Stat API 1971/1975-2024, Marriages and Divorces Australia 2024 data cube for 2020-2024, all CC BY 4.0, SA broken out in every table) — see run log
- [x] Second-hand dealer, motor vehicle dealer and credit provider licensing statistics (Consumer and Business Services — licence-holder counts and categories for second-hand dealers, motor vehicle/used-car dealers, and consumer credit/finance brokers; distinct from the liquor/gaming licensing angle just checked above and from the general CBS complaints/licensing-register check done 2026-07-06, neither of which examined this specific licence family) — consumer credit/finance-broker part covered nationally: ASIC's Credit Licensee + Credit Representative datasets added (`au-consumer-credit-licensing`, CC BY 3.0 AU, weekly-refreshed 2 July 2026 snapshot, dedicated SA breakdown); second-hand/motor-vehicle-dealer part remains a stale 2017-18 CBS snapshot already excluded in the 2026-07-06/2026-07-08 checks (see run log)
- [x] Public-sector integrity and corruption oversight statistics (Independent Commissioner Against Corruption SA / Office for Public Integrity — complaint, assessment, investigation and referral counts; a public-sector-integrity angle directly relevant to Rin's own public-sector analytics remit, distinct from the general court/tribunal workload data already covered) — statewide dataset added 2026-07-09 (`sa-icac-corruption-oversight-statistics`, CC BY 4.0, merges the pre-2021 ICAC/OPI predecessor org's 2013-14 to 2020-21 complaint/assessment/referral tables with the current Commission's self-contained 2014-15 to 2022-23 investigation/referral/PCD-Act-sanctions series; 13 tidy tables covering complaint intake, subject, assessment timeframe, own-initiative matters, formal corruption investigations, SAPOL referrals, and case-level PCD Act sanctions against individual SA Police officers) — see run log
- [x] Alcohol and other drug treatment service statistics (Drug and Alcohol Services SA / SA Health — treatment episode and client counts by service type and substance; distinct from the acute mental-health ward performance data already covered by `sa-mental-health-services` and from SAPOL's own drug-offence counts within `sa-crime-statistics`) — no genuine SA-DASSA-published open dataset exists (SA Health publishes narratively via PDF statistical bulletins, not structured data); AIHW's national AODTS NMDS added instead (`au-alcohol-other-drug-treatment-services`, CC BY 4.0, current 2024-25 edition published 25 Jun 2026, dedicated SA breakdown: client counts 2013-14 to 2024-25 across 10 tables, closed-treatment-episode counts 2015-16 to 2024-25 across 19 tables) — see run log
- [x] Roadside breath and drug testing statistics (SAPOL / Department for Infrastructure and Transport — random breath test and roadside drug test counts and positive-detection rates; directly relevant to Rin's own agency and distinct from the road-crash and expiation-notice datasets already covered by `sa-road-crash-data` and `sa-expiation-notices`, neither of which reports testing volumes) — breath-testing (RBT) half covered: statewide dataset added 2026-07-09 (`sa-driver-screening-tests`, SAPOL Annual Reporting Data, CC BY 4.0, Static/Mobile driver screening test volumes, FY2012-13 to 2024-25); no genuine open roadside drug testing (RDT) volume time series or positive-detection-rate dataset exists (see run log)
- [x] Natural disaster and emergency recovery funding statistics (SA/Australian Government joint disaster recovery payments and grants — bushfire, flood, storm recovery; distinct from the SES/CFS/MFS incident-count data already covered under "Emergency services") — national dataset added 2026-07-09 (NEMA's Location-Based Disaster Assistance Payments, CC BY 2.5 AU, 45 disaster events 2019-2026, dedicated SA breakdown: 2019 bushfires + 2022 River Murray floods, 16 SA LGAs) — see run log
- [x] Public sector enterprise bargaining and industrial relations statistics (working days lost to industrial disputes, enterprise agreement coverage across SA public sector agencies; a workforce sub-angle distinct from the classification/FTE composition data already covered under "SA public sector workforce and employment") — no genuine open SA-published enterprise-agreement-coverage or industrial-disputes dataset exists (SAET/AGD publish only case-file/document listings, not structured data); ABS's national Industrial Disputes, Australia, March 2026 added instead (`au-industrial-disputes-working-days-lost`, CC BY 4.0, working days lost/disputes/employees involved by state, industry, cause and duration, Mar 1985-Mar 2026, dedicated SA breakdown); no enterprise agreement coverage rate exists as open data anywhere, SA or national (see run log)
- [x] Health practitioner and pharmacy registration/licensing statistics (AHPRA-registered profession counts with an SA breakdown; distinct from the Working with Children Check/screening-clearance angle already covered) — checked 2026-07-09; no genuine open, redistributable dataset exists at any level (AHPRA itself, the Dept of Health's national tool, AIHW's own report, or an SA-specific board/authority); nothing added (see run log)
- [x] Public transport patronage and on-time-running performance statistics (Adelaide Metro — passenger boardings, service reliability/on-time-running figures; distinct from the static routing/timetable GTFS feed already covered by `sa-adelaide-metro-gtfs`, which contains no patronage or performance data) — patronage half covered: statewide dataset added 2026-07-09 (`sa-adelaide-metro-patronage`, DIT's banded Adelaide Metro Validations, CC BY 4.0, Q4 2025 extract of a 2013-2025 quarterly series); no genuine machine-readable on-time-running dataset exists (Adelaide Metro publishes it only as monthly PNG images, see run log)
- [x] Contaminated land and site contamination register statistics (EPA South Australia — public register of notified, being-assessed and remediated/decontaminated sites; distinct from the ambient air/water quality monitoring already covered by `sa-epa-air-quality-monitoring`/`sa-water-quality`) — the full public register is fee-gated, single-property-search only, so not open bulk data; EPA's own Groundwater Prohibition Area layer added instead (`sa-groundwater-prohibition-areas`, CC BY 4.0, 19 statewide areas, current to 15 June 2026) — see run log
- [x] Organ, tissue and blood donation statistics (DonateLife SA / Australian Red Cross Lifeblood — donor registration, donation volume and transplant-waitlist statistics with an SA breakdown; a public-health domain not yet explored, distinct from the acute hospital/ED-performance and mental-health-service angles already covered) — Services Australia's Australian Organ Donor Register registration-statistics dataset added 2026-07-09 (`au-organ-donor-register-statistics`, CC BY); DonateLife/OTA's donation-and-transplantation-outcome data and Lifeblood/NBA's blood donation data were both checked and found not to qualify (see run log)
- [x] Public libraries digital/e-resource usage statistics (SA Public Library Network — e-book/e-audiobook loans, digital database usage; distinct from the physical collections/visitation statistics already covered by `sa-public-library-statistics`) — checked 2026-07-09; a genuine SA-specific digital-usage breakdown exists (Libraries Board of SA's own Annual Reports: eBook/Audiobook loans, eMagazine/eNewspaper loans, digital holds/purchases) but carries no open licence (plain Crown copyright); data.sa.gov.au's identically-named CKAN dataset contains none of this data (governance boilerplate only); nothing added (see run log)
- [x] State emergency declarations and major incident timeline statistics (SA State Emergency Management Committee / SAFECOM — declared major emergencies and disaster declarations by type and date; distinct from the individual-agency incident-count data already covered under "Emergency services" and from the recovery-*funding* data covered by `au-disaster-recovery-payments-by-lga`) — no genuine SA SEMC/SAFECOM declared-emergency register exists as open data (narrative-only mentions in annual-report PDFs); Attorney-General's Department's national historical disaster-event catalogue added instead (`au-historical-disaster-events`, CC BY 3.0 AU, 673 events 1753-2014, dedicated SA subset of 74 events) — a static/unmaintained archive, not a current declarations register; see run log
- [x] Gambling and liquor licensing statistics (Consumer and Business Services SA / Office for Problem Gambling — gaming machine expenditure by venue/region, liquor licence counts by category; a regulatory domain not yet explored) — note: this duplicates the "Gambling and liquor licensing statistics" entry above, already checked 2026-07-08 with nothing added; marked covered 2026-07-10 without a fresh search per the entry's own note, since no new angle was identified
- [x] Immunisation and vaccination coverage rates (SA Health / national Australian Immunisation Register data — coverage rates by LGA/postcode and age cohort; distinct from the disease-case-count angle already covered by `sa-notifiable-conditions-surveillance`) — no genuine current SA-specific or national LGA/postcode-level open dataset exists; AIHW's "Immunisation rates for children in 2016-17" (CC BY 4.0, national/PHN/SA3/postcode/ATSI-SA4 tables) added instead, stale to 2016-17 (see run log)
- [x] Government procurement and contracts disclosure (SA Tenders and Contracts / South Australian Government contract disclosure register — awarded contract value, supplier and agency by financial year; a public-sector-spending transparency domain not yet explored) — re-checked 2026-07-10 (near-duplicate of "Public sector procurement and contracts transparency" above, checked 2026-07-08); `tenders.sa.gov.au` and `contracts.sa.gov.au` still return HTTP 403 (Cloudflare bot-challenge, same block as before); `data.sa.gov.au` CKAN search for "contract disclosure" restricted to the SA jurisdiction facet returns zero results (only Queensland agencies publish that dataset type); confirmed via web search that SA's tender platform has no OCDS/JSON API unlike federal AusTender; nothing added (see run log)
- [ ] Adult education and community learning statistics (SA Department for Education / TAFE SA-adjacent community education providers — enrolment and course-completion statistics distinct from the formal apprenticeship/traineeship data already covered by `sa-apprenticeships-traineeships`)
- [ ] Public sector ICT and digital service delivery statistics (Office for Data Analytics / Department of Premier and Cabinet — digital service uptake, website/app usage statistics for SA Government services)
- [ ] Native forestry and plantation timber production statistics (ForestrySA / Department for Environment and Water — plantation area, timber volume harvested; distinct from the native vegetation floristic-type data already covered by `sa-native-vegetation-floristic-areas`)

## Run log

- 2026-07-10 (scheduled run): checked **Government procurement and contracts disclosure**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026 with the same role (ASO7 Senior Data Analyst, SA Police), unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. First did light list housekeeping: the "Gambling and liquor licensing statistics" duplicate entry (which had carried its own note since 2026-07-08 to "treat as covered rather than re-running") was still showing an unchecked `- [ ]` box despite that note — struck it through this run as administrative cleanup, no fresh search spent on it, consistent with its own instruction. That made **Government procurement and contracts disclosure** the next unchecked item — itself a near-duplicate of "Public sector procurement and contracts transparency", already checked 2026-07-08 with nothing found (that run: `tenders.sa.gov.au`/`contracts.sa.gov.au` both 403 Cloudflare-blocked; only SA-jurisdiction CKAN match was a stale FY2020-21 State Procurement Board governance-disclosure workbook, not a whole-of-government register). Given the list keeps this as a separate "not yet explored" entry and this project's standing instruction to actually re-verify portal state rather than trust memory (network blocks have flipped before, e.g. data.sa.gov.au), re-tested rather than skipped it outright. Re-confirmed via direct `curl`: `tenders.sa.gov.au` → HTTP 403, `contracts.sa.gov.au` → HTTP 403 (same Cloudflare bot-challenge as 2026-07-08; `data.sa.gov.au` itself is HTTP 200, reachable as usual). Queried `data.sa.gov.au`'s CKAN API for `contract disclosure` restricted to `jurisdiction:"Government of South Australia"` — 0 results (the unrestricted query returns 1,726 results, but every "Contract Disclosure" dataset on the federated portal belongs to Queensland agencies, e.g. Queensland Fire Department, Queensland Health, DESBT — none from South Australia). Web search corroborated: SA's Premier and Cabinet Circular PC027 ("Disclosure of Government Contracts") routes eligible-contract disclosure to the Tenders & Contracts website itself (the blocked platform), and unlike the Commonwealth's AusTender, SA's platform has no OCDS-compliant JSON API or bulk export — it's a live tendering/registration system for suppliers, not an open-data source. Conclusion unchanged from 2026-07-08: no genuine open SA contracts/procurement dataset exists as of this run; nothing added. 3 domains remain unchecked after this run (Adult education and community learning statistics; Public sector ICT and digital service delivery statistics; Native forestry and plantation timber production statistics) — at, not below, the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-09 (scheduled run): checked **Immunisation and vaccination coverage rates**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026 with the same role (ASO7 Senior Data Analyst, SA Police), unchanged from every prior run's check — `CLAUDE.md` in the same folder had a newer 4 July 2026 timestamp but its content is only folder-structure/rules documentation, not a role or goal change, so no reordering of the candidate list was needed; proceeded top-of-list as usual (skipping the already-annotated "Gambling and liquor licensing statistics" duplicate at the list's bottom per the prior run's note). Checked four candidate sources: (1) **`data.sa.gov.au`** (confirmed reachable, HTTP 200) — no genuine SA-specific immunisation coverage dataset exists; searches for "immunisation"/"vaccination" surfaced only the same nationally-federated results as (2)-(4) below, no SA Health-published dataset. (2) **`health.gov.au`'s current "Childhood immunisation coverage" pages** — genuinely current (quarterly-refreshed, page last updated 2 February 2026, covering the annual period to September 2025; South Australia broken out at 92.00%/89.61%/94.14% fully immunised at ages 1/2/5) but the department's own Copyright page (`health.gov.au/using-our-websites/copyright`, fetched directly) states "you must not use the whole or any part of the content on this website for any commercial purpose... all other rights are reserved" — an all-rights-reserved, non-commercial-only notice, excluded on licensing grounds same as ACARA My School/AHPRA; also state-level only, no LGA/postcode breakdown published there at all. (3) **AIHW/AURIN-harvested "Immunisation Rates for Children" datasets** on the `data.gov.au`/`data.sa.gov.au` federated catalogue — CKAN metadata claims CC-BY-3.0-AU, covering PHN/SA3/SA4 geography 2011-2017, but the only download path is AURIN's Data Provider portal (`adp.aurin.org.au`/`adp-gatekeeper.prod.aurin-prod.cloud.edu.au`), confirmed this run to be a login-gated JS app that returned no data to a direct unauthenticated WFS request — excluded on Accessible/unauthenticated-bulk-access grounds, same reasoning as the earlier Health Workforce Data Tool exclusion, regardless of the claimed licence. (4) **PHIDU's "Child and Youth Health" series** (Torrens University Australia, LGA/PHA/PHN-level, more recent vintages than AIHW's own release) — confirmed CC BY-NC-SA 3.0 AU (non-commercial, share-alike) across every edition checked, excluded on licensing grounds same as ACARA My School. Found the genuine fit instead directly on **`aihw.gov.au`** (bypassing the AURIN wrapper entirely): AIHW's own "Immunisation Data" hub page (last updated 25 May 2026, confirmed current) still serves the same underlying "Immunisation rates for children in 2016–17" workbook as its only downloadable local-area data table — confirmed CC BY 4.0 via AIHW's site-wide copyright page (same wording already used for other AIHW datasets in this repo), downloaded directly over plain HTTPS (3.3 MB XLSX, no blocking, no `fetch.sh` needed). This workbook is far richer than the domain description's minimum bar: six tabs covering national, PHN, SA3, **residential postcode** (privacy-banded percentages, not exact figures — a suppression measure built into the source itself) and a dedicated Aboriginal and Torres Strait Islander children breakdown by SA4, 2011-12 to 2016-17. Added `datasets/au-childhood-immunisation-coverage-rates/`: `raw/` holds the verbatim source XLSX; `data/` holds five tidied CSVs built by `convert.py` — national totals (unpivoted from the source's side-by-side All/ATSI columns into a `population_group` column), a merged PHN file (combining the source's separate All-children and ATSI-children PHN tabs into one table with the same `population_group` slice column), SA3, postcode (42,063 rows, largest file) and ATSI-by-SA4, each with a `south_australia_related` boolean flag (true wherever `SA` appears as a token in the source's sometimes-joint state codes, e.g. `NT/SA/WA`) rather than a hard state filter, so cross-border areas aren't silently dropped. No figures recalculated, only reshaped/decoded/renamed to snake_case. Privacy check passed clean: every field is a place/age-cohort aggregate, and the source's own small-area suppression (`NP` withheld values, `#` caution flags, banded not exact postcode percentages) was preserved rather than reconstructed. Documented plainly in the README that this local-area breakdown is stale (~8 years, frozen at 2016-17) while the genuinely current national/state-level AIR figures exist but aren't open-licensed — a real, undisclosed-elsewhere gap, not papered over. 4 domains remain unchecked after this run (Government procurement and contracts disclosure; Adult education and community learning statistics; Public sector ICT and digital service delivery statistics; Native forestry and plantation timber production statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-09 (scheduled run): checked **State emergency declarations and major incident timeline statistics**. Career context not re-checked separately this run — the two most recent entries above already re-verified it today (2026-07-09) with no change since 25 May 2026, so picking top-of-list was sufficient. Also noticed while reading the candidate list that "Gambling and liquor licensing statistics" appears twice — once already checked and struck through (2026-07-08, nothing added) and again unstruck near the bottom (added in an earlier run's "propose 2-3 more" step, apparently without checking against the existing list first); annotated the duplicate entry rather than re-running it, so a future run doesn't burn a search budget on it again. For the actual domain: searched `data.sa.gov.au` and the open web for a SAFECOM/State Emergency Management Committee (SEMC) declared-major-emergency register — none exists as structured open data; the only concrete evidence found was narrative mentions of individual declarations (e.g. 28 January 2022, signed by the State Coordinator) buried in SASES annual-report PDFs, with no dataset, CSV or API behind them. Checked `data.sa.gov.au` CKAN search directly (`emergency declaration`, 1,343 federated results, all either Victoria/ACT/NSW-specific or generic emergency-management-infrastructure records, nothing SA-declarations-specific). Found and added a national alternative instead: the Attorney-General's Department's "Disaster Events with Category Impact and Location" (`au-historical-disaster-events`, data.gov.au, CC BY 3.0 AU confirmed via CKAN `package_show`) — a historical catalogue of 673 major disaster/incident events nationwide (1753-2014) with type, dates, location, deaths/injuries/damage and government-assistance figures, filtered to a 74-event South Australia subset. This is a genuine, licensed, downloadable dataset that covers the "major incident timeline" half of the domain reasonably well, but not the "declarations" half specifically, and it is explicitly `update_freq: never` (frozen since 2014, effectively an abandoned Knowledge Hub archive) — documented plainly in the README rather than glossed over, along with a caveat that the source's per-event `regions` field marks every affected state, not necessarily the event's actual location, so the SA subset includes some multi-state events. Both `raw/` (verbatim CSV mirror, 631,949 bytes) and `data/` (two processed files: full national + SA subset, snake_case columns, ISO dates, decoded HTML entities, added `south_australia_affected` flag) were produced via `convert.py`; no figure recalculated. Privacy check passed clean (aggregate event-level impact counts only, no individual named). Only 3 non-duplicate domains remained unchecked after this run (below the "fewer than 3" threshold once the duplicate is set aside), so proposed 3 more: Adult education and community learning statistics; Public sector ICT and digital service delivery statistics; Native forestry and plantation timber production statistics.

- 2026-07-09 (scheduled run): checked **Public libraries digital/e-resource usage statistics**. Career context re-checked directly first (Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/` still shows the same role — ASO7 Senior Data Analyst, SA Police, since March 2026 — last updated 25 May 2026, no material change since the 2026-07-06 ordering note), so no reordering of the candidate list was needed. Ran three parallel research angles to look beyond the aggregate "Digital items: 0.19m, Digital loans: 4.27m" (SA, FY22-23) figure already documented in `sa-public-library-statistics`, then adversarially verified every candidate surfaced: (1) **data.sa.gov.au** — full CKAN search plus direct download/parse of all 8 resource files inside the "Libraries Board of South Australia Annual Report data" dataset (CC BY 4.0): despite the promising name and licence, it contains zero digital/e-resource fields in any year — 2016-17 to 2020-21 vintages have only an undifferentiated "Public library One Card loans" total (all formats combined), and 2021-22 onward dropped even that, leaving pure governance-disclosure boilerplate (fraud/whistleblower/consultant-spend/complaints), the same "named-like-the-real-thing but isn't" pattern already documented elsewhere in this repo (OCPSE, etc.). (2) **State Library of South Australia's own Annual Reports** (FY2022-23, 2023-24, 2024-25 PDFs, downloaded and read directly) — these do contain a genuine, more-granular digital breakdown: "eBook and Audiobook loans" and "eMagazines and eNewspaper loans" as separate line items (e.g. FY24-25: 2,680,658 and 703,806 respectively), plus digital holds/purchases and State Library eResource-access counts — confirmed as the same underlying SA data (its FY22-23 physical-loans figure of 7,660,758 matches the existing dataset's 7.66m exactly). But none of the three PDFs checked carry any Creative Commons or other reuse licence statement — the Library's own copyright page confirms CC terms apply only where specifically stated on the website, not to these statutory reports, which read as Crown copyright. Excluded on licensing grounds, not content. (3) **Interstate/national alternatives** — Queensland's data.qld.gov.au "Queensland public libraries annual statistics" (CC BY 4.0) has the most granular open digital-format split found anywhere (per-council USAGE EBOOK/EAUDIO/EVIDEO/OTHER columns) but is Queensland-specific, not SA or national, and its most recent year is FY2021-22 (stale ~4 years); NSW's "Public Library Statistics" Excel tables (CC BY 4.0) give a per-library-service digital circulation figure but it's a bundled e-book+digital-music+e-audio+e-journal total, not split by loan type, and again NSW-specific not SA/national. No source anywhere — SA government, national NSLA/ALIA, or e-lending vendors (BorrowBox/OverDrive/Libby) — publishes a monthly or platform-level digital loan breakdown for SA specifically or nationally. Net: nothing satisfies "more granular than the existing aggregate" AND "SA-specific or national" AND "openly licensed" simultaneously, so nothing was added this run — a real, documented gap (SA's own more-granular digital breakdown exists but isn't open-licensed) rather than a fabricated dataset. This leaves 4 domains unchecked after this run: State emergency declarations and major incident timeline statistics; Gambling and liquor licensing statistics; Immunisation and vaccination coverage rates; Government procurement and contracts disclosure.

- 2026-07-09 (scheduled run): checked **Organ, tissue and blood donation statistics**. Career context re-checked directly first: the Career Path CLAUDE.md and MEMORY.md at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/` still carry the same North star (career-goals.md unchanged since 23 May 2026, career-profile.md unchanged since 23 May 2026, MEMORY.md unchanged since 25 May 2026), so no reordering of the candidate list was needed. One new file did appear there since the last check — a Role Research PDF for an "ASO5 Data Analytics Officer" role, saved 26 June 2026 — but Role Research is explicitly scoped in that vault's own CLAUDE.md as "notes on specific organisations or roles being considered", not a change to the north-star goals file, so this reads as one option being explored rather than a confirmed pivot; noting it here rather than silently reordering on the strength of a single saved PDF. Searched DonateLife/Organ and Tissue Authority (OTA) and Australian Red Cross Lifeblood/National Blood Authority (NBA) for open bulk datasets. donatelife.gov.au's HTML pages 403'd against a browser user-agent (bot detection, same pattern seen on other .gov.au sites in prior runs) but its PDF files under /sites/default/files/ downloaded directly — fetched the "Summary of the 2025 Australian donation and transplantation data" fact sheet (confirmed a genuine South Australia breakdown: 35 deceased organ donors, 91 transplant recipients, 24 living donors, 54% consent rate, 28,794 new AODR registrations in 2025) and the OTA Annual Report 2021-22, which carries an explicit, directly-quoted all-rights-reserved notice: "© Commonwealth of Australia 2022 ... This work is copyright. Apart from any use as permitted under the Copyright Act 1968, no part may be reproduced by any process without prior written permission from the Commonwealth." Not CC BY, so DonateLife/OTA donation-and-transplantation-outcome data was excluded, matching the AHPRA precedent. Checked blood.gov.au (National Blood Authority): the site itself is CC BY 4.0 (confirmed via its own /copyright page), but its own "Data and reporting" page states state-level data is only available on individual request via a data request form, and a direct full-text search of its 218-page Annual Report 2023-24 found zero state/territory blood-collection tables (only two mentions of South Australia, both in board-member biography text) — confirmed absence, not just failure to find; excluded as not bulk-accessible. Pivoted to a data.sa.gov.au CKAN search (which turned out to be querying a national federated index rather than an SA-only one — the specific dataset's own landing page 404'd on data.sa.gov.au but resolved cleanly on data.gov.au directly, so data.gov.au is cited as the authoritative source in the README) and found Services Australia's "Australian Organ Donor Register (AODR) Statistics": a historical bulk CSV archive (684 monthly files, consent + intent registrations by state and sex, CC BY 3.0 AU, live-downloaded and confirmed working, but not maintained by its publisher past July 2016) plus Services Australia's own live statistics page (CC BY 4.0, confirmed directly via servicesaustralia.gov.au/copyright), which publishes a current state × age-group × gender snapshot ("as at 31 May 2026") for both registration types. Added `datasets/au-organ-donor-register-statistics/`: `raw/` holds the unzipped historical archive (684 files) plus a full verbatim capture of the live statistics page; `data/` holds two processed files — a merged monthly time series (5,432 rows, 2007-2016, state × month × registration-type × sex) parsed from the 684 source CSVs with a handful of malformed duplicate-header artefact rows dropped, and a current snapshot (256 rows) parsed from the six HTML tables on the Services Australia page. Both README and LICENSE-DATA.md document the two-licence split and the 2016-2026 gap plainly rather than papering over it. Privacy check: every field is a place/age-band/gender aggregate registration count, no individual identified — passes clean, consistent with the `au-veteran-population-by-lga`/`au-ndis-participants-by-lga` aggregate-count precedent. This leaves 5 domains unchecked after this run (2 pre-existing plus 3 newly proposed this run, since only 2 remained — below the "fewer than 3" threshold): Public libraries digital/e-resource usage statistics; State emergency declarations and major incident timeline statistics; Gambling and liquor licensing statistics; Immunisation and vaccination coverage rates; Government procurement and contracts disclosure.

- 2026-07-09 (scheduled run): checked **Contaminated land and site contamination register statistics (EPA South Australia)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched for "contaminated land"/"site contamination register"/"notified sites" and found only out-of-state hits (NSW, ACT, WA, Victoria EPA registers) plus SA's own `groundwater-prohibition-area` package. Went directly to the EPA's own site (`www.epa.sa.gov.au`) to check the actual public register: its **Site contamination index / Public Register** page (fetched with a browser user-agent after WebFetch's default agent was blocked with a 403 — plain `curl -A "Mozilla/5.0..."` succeeded at HTTP 200, so this was a bot-detection block rather than a genuine domain block) confirmed via a live web search that the register covers every s83/s83A notification, s103E liability transfer, s103I/s103K voluntary assessment/remediation proposal, s103Z audit notification and pre-2009 SA Health Commission report for individual properties statewide, but is accessed by searching one property at a time, and the EPA's own fee schedule states viewing/accessing register information attracts "a prescribed fee, set by Parliament... indexed annually" — no bulk export or open API exists for the full register, so it fails this project's Accessible/unauthenticated-bulk-access standard the same way AHPRA's and ReturnToWorkSA's registers were excluded in prior runs. However, `data.sa.gov.au`'s **Groundwater Prohibition Area** package (`groundwater-prohibition-area`, org: Environment Protection Authority) is a genuine open, bulk, currently-maintained dataset that sits inside this same regulatory system: it's the EPA's spatial record of areas where site contamination has actually triggered a formal prohibition/restriction on taking groundwater under the Act. CC BY 4.0 confirmed both via CKAN (`license_id: cc-by`) and independently via the Location SA Metadata System's report for dataset 2182 (`AusGOAL Licensing Classification: CC BY (Attribution)`), which also supplied the field dictionary (`TYPE`, `SITE`, `EPA_REFERENCE_NUMBER`, `DATE_ESTABLISHED`, `DEPTH`) since CKAN itself publishes no separate data dictionary. 19 areas statewide, last updated 15 June 2026 (a schema change adding three fields; no features added or removed). Downloaded all three published formats (GeoJSON, Shapefile zip, KMZ) directly over HTTPS, no blocking encountered. Processed `data/sa-groundwater-prohibition-areas.geojson` by decoding the source's epoch-millisecond `DATE_ESTABLISHED` field into a human-readable ISO 8601 `DATE_ESTABLISHED_ISO` column; no other transformation needed since the GeoJSON was already a single clean file with no multi-file merge required. Privacy check: every field describes a place (area name, reference number, gazettal date, depth), no property address, Certificate of Title or landholder name — passes clean. Added `datasets/sa-groundwater-prohibition-areas/`. This covers a genuinely open but narrower slice of the domain than a full contaminated-sites register would be; documented as such in the README rather than overstating coverage. 3 domains remain unchecked after this run (Organ, tissue and blood donation statistics; Public libraries digital/e-resource usage statistics; State emergency declarations and major incident timeline statistics) — at, not below, the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-09 (scheduled run): checked **Public transport patronage and on-time-running performance statistics (Adelaide Metro)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200, current `/data/api/3/action/` path). Searched for "adelaide metro" and found the Department for Infrastructure and Transport's **Adelaide Metro Validations** package (`adelaide-metrocard-validations`), CC BY 4.0 confirmed both via `package_show` (`license_id: cc-by`, `license_url: .../4.0`) and directly against the dataset's own landing-page footer copyright text — 44 quarterly resources, banded (bucketed, e.g. `1-9`/`10-19`) total onboard validations by date, mode, route, direction, GTFS stop and payment-medium type, current through the just-published **2025 Q4** resource (Oct-Dec 2025). This covers the **patronage** (passenger-boardings) half of the domain, distinct from the static timetable/routing GTFS feed already in `sa-adelaide-metro-gtfs`, which has no boarding counts at all. Downloaded the Q4 2025 CSV directly over plain HTTPS (56 MB, no `fetch.sh` needed) and cross-checked the source's own "Banded Validations Metadata" DOC (converted via macOS `textutil`) for its documented mode-of-transport codes (1=Bus, 4=Tram, 5=Train, 8=Bike Cage, 11=Carpark/DRT) and route-direction codes (0=unknown, 1/2=direction, "Towards/Away from City" only for routes that terminate in the City) — decoded both in `data/build_data.py`. Checked the `MEDIUM_TYPE` field for a numeric-code mapping too, but found the codes in use have changed across the dataset's own history (`1`/`3` in a 2017 quarter vs `1`/`11`/`99` in the 2025 Q4 quarter, confirmed by downloading and inspecting both directly) with no DIT-published crosswalk for either era — left as the source's raw code rather than guessing a label. For the **on-time-running** half: `adelaidemetro.com.au` returned HTTP 403 (Cloudflare bot challenge, same block already noted in `sa-adelaide-metro-gtfs`'s README) to a direct fetch, so retrieved a Wayback Machine snapshot of its "On-time running data" page instead — confirmed the page publishes monthly punctuality figures only as **PNG images** (e.g. `250930-on-time-running-table.png`) back to January 2018, not as any machine-readable format, with no stated open licence; a `data.sa.gov.au` CKAN search for on-time-running/reliability/service-performance terms surfaced no SA-specific structured dataset (only NSW/ACT/QLD transit-authority datasets, and DIT's own `adelaide-metro-infoline-performance`/`adelaide-metro-complaints-answered`, which measure customer-service response times, not service punctuality) — documented as a genuine, undisclosed gap rather than force-fitted in. Scoping note: the source's full quarterly history runs January 2013 to December 2025 (44 files, ~2 GB cumulative) — mirroring and merging all of it in one run isn't practical (this repo's largest existing dataset, `sa-crime-statistics`, sits at 172 MB), so only the most recent completed quarter was mirrored/processed; the README documents the other 43 quarters' direct source URLs for a future run to extend if a longer time series is wanted. Privacy check: boarding counts are deliberately banded (not exact figures) specifically to prevent re-identification of individual trips per the source's own stated purpose — no passenger, Metrocard-number or payment-detail field of any kind. Added `datasets/sa-adelaide-metro-patronage/`. 4 domains remain unchecked after this run (Contaminated land and site contamination register statistics; Organ, tissue and blood donation statistics; Public libraries digital/e-resource usage statistics; State emergency declarations and major incident timeline statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-09 (scheduled run): checked **Health practitioner and pharmacy registration/licensing statistics (AHPRA-registered profession counts with an SA breakdown)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Checked four separate candidate sources directly, all excluded: (1) **AHPRA** itself publishes quarterly per-profession "Registrant data" tables (state/territory, age, gender breakdowns) but only as PDF documents, and its own Terms page (`ahpra.gov.au/Support/Terms`, fetched directly after its dedicated `/Legal/Copyright.aspx` page 404'd) states explicitly: "Ahpra and the Health Practitioner Boards reserve their rights with respect to the copyright of their information... You may not reproduce this information without the consent of Ahpra or the relevant Board" — an explicit all-rights-reserved, no-redistribution term, excluded on licensing grounds same as ACARA My School. (2) The **Department of Health, Disability and Ageing's Health Workforce Data Tool** (`hwd.health.gov.au/datatool/`), the current custodian of the National Health Workforce Dataset since AIHW's custodianship expired 30 June 2016, requires **Login/Register** to access any table-building functionality — confirmed via direct fetch of the page's own text ("Access the Data Tool: Login / Register") — failing this project's Accessible/unauthenticated-access standard regardless of what licence might apply once inside. (3) **AIHW's own "Health workforce" web report** (`aihw.gov.au/reports/workforce/health-workforce`, CC BY 4.0 confirmed via `aihw.gov.au/copyright` — same licence as prior AIHW datasets in this repo) turned out to be a Tableau-only embed (`data-report-view-platform="Tableau"` in the page's own markup) with no underlying downloadable data table found in the rendered page; AIHW's own NHWDS metadata page (`aihw.gov.au/about-our-data/our-data-collections/national-health-workforce-dataset`) now 404s directly, consistent with the 2016 handover to the Department. (4) On the SA side, `data.sa.gov.au`'s CKAN API (confirmed reachable, HTTP 200) was queried for "AHPRA", "health practitioner registration", "pharmacy" and "nursing midwifery medical board": the only genuinely SA-specific, on-topic organisation found was **Pharmacy Regulation Authority SA (PRASA)** (11 annual releases, 2018-2024, all `cc-by`) — but every one of its published resources (`complaints`, `executive-employment-in-the-agency`, `fraud-detected-in-the-agency`, `work-health-and-safety-and-return-to-work-performance`, `public-interest-disclosure`, `consultants`, `contractors`) is mandatory annual-report governance-disclosure boilerplate, the same category already excluded as off-topic elsewhere in this project — none contains pharmacy premises/pharmacist registration or licensing counts. No other SA health-practitioner-board dataset exists; the same CKAN search's non-SA hits were exclusively stale NSW council/board annual-report PDF scans under a `lsba` (not-open) licence. Nothing added this run. 5 domains remain unchecked after this run (Public transport patronage and on-time-running performance; Contaminated land and site contamination register; Organ, tissue and blood donation statistics; Public libraries digital/e-resource usage statistics; State emergency declarations and major incident timeline statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-09 (scheduled run): checked **Public sector enterprise bargaining and industrial relations statistics**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Checked SA sources directly: the **South Australian Employment Tribunal (SAET)**, which registers/approves public sector enterprise agreements under the *Fair Work Act 1994 (SA)*, publishes only a case-file register inspectable by contacting its Registry directly (`saet.sa.gov.au/awards-agreements-and-registers/enterprise-agreements/`) — not a downloadable dataset. The **Attorney-General's Department**'s "Current agreements" page (`agd.sa.gov.au/industrial-relations/current-agreements`) returned HTTP 403 to a direct fetch, but an earlier search already established it as a document-listing page (PDF links to individual agreements), not structured data — consistent with the pattern. A `data.sa.gov.au` `package_search` for "enterprise agreement" and "industrial relations" surfaced only unrelated federated noise (State Library photograph collections) and one non-open dataset — DEWR's "Workplace Agreements Database: Trends in Enterprise Bargaining" (harvested from `data.gov.au`), whose CKAN record states `license_id: "other-unpublished"`, `isopen: false` — excluded on licensing grounds. No genuine open SA-specific dataset exists for either half of this domain (working days lost to disputes, or enterprise agreement coverage rates). Found ABS's **Industrial Disputes, Australia, March 2026** quarterly release instead: confirmed reachable directly (`abs.gov.au`, HTTP 200), licence confirmed verbatim from ABS's own copyright page (CC BY 4.0, same as prior ABS datasets in this repo). Downloaded all 8 published time-series workbooks (Tables 1, 2a-2b, 3a-3b, 4a-4c) directly over plain HTTPS, no `fetch.sh` needed. Tables 3a/3b carry an explicit South Australia column (confirmed: March quarter 2026 reads 0.6 thousand working days lost, 0.6 per 1,000 employees — matches the release's own published "States and territories" summary text exactly); Table 4a usefully isolates "Enterprise Bargaining related" disputes from non-EB-related ones, directly relevant to the domain's industrial-relations framing, though it is a cause-of-dispute category, not an agreement-coverage-rate measure. Wrote `convert.py`: unpivoted each table's wide ABS Time-Series-Workbook layout (one column per series, semicolon-delimited header descriptors) into long format using the source's own header/unit/series-ID metadata, no values recalculated. Spot-checked national and SA figures against the release's own headline text — exact match. Noted as a genuine, undisclosed gap: no open dataset (SA or national) publishes an enterprise-agreement-*coverage-rate* measure at all; Table 4a's Tables 4a-4c also lag Tables 1-3 by one quarter in the source's own publication schedule (ends December 2025 vs March 2026), documented as the source's own limitation, not a mirroring gap. Privacy check: every row is a time-period/geography/industry/cause aggregate, never an individual employer, union or employee record — passes the standing individual-identifier rule cleanly. Added `datasets/au-industrial-disputes-working-days-lost/`. 5 domains remained unchecked after marking this one (above the "fewer than 3" threshold), but 2 more were added anyway for a healthier backlog: public libraries digital/e-resource usage statistics, and state emergency declarations/major incident timeline statistics (distinct from the recovery-funding and per-agency incident-count data already covered).

- 2026-07-09 (scheduled run): checked **Natural disaster and emergency recovery funding statistics**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. First attempted `data.sa.gov.au`'s CKAN API at its previously-used path (`https://data.sa.gov.au/api/3/action/...`) and got a bare HTTP 404 — this is new: the site now serves a Drupal 10 marketing page at its root instead of the CKAN catalogue. Found the catalogue has moved to `https://data.sa.gov.au/data/api/3/action/...`; confirmed reachable there (HTTP 200) and noted the path change in the new dataset's README so future runs' scripted queries aren't caught out by it. Searched that API for "disaster" (1,291 federated hits, mostly noise per the now-familiar data.gov.au-federation pattern) and found **National Emergency Management Agency (NEMA)**'s "Location-Based Disaster Assistance Payments" package (`disaster-history-payment-by-lga`), harvested from `data.gov.au` — confirmed licence directly against `data.gov.au`'s own `package_show` API (not just the SA mirror): `license_id: cc-by-2.5`, `license_title: Creative Commons Attribution 2.5 Australia`, `license_url: http://creativecommons.org/licenses/by/2.5/au/` (same licence family already used for `au-waste-resource-recovery`). Downloaded the CSV resource directly over plain HTTPS from `data.gov.au` (no `fetch.sh` needed, 1,087,769 bytes, 5,825 rows) — a single already-tidy file covering all of Australia, LGA/suburb-level, 45 declared disaster events, 14 Feb 2019 to 16 Apr 2026, with claim-processing counts (eligible/ineligible/finalised/cancelled/withdrawn/onhand/incoming/received/applications) and dollars granted/paid by payment type (AGDRP, DRA, small-business/primary-producer grants and loans, and event-specific schemes). South Australia has 110 rows across 16 LGAs and 2 declared events: the November 2019 bushfires (Cudlee Creek/Kangaroo Island/Yorketown) and the November 2022 River Murray floods (AGRN 1042) — distinct from the SES/CFS/MFS incident-count data already covered under "Emergency services" (this measures recovery *funding*, not emergency *response*). Wrote `convert.py`: snake_case column names, decoded the `Location Type` code (LGA/SAL/NONE) into a readable label, and normalised `$`/thousands-separator formatting on the numeric fields, while explicitly preserving NEMA's own small-cell privacy-suppression markers (`<20` claims, `<$20,000.00` → normalised to `<20000.00`, same threshold) rather than estimating or blanking them. Privacy check: every row is a location/disaster/payment-type aggregate, never a per-claimant record — passes the standing individual-identifier rule cleanly. Also noted, but did not pursue further, a stale 2018 SA-specific "Disaster Impacts Statistics – Pinery Bushfire" dataset (Dept of Human Services, marked inactive, single-event-only) — narrower and staler than the national NEMA dataset added here. Added `datasets/au-disaster-recovery-payments-by-lga/`. After marking this domain checked, only 2 domains remained unchecked (enterprise bargaining/industrial relations; health practitioner/pharmacy registration) — below the "fewer than 3" threshold, so proposed 3 new candidate domains this run (public transport patronage/on-time-running performance, EPA contaminated land register statistics, and organ/tissue/blood donation statistics), bringing the unchecked count back up to 5.
- 2026-07-09 (scheduled run): checked **Roadside breath and drug testing statistics (SAPOL / Department for Infrastructure and Transport)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run (no `fetch.sh` needed). Searched the CKAN API for "breath test" / "roadside drug" — both returned irrelevant federated-harvest noise (data.sa.gov.au now federates data.gov.au broadly, surfacing e.g. seal-tracking and NSW Police Integrity Commission records for these terms) — so switched to checking SAPOL's own organisation page directly instead. Found SAPOL's "Annual Reporting Data" package includes a "Road Traffic Act 1961" resource (CC BY 4.0, confirmed via `package_show`) breaking out "Static" and "Mobile" **driver screening test** (random breath test) volumes by financial year, 2012-13 to 2024-25 in one file — cross-checked against SAPOL's own 2019-20 annual report "agency's performance" table (fetched via the Wayback Machine after a direct `police.sa.gov.au` fetch returned HTTP 403) confirming this is SAPOL's official RBT-volume disclosure and that Static+Mobile sums to the published Total in every year. Added `sa-driver-screening-tests` (39-row tidy long-format CSV, `convert.py` transposes the source's year-as-columns layout). This covers only the **breath**-testing half of the domain, though: no genuine open roadside-drug-testing (RDT) time series exists — SAPOL's public communications report drug-test activity only as one-off cumulative totals in news releases (not a licensed structured dataset), and the federal National Road Safety Data Hub's "Police enforcement" page (`datahub.roadsafety.gov.au`) shows only a Power BI–embedded single-year (2024) per-10,000-licence-holder rate with no downloadable raw counts or stated dataset licence — documented as a genuine, undisclosed-elsewhere gap in the new README rather than force-fitted in. No positive-detection/BAC-over-limit counts are published alongside the SAPOL volume figures either, so that half of the domain title also remains a documented gap.
- 2026-07-09 (scheduled run): checked **Alcohol and other drug treatment service statistics (Drug and Alcohol Services SA / SA Health)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Searched `data.sa.gov.au`'s CKAN API for "drug and alcohol", "DASSA", "treatment episodes": found no DASSA-published treatment-service dataset — only tangential hits (food/controlled-drugs-branch enforcement actions, a stale 2007-2013 "SASP Target 81 - Alcohol Consumption" indicator, and a genuinely current, CC BY 4.0, SA-specific "Drug Use in Adelaide Monitored by Wastewater Analysis" dataset that measures population drug consumption via sewage sampling rather than treatment service activity — noted in the new README as related-but-distinct, not pursued this pass). Confirmed directly via `sahealth.sa.gov.au` that DASSA/SA Health publish drug and alcohol statistics narratively (PDF statistical bulletins, prose summary pages) rather than as downloadable structured data; a direct `WebFetch` of SA Health's own "drug and alcohol service activity and performance data" page returned HTTP 403. Substituted AIHW's national Alcohol and Other Drug Treatment Services National Minimum Data Set (AODTS NMDS) instead, current 2024-25 edition published 25 June 2026, CC BY 4.0 confirmed via `aihw.gov.au/copyright`. AIHW's own data-listing page renders its download links via a client-side JS call to an internal `/api/search/all-downloadable-resources` endpoint rather than static HTML — reverse-engineered the request payload from the page's own webpack bundle (`dataOrResource.js`) to call it directly and retrieve exact, current `getmedia` download URLs rather than relying on stale/guessed filenames; both target workbooks then downloaded directly over plain HTTPS (no `fetch.sh` needed, domain fully reachable this run). Extracted only the South Australia sheets from each of the two "state and territory" workbooks (`SCR.Clients (state and territories numbers)`, 81 sheets total across all states; `ST.State and territories (episodes)`, 153 sheets total) — 10 SA client-count tables (2013-14 to 2024-25) and 19 SA closed-treatment-episode tables (mostly 2015-16 to 2024-25, several 2024-25-only cross-tabs). Wrote a general-purpose reshape script (`data/build_data.py`) handling both table shapes found in the source (financial-year-as-columns time series, and category-as-columns single-year cross-tabs, including 3-variable tables like referral source × treatment type × client type) into two consistent long/tidy CSVs, each row traceable back to its exact AIHW table number via a `source_table` column; spot-checked multiple extracted values directly against the source workbook cells (e.g. SA 2024-25 alcohol principal-drug-of-concern clients: 2,755; SA 2024-25 counselling episodes referred via self/family for own drug use: 2,097) — all matched exactly. Added `datasets/au-alcohol-other-drug-treatment-services/`. Privacy check: every row is a statewide aggregate count from AIHW's de-identified, statistically-linked collection — no client-level records. 4 domains remain unchecked after this run (Roadside breath and drug testing statistics; Natural disaster and emergency recovery funding statistics; Public sector enterprise bargaining and industrial relations statistics; Health practitioner and pharmacy registration/licensing statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-09 (scheduled run): checked **Public-sector integrity and corruption oversight statistics (Independent Commission Against Corruption SA / Office for Public Integrity)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Queried for "ICAC"/"corruption"/"integrity" and found two CKAN organisations: `independent-commission-against-corruption` (current Commission structure, post-2021 restructure, 11 datasets) and `independent-commissioner-against-corruption-and-office-for-public-integrity` (predecessor structure, 10 datasets, 2013-14 to 2020-21 data) — both CC BY 4.0 confirmed via each package's own `license_id`/`license_url`. Unlike most other agencies checked in this project, ICAC/OPI's own statutory annual-reporting obligations (s45 of the ICAC Act, s31 of the Police Complaints and Discipline Act 2016) require it to publish genuine complaint/investigation/referral counts, not just governance-disclosure boilerplate (workforce/executive/fraud/consultant disclosures, which also exist here but were excluded as off-topic, consistent with how this project has treated those elsewhere). Fetched and cross-checked six source CSV/XLSX resources directly (no `fetch.sh` needed): the predecessor org's "reporting required under any other act" (section i) and PCD Act files (8-year and 4-year time series respectively, 2013-14 to 2020-21), and the current org's "general nature of matters investigated," "file closure" (12-month KPI), "matters referred to SAPOL" and "PCD Act sanctions" files (each already a self-contained multi-year series through 2022-23). Built `sa-icac-corruption-oversight-statistics`: 13 tidy CSVs via a column-position-verified parser (`data/build_data.py`), cross-checked against each source table's own "Total" row (e.g. 2022-23 formal-investigation categories sum to 44/44; 2021-22 SAPOL referrals sum to 33/33). Privacy check applied per this project's standing individual-identifier rule: the PCD Act sanctions table is case-level (one row per SA Police officer's disciplinary outcome) but uses only a per-case "reference no." reused across reporting periods (not a persistent personnel ID) and a breach/outcome description — no officer names appear anywhere in the source. Two known gaps disclosed in the README: (1) the two reporting eras use different category taxonomies after the 2021 restructure, kept separate rather than force-merged; (2) the older era's SAPOL-referral category breakdown (2013-14 to 2020-21) drops row labels in the source spreadsheet itself, so only year-level totals are included for those years, not a category split. After marking this domain checked, only 2 domains remained unchecked (Alcohol and other drug treatment service statistics; Roadside breath and drug testing statistics) — below the "fewer than 3" threshold, so proposed 3 new candidate domains this run (natural disaster/emergency recovery funding statistics, public sector enterprise bargaining/industrial relations statistics, and health practitioner/pharmacy registration statistics), bringing the unchecked count back up to 5.
- 2026-07-09 (scheduled run): checked **Second-hand dealer, motor vehicle dealer and credit provider licensing statistics (Consumer and Business Services)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check (including this same day's earlier BDM run), so no reordering of the candidate list was needed; proceeded top-of-list as usual. This domain bundles three distinct licence families, checked separately. **Second-hand vehicle dealers / motor vehicle dealers (SA CBS):** confirmed via `data.sa.gov.au`'s CKAN API and a direct fetch of the **Annual Report CBS** dataset page that the only candidate — its "Second-hand Vehicle Dealer Act data" resource — is the same 2017-18 snapshot (last updated 18 October 2018) already found and excluded in this project's 2026-07-06 and 2026-07-08 Consumer and Business Services checks; nothing newer exists, so nothing new added for this sub-angle. **Consumer credit / finance broker licensing:** confirmed this is a federal matter, not an SA CBS function — nationalised under the National Consumer Credit Protection Act 2009 (Cth) since 1 July 2010, explaining why no SA register exists. Found ASIC's **Credit Licensee Dataset** and companion **Credit Representative Dataset** on `data.gov.au` (both CC BY 3.0 AU, confirmed directly via each dataset's own JSON-LD metadata; `data.gov.au`'s dataset landing pages returned HTTP 403 to `WebFetch` but were directly reachable via plain `curl`/HTTPS, as were the actual CSV/PDF resource downloads — no `fetch.sh` needed). Both are genuinely current: weekly-refreshed every Thursday since March 2025, this extract dated 2 July 2026 (`credit_lic_202607.csv`, 4,403 rows; `credit_rep_202607.csv`, 47,435 rows), with a `state` field giving a clean South Australia cut (196 licensees, 2,853 representatives). Privacy check: both registers legitimately mix organisation and named-individual entries per ASIC's own documented format logic ("Last name, First name" for persons) — applied this project's standing individual-name redaction (same treatment as `sa-mineral-tenements`), replacing 383 of 4,403 licensee names and 29,811 of 47,435 representative names with a fixed marker in both `raw/` and `data/`, disclosed as a deviation from the usual untouched-raw convention; organisation names left as published. Added `au-consumer-credit-licensing`: two tidy CSVs (`~`-delimited multi-value fields split and rejoined, `APPR`/`SUSP` status and EDRS scheme codes decoded, dates reformatted to ISO) built by `data/redact_and_convert.py`. One documentation discrepancy disclosed in the README: ASIC's own help-file PDF lists only 4 legacy EDRS codes (FOS/COSL/CIO/FOS:COSL) but the actual current data uses an undocumented `AFCA` code (the body that replaced all three in November 2018) — decoded based on external knowledge of the EDR-scheme history, not the stale help file. 3 domains remain unchecked after this run (Public-sector integrity and corruption oversight statistics; Alcohol and other drug treatment service statistics; Roadside breath and drug testing statistics) — at, not below, the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-09 (scheduled run): checked **Births, deaths and marriages registration statistics (Attorney-General's Department / South Australian Registry of Births, Deaths and Marriages)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Queried it for "births deaths marriages registrations" — the only hits under South Australian orgs were `popular-baby-names` (Attorney-General's Dept, CC BY, top-100 names by year, current to 2025) and the standard non-BDM annual-report-disclosure datasets; listed all 77 datasets under the Attorney-General's Dept org directly (`package_search?fq=organization:attorney-general-s-dept`) and confirmed none is a registration-count series. By contrast the same search surfaced genuine registration-by-month/year datasets from NSW, Victorian, Tasmanian and ACT Registries of Births, Deaths and Marriages (all indexed in `data.sa.gov.au`'s federated search) — confirming SA's own registry is the outlier in not publishing this, not a search-term miss. Substituted three related ABS national publications, each broken out by state: *Births, Australia, 2024* and *Deaths, Australia, 2024* pulled via the ABS.Stat SDMX Data API (`data.api.abs.gov.au`, dataflows `BIRTHS_SUMMARY` 1975-2024 and `DEATHS_SUMMARY` 1971-2024, both reachable directly this run), and *Marriages and Divorces, Australia, 2024*'s own data cube workbook (2020-2024; ABS reduced this publication to 3 tables from 2022 onwards per its own release notes — noted as a real source-side reduction, not something dropped in processing). Added `au-births-deaths-marriages-registrations`: 6 tidy CSVs (births/deaths long time series decoded from ABS.Stat's numeric dimension codes against their own published codelists; marriages/divorces reshaped from the source workbook's multi-section layout into long format with a `section` column), all CC BY 4.0, confirmed directly from `abs.gov.au`'s copyright page this run, South Australia broken out in every table, spot-checked against the ABS releases' own headline figures (SA 2024 births 18,444, deaths 15,739, marriages 7,500, divorces 3,147 — all matched exactly). One minor, disclosed imprecision: two rows in the marriage-indicators file ("Median age at marriage" for males/females) inherit an imprecise `section` label from the source sheet's layout — values unaffected, documented in the dataset's Known limitations.

- 2026-07-08 (scheduled run): checked **Coroners Court death investigation statistics (Courts Administration Authority — Coroners Court case numbers, cause-of-death category breakdowns, inquest counts and timeframes)**. Career context re-checked directly first: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Listed all 15 datasets published by the Courts Administration Authority (org `caa`) directly via `package_search?q=organization:caa` — one is the whole-of-courts `caa-ar-at-a-glance` series already used by `sa-courts-at-a-glance` (Coroners Court appears there as a single aggregate line only), the other 14 are the standard mandatory annual-report-disclosure template (fraud, contractors, consultants, complaints, executive employment, etc.) documented repeatedly elsewhere in this repo's run log — none is a dedicated coronial dataset. Also checked `criminal-civil-matters` (Attorney-General's Dept org, 77 datasets listed) — explicitly excludes Coroners, and is stale (metadata last modified 2015, covering 2008-09 to 2012-13). Checked the National Coronial Information System (`ncis.org.au/about-the-data`) directly: confirmed it is application-only access for approved death investigators/researchers, not open data — aggregate reports are fee-for-service. No genuine SA-CAA-published Coroners Court dataset exists. Substituted the Productivity Commission's Report on Government Services 2026, Part C Section 7 (Courts) — its 37 data tables include Coroners' courts as a distinct row (alongside Supreme/District/Magistrates/Children's) in 17 of them (lodgments, finalisations, both per-100k rates, expenditure ×4, backlog, on-time processing, inquest-attendance rate, clearance ×2, judicial officers ×2, FTE staff, expenditure-per-finalisation ×2). Downloaded both the source xlsx (`rogs-2026-partc-section7-courts-data-tables_0.xlsx`) and its companion wide-format CSV (`rogs-2026-partc-section7-courts-dataset_0.csv`) directly from `assets.pc.gov.au` over plain HTTPS — no `fetch.sh` needed, same reachability as every other RoGS-sourced dataset in this repo. Added `datasets/au-coroners-court-statistics/`: `raw/` holds both untouched source files (all 37 tables/court types, not pre-filtered); `data/` holds the companion CSV filtered to `Court_Type == "Coroners'"` and unpivoted from wide-by-jurisdiction to long format via `convert.py` — 17 per-table CSVs, a 2,592-row `all-tables-long.csv`, a 300-row SA-filtered `south-australia.csv`, and a `table-index.csv`. Spot-checked SA 2024-25 figures against the raw CSV cells directly: 3,848 lodgments, 4,174 finalisations, 108.5% clearance rate, 2,635 pending caseload, 2.3 average inquest attendances per finalisation — all matched exactly. CC BY 4.0 confirmed via `pc.gov.au/copyright` (same licence text already used for `au-ambulance-services-performance` and `au-child-protection-services`). Noted plainly in the README: RoGS reports court-caseload/timeliness/resourcing statistics only — it does not include a cause-of-death category breakdown (that would require NCIS, which isn't open), so that specific facet of the original candidate-domain description remains a genuine, disclosed gap rather than a silently dropped one. Privacy-checked: every figure is a jurisdiction-level aggregate (count/rate/%/$/FTE), no case-level or individual-identifying data. Running low on unchecked domains (2 remained before this run), so added 3 more to the candidate list per the task's standing instruction: public-sector integrity/corruption oversight statistics (ICAC SA/OPI), alcohol and other drug treatment service statistics (Drug and Alcohol Services SA), and roadside breath/drug testing statistics (SAPOL/DIT) — the latter two chosen partly for relevance to Rin's SAPOL analytics remit, consistent with the existing career-prioritisation ordering note.

- 2026-07-08 (scheduled run): checked **Concessions and cost-of-living support statistics (Department of Human Services — Cost of Living Concession, Emergency Electricity and Gas Payment recipient counts/expenditure)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched `package_search` for "concession", "cost of living concession", "energy payment", "emergency electricity", "electricity gas payment" and "concessions": found exactly two genuine, on-topic candidates, both published by the Department of Human Services' Concessions and Anti-Poverty Unit and both `license_id: cc-by` confirmed via `package_show` — **Cost of Living Concessions** (COLC, 3 DOC/PDF resources, FY2015-16 to FY2017-18) and **Emergency Electricity Payments Scheme** (EEPS, 2 DOC resources, FY2015-16 to FY2016-17). Cross-checked against a full listing of all 40 packages in the DHS CKAN organisation: no newer or renamed concessions/EEPS dataset exists anywhere in the org's catalogue. Also checked the live agency site directly rather than assuming from the stale CKAN metadata alone: `dhs.sa.gov.au/services/concessions-and-support` returned an initial 403 to a plain `curl`, but a retry with a browser user-agent, `Accept-Language` header and forced HTTP/1.1 got a 301 that resolved to a generic `dhs.sa.gov.au/how-we-help` landing page — confirming no live current-statistics page exists to fill the post-2018 gap, not just a blocked request. Downloaded all 5 raw DOC/PDF resources directly over plain HTTPS (no `fetch.sh` needed). Inspected every file directly before writing conversion code: the two COLC `.docx` files (FY2015-16, FY2016-17) turned out to contain no tables at all, only prose paragraphs stating the same 4 figures each year (total recipients, homeowner-occupier count/%, tenant count/%, Commonwealth Seniors Health Card (CSHC) subset count/%) — transcribed exactly as stated rather than left unparsed. The two EEPS `.docx` files contain genuine Word tables (by-reason, by-region, and — 2016-17 only — a full ATSI identity-category breakdown). The most recent COLC/EEPS resource (FY2017-18) turned out on inspection to actually be a single combined PDF, *Concessions and Support Services Open Data 2017/18*, covering both COLC and EEPS for that year **plus three further DHS programs from the same report** (GlassesSA, Funeral Assistance SA, Personal Alert Systems Rebate Scheme) — extracted with `pdfplumber` and included as a disclosed scope extension beyond the domain's original COLC/EEPS framing, since they're the same licence, same report, same year, and would otherwise have been left on the table. Found and disclosed two genuine source-data quirks rather than silently normalising them: (1) COLC's CSHC figure is described as *"a subset of homeowner-occupiers and tenants"* (overlapping, non-additive) in the FY2015-16/2016-17 editions, confirmed by homeowner + tenant summing exactly to the stated total in FY2015-16 (153,846 + 28,973 = 182,819), but in FY2017-18 the same three categories instead sum exactly to that year's total (134,957 + 7,916 + 31,979 = 174,852) — CSHC has become a mutually-exclusive category that year, a genuine methodology change preserved via a `category_relationship_to_total` column rather than merged as if directly comparable; (2) FY2016-17's own stated COLC total (180,513) doesn't reconcile with its own homeowner+tenant figures (180,523, a 10-recipient gap) — reported exactly as published, not corrected. EEPS's FY2016-17 by-reason and by-region tables also carry an extra "not recorded" residual category absent from the other two years, per the source's own note about that year's data-system migration losing some values — kept as its own explicit category. Wrote `convert.py` producing 10 tidy CSVs; validated before finalising that EEPS's by-reason and by-region rows sum exactly to each year's own stated total-applications figure (1,170 / 1,020 / 1,014) and that the 2016-17 ATSI detail table's five rows sum to 1,020 exactly. Added **`datasets/sa-concessions-cost-of-living-support/`**, a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in every source and converted file is a whole-of-program aggregate count or percentage by financial year and category (household type, application reason, region, ATSI identity category, month, lens type) — no individual recipient's name, address, payment amount or case-level detail anywhere. **Genuine, disclosed gap:** despite the domain brief's own framing naming "expenditure", no dollar/payment-amount figure is published anywhere in either source — both report recipient/application counts only. 3 domains remain unchecked after this run (Coroners Court death investigation statistics; Births, deaths and marriages registration statistics; Second-hand dealer, motor vehicle dealer and credit provider licensing statistics) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run, consistent with how this repository has treated the same boundary case in prior runs.
- 2026-07-08 (scheduled run): checked **Boating and marine safety statistics (Department for Infrastructure and Transport / Marine Safety SA — recreational vessel registrations, boating safety incidents, mooring licences)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched `package_search` for "boating", "vessel", "boat licence", "mooring", "boating incident" and "marine safety", plus an organisation-scoped sweep of all 61 packages published by the Department for Infrastructure and Transport (`department-of-planning-transport-and-infrastructure`): confirmed DIT publishes exactly 2 boat-related datasets, both `license_id: cc-by` confirmed via `package_show` — **Boat Registrations** (`RBA011`, 11 resources: one XLSX per financial year FY2015-16 to FY2024-25, published 3 July 2025, plus a bundled `cdata.saregisteredboats2.zip` covering calendar years 2007-2014 and financial years FY2007-08 to FY2014-15) and **Boat Licence Statistics** (`RBA014`, a single cumulative CSV). The broader "mooring", "boating incident" and "marine safety" queries surfaced only other states' datasets (NSW, Tasmania, WA) via the portal's federated national index, not South Australian results — consistent with the shared national-index noise already documented in prior runs (e.g. the SAPOL missing-persons check). Downloaded all 11 Boat Registrations resources plus the licence-statistics CSV directly over plain HTTPS (no `fetch.sh` needed). Inspected every workbook directly with `openpyxl` before writing conversion code: the 10 FY2015-16–FY2024-25 files share a consistent 18-19-sheet layout (one sheet per breakdown dimension — hull type/make/material/length/breadth, Australian Builder's Plate status, engine configuration/type/fuel/make/power/capacity plus a secondary-engine variant of each, fee-length bands, postcode, location used), each with a `New Boat Reg`/`% New Boat Reg`/`Boats Currently Reg`/`% Boats Currently Reg` header block whose column position genuinely differs between the `Location Used` sheet and every other sheet — handled by locating each header by scanning for the literal cell text `New Boat Reg` rather than assuming a fixed column position. `Location Used` itself is missing from two years' workbooks (FY2020-21, FY2021-22) — a genuine gap in what DIT published those years, not a parsing failure. The bundled 2007-2015 zip uses a materially simpler, incompatible 11-sheet layout (no `%` columns, no `ABP`/fee-band/secondary-engine/postcode/location breakdowns) — mirrored in `raw/` for provenance but not merged into `data/` this run, a disclosed scope decision consistent with the precedent set by `sa-vehicle-registrations-and-licences` leaving its own older annual editions unmerged. Wrote `convert.py` producing one merged long-format table across all 10 consistent years (13,665 rows); validated before finalising that for every (year, category) combination, `new_boat_registrations ÷ new_boat_registrations_pct` reconstructs the same implied grand total across every row within floating-point tolerance — 188 (year, category) groups checked, 0 inconsistent. Separately converted the Boat Licence Statistics CSV (which turned out, on inspection, to actually run through issue year 2025 despite the CKAN package's own description text still reading "1975 - 2014" — a stale metadata description, not real staleness, documented explicitly in the README rather than silently corrected or trusted at face value) into a tidy 52-row table (51 issue-years plus one totals row); validated that `female + male + unknown` reconstructs each row's own `year_sub_total` exactly, including the totals row (362,061 total currently-valid SA boat licences: 71,176 female, 290,843 male, 42 unknown gender). Checked the other two sub-angles named in this domain directly rather than assuming from the registrations/licence find alone: web-searched for SA boating safety incident/fatality statistics and mooring-licence data specifically. DIT's own [2025-2030 Recreational Boating Safety Strategy PDF](https://www.dit.sa.gov.au/__data/assets/pdf_file/0004/1452118/SA-Recreational-Boating-Safety-Strategy-20252030.pdf) and news releases cite aggregate fatality/compliance-check figures (65 boating-related drowning deaths in the 20 years to June 2024, a further 5 trauma/collision deaths in the preceding 10 years, over one-third of 18,110 vessels checked found non-compliant) but only as prose inside PDF strategy documents and press releases, not as a downloadable structured dataset, and with no stated open-reuse licence — checked directly, not assumed from the search-result summary alone. No SA mooring-licence dataset was found on `data.sa.gov.au` under any organisation. Added **`datasets/sa-boat-registrations-and-licences/`** (2 tidy CSVs; `raw/` mirrors all 11 registrations resources plus the licence-statistics CSV), a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in every source and converted file is a postcode/category-level aggregate count — no owner name, boat registration/hull identification number, licence number, or address finer than postcode anywhere in either dataset. 4 domains remain unchecked after this run (Concessions and cost-of-living support statistics; Coroners Court death investigation statistics; Births, deaths and marriages registration statistics; Second-hand dealer, motor vehicle dealer and credit provider licensing statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Gambling and liquor licensing statistics (Consumer and Business Services / Office of Problem Gambling — liquor licence counts by category, gaming machine numbers, problem gambling support-service usage)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran three parallel sub-topic searches covering the domain's three distinct angles rather than one linear search. **(1) Liquor licence counts by category (on-premises/off-premises/club/special circumstances):** `data.sa.gov.au`'s CKAN API was directly reachable this run; searched `package_search` with 16 query variants ("liquor", "liquor licence", "gambling", "OLGC", "office of liquor", "licence categories", etc.) plus a full `organization:attorney-general-s-dept` catalogue sweep (77 packages reviewed). Found only raw, per-licence register extracts — **Liquor & Gaming Licences** and **Liquor & Gaming Applications** (both CC BY 4.0, confirmed via `package_show`) — individual-licence-level rows, not aggregated counts by category, and both dead since August 2019 (`metadata_modified` and last resource both 2019-08-19, confirmed identically mirrored on `data.gov.au` under the AURIN harvest, so no newer version exists anywhere). A "Liquor Licensing Act data" resource inside the **Annual Report CBS** package turned out on download-and-inspection to be barring-order counts by suburb (2017-18 only), not licence-category counts — also stale. `cbs.sa.gov.au` itself initially returned HTTP 403 to both curl and WebFetch; worked around by using a browser User-Agent, an Accept-Language header, and `--http1.1` (forcing HTTP/1.1 resolved the block). Once reachable, CBS's own "Gaming statistics" page proved genuinely live (quarterly PDFs through Q3/Q4 2025-26) but covers gaming machines only, not liquor, and carries no open-licence statement anywhere on the site. No liquor-licence-category-counts dataset exists as open data anywhere found. **(2) Gaming machine (EGM/poker machine) numbers, venue counts or expenditure:** confirmed via direct API search (`package_search` for "gaming machine licence statistics" and "net gambling revenue" both returned 0 results) that CBS's genuinely current quarterly **Gaming Machine Licence Statistics** PDF series (licences granted/entitlements/live licences/live machines by Casino/Clubs/General/Hotel, continuous FY2009-10 through Q1 FY2025-26, confirmed by downloading and text-extracting the actual Q1 2025-26 PDF via a Wayback Machine snapshot) and the companion annual **Net Gambling Revenue by LGA/postcode** series are not indexed on data.sa.gov.au's CKAN catalogue at all, and `cbs.sa.gov.au`'s only site-wide legal text is a generic 2018 "Copyright & Disclaimer" page with no Creative Commons or open-reuse grant — a standard liability disclaimer, not an open licence. The data is real, current and exactly on-topic, but fails this repository's standing CC-BY-or-equivalent requirement; the only CC BY-licensed gambling-adjacent datasets on data.sa.gov.au remain the same stale August-2019 licence/application registers from angle (1), which don't cover machine counts anyway. **(3) Problem-gambling support-service usage statistics:** searched `data.sa.gov.au` (11 query variants including "problem gambling", "self-exclusion", "office for problem gambling") and `data.gov.au`; the only gambling-related SA dataset found, **Contact details for Problem Gambling Services** (Department of Human Services, CC BY 4.0, confirmed via `package_show`), is a service-locations/contact directory (ArcGIS points + DOCX), not usage/utilisation statistics. Checked AIHW's live "Gambling" welfare-topic page (updated 16 Oct 2025) directly — a rich narrative report citing HILDA, the AGRC panel survey and Australian Gambling Statistics/QGSO expenditure data, but it reports no gambling-help-service usage or self-exclusion counts and is not itself a downloadable dataset. SA Health's current gambling-support service (now the Statewide Gambling Therapy Service, run by Southern Adelaide Local Health Network/Flinders Medical Centre, found via a DuckDuckGo site-search workaround after SA Health's own JS-rendered search returned nothing useful) publishes no usage statistics either; the Australian Gambling Research Centre (AIFS) site returned a Cloudflare 403 and could not be checked directly. **Net result: nothing added.** All three sub-angles were confirmed empty of any suitable (open, currently-live, CC-BY-or-equivalent) dataset — consistent with the precedent set by the SAPOL missing-persons check (commit `d565aac`), where no genuine open dataset existed and nothing was added rather than force-fitting a stale or unlicensed source. Privacy check: n/a — no candidate dataset from any of the 3 sub-angles was judged suitable, so nothing was added or needs checking for individual-identifying fields; the one directory-style dataset considered (Contact details for Problem Gambling Services) contains only service-location/contact fields, no client-level or individual data, but was excluded on relevance grounds regardless. 2 domains remained unchecked after this run (Boating and marine safety statistics; Concessions and cost-of-living support statistics) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Coroners Court death investigation statistics (Courts Administration Authority), Births, deaths and marriages registration statistics (Attorney-General's Department / SA Registry of Births, Deaths and Marriages), and Second-hand dealer, motor vehicle dealer and credit provider licensing statistics (Consumer and Business Services).

- 2026-07-08 (scheduled run): checked **Public sector executive remuneration and senior officer disclosures (SA Government — executive/senior-officer salary bands and headcount by agency)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched `package_search` for "executive remuneration", "senior officer", "executive employment" and "senior executive service", plus the Office of the Commissioner for Public Sector Employment's (OCPSE) own 16-package organisation catalogue directly: confirmed (consistent with the 2026-07-06 "SA public sector workforce" finding) that **no whole-of-sector executive-remuneration register exists** — OCPSE publishes only its own small agency's disclosures, not a statewide one. Instead, the "executive employment" search alone returned exactly 100 hits, nearly all of them genuine — one *individually-published, standalone* CKAN dataset per SA agency, each disclosing that agency's own executive/Senior Executive Service (SAES) headcount by classification band as part of its statutory annual reporting (a mandatory disclosure requirement, not a discretionary one). Rather than leave this as an undifferentiated "fragmented, not force-fitted" gap (the 2026-07-06 precedent for the parallel workforce domain), judged that assembling the individually-published agency disclosures into one merged table was itself a genuine, valuable act of open-data curation — exactly the kind of thing this repository exists to do — so built it, scoped to the ~75 CKAN packages whose name/title unambiguously identifies them as a dedicated executive-employment dataset (excluding ~20 packages where the same disclosure sits as one resource inside a larger, generically-named multi-resource "annual report data" bundle alongside unrelated disclosures like fraud/complaints/contractors — SA Police, Treasury and Finance, every SA Health Local Health Network, Funds SA, the CTP Regulator, PRASA, etc. — isolating those was judged out of scope for one run and left as a documented further-pass candidate). Fetched full `package_show` metadata for 42 candidate packages directly via the CKAN API, then downloaded all CSV/XLSX resources whose name contained "exec" (143 files, ~1MB total, no `fetch.sh` needed) — deliberately keeping full historical editions per agency where published (up to 14 resources for one agency), not just the latest year. **Genuine engineering effort went into the parser**, since source table shapes vary enormously across ~90 independently-run agency annual-report processes: wide classification-rows-by-year-columns, long Classification/Year/Number tables in varying column order, year-rows-by-classification-columns (transposed), and free-text "As at 30 June [date]" single-snapshot tables with no formal header row at all — a multi-strategy parser (tried in order: long-format, wide-block, transposed, "as at" fallback) was built and iteratively corrected against three real bugs caught by an explicit post-parse audit rather than trusting the first clean-looking run: (1) a header-detection rule that was initially too permissive matched a repeated-label gender-breakdown sub-header row in one Renewal SA legacy file, producing swapped classification/period columns — caught via an audit specifically checking for year-shaped values leaking into the classification column, fixed with a header-uniqueness check, and the one file it couldn't safely re-parse was excluded rather than force-fitted; (2) six Attorney-General's Department resource files all shared an identical filename on the CKAN server, silently overwriting each other during download so only the last-fetched (and, as it turned out, least complete) edition survived to disk — caught by comparing manifest entry counts against actual files on disk, fixed by suffixing colliding filenames with their CKAN resource ID, which recovered a 2012-13 to 2018-19 edition that would otherwise have been silently lost entirely; (3) a fallback period-label regex tried a 2-digit alternative before a 4-digit one in an ordered alternation, mangling one Department for Infrastructure and Transport file's internal document-management ID into a nonsense period label — caught by scanning all output periods for implausible values, fixed by reordering the alternation and by preferring a year found in the file's own title text over the filename. A fourth genuine data-quality issue was found and could not be safely auto-fixed: the Independent Commission Against Corruption's own file uses a multi-year, multi-column-pair layout with footnote markers embedded directly in numeric cells (e.g. a cell literally containing `"4 1"`), and produced internally conflicting values for the same agency/classification/year across overlapping blocks — excluded from the merged table rather than risk publishing wrong figures (raw file still mirrored for manual use). Also excluded on licensing grounds: Judicial Conduct Commissioner (CC BY-ND, incompatible with a derived/merged table), Adelaide Festival Centre Trust (no licence stated) and Adelaide Film Festival ("Other (Open)" with no quotable terms) — consistent with how this repository treats every other ambiguous-licence source. Carrick Hill was excluded because its only resource is a PDF (not machine-readable). Final result: **37 of 42 checked agencies included**, 874 merged rows, zero remaining (agency, classification, period) value conflicts after exclusions (verified programmatically, not just spot-checked) — plus spot-checks of several agencies' totals against their raw source files before finalising. Added **`datasets/sa-public-sector-executive-employment/`** (`data/executive-employment-by-agency.csv` merged long-format table plus `data/agency-index.csv` listing all 42 agencies checked including the 5 exclusions and why; `raw/` mirrors all 143 downloaded source files by agency), 6 new CC BY 4.0 / exclusion attribution lines in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. **Genuine, disclosed limitation named explicitly in the README:** despite the candidate-domain brief's own framing ("salary bands"), no SA agency publishes actual dollar salary/remuneration figures in open data anywhere found this run — every source publishes headcount by classification tier only, never a dollar figure; documented plainly rather than silently substituting classification-tier counts for what the domain brief implied. Privacy check: every field is an agency-level aggregate headcount by classification band and period — no individual's name or salary figure anywhere. 3 domains remain unchecked after this run (Gambling and liquor licensing statistics; Boating and marine safety statistics; Concessions and cost-of-living support statistics) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run, consistent with how this repository has treated the same boundary case in prior runs.
- 2026-07-08 (scheduled run): checked **Coastal and marine management (Department for Environment and Water — coastal erosion/protection works, marine park zoning and compliance)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200, `package_search` queries executed directly). Searched `q=coast` and `q=marine` directly against the live API rather than trusting memory: the `coast` query's only DEW-specific structured candidate was **`coast-protection-board-annual-report-data`** (CC BY 4.0) — downloaded its 2018-19 CSV edition directly and inspected it before deciding: it contains only the same mandatory annual-report governance boilerplate already documented elsewhere in this repository (complaint categories, fraud/whistleblower disclosures, executive employment, consultants/contractors), no coastal-works or erosion data of any kind; its later 2019-20 to 2023-24 editions are PDF-only full reports. No genuine coastal-erosion/protection-works dataset exists — a documented gap, not force-fitted. The `marine` query surfaced **Marine Park Zoning** and **Marine Park Network Boundaries** (both DEW, `license_id: cc-by`, confirmed via `package_show` for both packages, IDs `137e6931-05ce-48cd-b1da-e04c1ba3c844` and `4ecfa227-ace7-447a-bd68-fff8292c8894`) — South Australia's 19 marine parks (established under the *Marine Parks Act 2007*) and their 214 internal management zones (Sanctuary, Habitat Protection, General Managed Use, Restricted Access), proclaimed 2012. Both archives' download host (`waterconnect.sa.gov.au`) responded with a `301` from the CKAN-listed `DEWNR` path to a current `DEW` path, followed automatically by `curl -L`; downloaded both GeoJSON zips whole (5,781,495 and 4,277,208 bytes) and confirmed each bundles its own `CC_BY.txt` licence file plus GDA94/GDA2020 datum duplicates of the same features (214 zoning / 31 boundary features each) — matching the CKAN portal's `cc-by`/CC BY 4.0 field with no discrepancy this time (unlike the recurring CC BY 3.0 AU-vs-4.0 mismatch seen for several other DEW/DHUD datasets in this repo). Checked what the source's own `ZONE_TYPE` sub-codes `RAZ_L`/`RAZ_D` meant before deciding how to decode them: neither the CKAN record, the Location SA metadata report, nor a targeted web search turned up a definition, and the zones' own `ZONE_NAME`/`ZONE_TYPCO` fields render all three RAZ variants identically (no `_L`/`_D` visible in the name) — decoded all three to the same "Restricted Access Zone" label in `ZONE_TYPE_LABEL` rather than inventing an unconfirmed distinction. Wrote `convert.py`, reading the GDA2020 feature collection directly out of each mirrored zip (no prior unzip needed): added `ZONE_TYPE_LABEL` (decoded zone-type name) to the 214-feature zoning layer, and `GAZ_DATE_ISO`/`LATEST_GAZ_ISO` (epoch-millisecond gazettal dates converted to ISO 8601) to the 31-feature boundaries layer; verified feature counts and a handful of raw property values against the source geojson directly before finalising. Kept the two layers as separate `data/` files rather than merging — they're different geometric granularity (19 park boundaries vs. 214 nested zones) describing different things. Added **`datasets/sa-marine-park-zoning/`** and a new CC BY 4.0 attribution line in `LICENSE-DATA.md`; root `README.md` updated with one new bullet. Privacy check: every field describes a place (park/zone name, code, gazettal date, GIS geometry measures) — no individual-identifying field of any kind. No compliance/enforcement/permit dataset was found for marine parks either — documented as a further gap alongside the coastal-protection-works one, consistent with the domain's own framing ("coastal erosion/protection works, marine park zoning **and compliance**" — only the zoning/boundary third of that three-part domain description turned out to have a genuine open dataset). 4 domains remain unchecked after this run (Public sector executive remuneration and senior officer disclosures; Gambling and liquor licensing statistics; Boating and marine safety statistics; Concessions and cost-of-living support statistics) — at or above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-08 (scheduled run): checked **Public housing and homelessness services performance (SA Housing Authority / Specialist Homelessness Services — waitlist times, tenancy turnover, homelessness-service client outcomes)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched the `sa-housing-authority` organisation's full catalogue (23 packages) directly rather than trusting a keyword search alone: exactly 4 genuine, distinct candidates exist — **Social Housing – dwellings**, **– households**, **– new households housed** and **– household members** (all `license_id: cc-by`, confirmed via `package_show` for all four), each covering Public Housing and State Owned and Managed Indigenous Housing (SOMIH) by Local Government Area for FY2019-20 and FY2020-21 — these are the exact 4 datasets flagged as an unpursued "future-pass candidate" in the general Housing domain's own 2026-07-06 run-log entry. No edition since FY2020-21 exists (`metadata_modified: 2022-06-06` on all four, checked directly, not assumed) — documented as a stalled/discontinued series. Downloaded all 16 raw XLSX workbooks (4 measures × 2 housing types × 2 years) directly over plain HTTPS (no `fetch.sh` needed). Inspected every workbook directly with `openpyxl` before writing conversion code: each is an LGA-by-characteristic table with a merged 2-row header and a trailing `Total`/`% Total` summary row pair (kept and flagged via a new `is_total_row` column, not dropped, consistent with this repo's population-projections precedent). Column sets are identical across housing-type/year combinations within each measure except for a handful of near-identical group/column-label wording variants (e.g. "Main source of income" vs "Main source of income - households paying less than market rent only" as the merged group header; "Younger person (main tenant under 25 years)" vs "... (household main tenant ...)" as a leaf label) — resolved by keying columns on their own unique leaf name only (the group label is documented in the README rather than baked into the column name) and by keeping the first-seen wording as canonical when a later file's leaf label itself varied slightly. Wrote `convert.py`; verified before finalising by summing each measure's `Total - <measure>` row for PH/SOMIH 2019-20 against the raw workbook cells directly — all matched exactly (dwellings 32,147 PH / 1,388 SOMIH; new households housed 1,941 PH; household members 49,521 PH). Added **`datasets/sa-social-housing/`** (4 tidy CSVs, 252 rows each) and a new CC BY 4.0 attribution line in `LICENSE-DATA.md`. This dataset does **not** cover the domain's "waitlist times" or "tenancy turnover" measures specifically — no field in any of the 4 SA Housing Authority datasets reports either (only whether a greatest-need household waited under/over 6 months); no genuine open SA-published dataset for either specific measure was found, checked directly via the organisation's full catalogue and general web search, documented as a genuine gap rather than force-fitted. Paired this with the domain's Specialist Homelessness Services angle, which is a distinct client population (crisis/outreach homelessness support, not necessarily social-housing tenants) with no genuine SA-government-published dataset (data.sa.gov.au's only homelessness-tagged result is a stale 2015 rough-sleepers XLS): pivoted to AIHW's **Specialist Homelessness Services Collection historical tables 2011-12 to 2024-25** (Cat. no. HOU 343), current 2024-25 edition. The AIHW data page renders its download links client-side via JavaScript, so a static page fetch didn't surface the file directly — resolved the exact `getmedia` URL via web search instead, then downloaded it directly over plain HTTPS. Licence independently confirmed directly from `aihw.gov.au/copyright`: CC BY 4.0, quoted verbatim, consistent with this repository's two existing AIHW datasets. Inspected all 16 sheets (15 HIST.* historical tables plus Contents/Explanatory notes) directly with `openpyxl` before writing conversion code: each shares the same 4-row preamble structure but a different, table-specific set of leading dimension columns (state/territory, data type, sex, Indigenous status, remoteness area, or client characteristic) followed by financial-year columns and an "Average annual change" summary column whose position isn't always after the last year column (one table, `HIST.YOUNG`, appends its most recent year's column after the average-change column rather than before it — the source's own layout quirk, handled by classifying columns by content pattern rather than fixed position). Wrote `convert.py` producing both a native-wide-shape CSV per table (`data/tables/`) and one combined long-format file (`data/all-tables-long.csv`, 22,454 rows) plus a `data/table-index.csv`; verified before finalising that South Australia's `Clients (number)`/`All clients` figures for 2011-12 (19,497) and 2024-25 (17,587), plus the average-annual-change figure (-0.8%), match the raw workbook cells exactly. 14 of the 15 historical tables carry an explicit `South Australia` row (only the Remoteness-Area population-rate table, `HIST.INDIGENOUS_REG`, is Australia-wide only). Added **`datasets/au-specialist-homelessness-services/`** and a new CC BY 4.0 ("Based on... material") attribution line in `LICENSE-DATA.md`. Privacy check on both new datasets: every field is an LGA-level or jurisdiction-level aggregate count/rate/percentage — the SA Housing Authority workbooks already suppress any LGA with 1-3 dwellings/households/members as `"n.p."` and round all other counts to the nearest 5 (source's own small-cell protection, preserved as-is); the AIHW workbook's underlying client-level SHSC extracts are never published, only cross-tabulated aggregates with small-number suppression and (from 2022-23) cell perturbation applied by AIHW itself — no individual's name, address or case-identifying detail anywhere in either dataset. Root `README.md` updated with two new bullets. Only 2 domains remained unchecked after this run (Coastal and marine management; Public sector executive remuneration and senior officer disclosures) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Gambling and liquor licensing statistics (Consumer and Business Services / Office of Problem Gambling), Boating and marine safety statistics (Department for Infrastructure and Transport / Marine Safety SA), and Concessions and cost-of-living support statistics (Department of Human Services).

- 2026-07-08 (scheduled run): checked **Missing persons statistics (SAPOL — reported missing person counts, demographics and outcomes)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Pulled SAPOL's own organisation catalogue (`organization=south-australia-police`) directly rather than trusting a keyword search alone: SAPOL publishes exactly 4 datasets — `expiation-notice-system-data` and `crime-statistics` (both already mirrored as `sa-expiation-notices`/`sa-crime-statistics`), `road-safety` (feeding into `sa-road-crash-data`), and `annual-reporting-data` (inspected directly via `package_show`: 30 governance-disclosure resources — Complaints, Corruption, Executive Employment, Public Interest Disclosure, Young Offenders, etc. — the same mandatory annual-report boilerplate pattern already documented for other SA agencies elsewhere in this repo; no missing-persons resource anywhere in it). A broad keyword `package_search` for "missing person(s)" on the same portal returned 1,207 hits, none genuinely on-topic (the shared national-index noise already documented in prior runs — top result was a 1916-1919 WW1 Red Cross enquiry-bureau history dataset from the State Library, not a current SAPOL statistic). Checked the Attorney-General's Department's "Crime and justice data" page (`agd.sa.gov.au`, surfaced by web search as a plausible cross-agency host) directly via `curl` with a browser user-agent: HTTP 403 with `cf-mitigated: challenge` in the response headers, confirming a live Cloudflare bot-challenge rather than assuming a block from the status code alone — consistent with the same pattern already documented for several other `.sa.gov.au` domains in this repository; no Wayback Machine snapshot exists to check instead. Pivoted to the national fallback: the Australian Federal Police-run **National Missing Persons Coordination Centre** (`missingpersons.gov.au`) and the **Australian Institute of Criminology** both publish missing-persons research only as PDF reports — "Missing persons in Australia, 2008–2015" (Statistical Bulletin, covering 2008-2015) and "Missing Persons: Who's at risk?" — fetched both sites' research/statistics pages directly and confirmed neither offers a downloadable CSV/XLSX/JSON data file, only narrative PDFs, and neither states an explicit reuse licence. Even setting the missing-machine-readable-format and missing-licence issues aside, the most recent of these bulletins is already roughly a decade stale. A `data.gov.au` CKAN search for "missing persons" returned the same kind of federated noise as the SA portal (ABS personal-income tables, unrelated state datasets), confirming no national structured dataset exists there either. Conclusion: no genuine, currently open, machine-readable missing-persons dataset exists at either the SA or national level — nothing added this run, a genuine documented gap rather than a silently skipped domain. Privacy: n/a, no candidate data was found to check. 3 domains remain unchecked after this run (Public housing and homelessness services performance; Coastal and marine management; Public sector executive remuneration and senior officer disclosures) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run, consistent with how this repository has treated the same boundary case in prior runs.
- 2026-07-08 (scheduled run): checked **SA Civil and Administrative Tribunal (SACAT) case statistics**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched `package_search` for "SACAT" and confirmed via the organisation page (`south-australian-civil-and-administrative-tribunal`) that it publishes exactly 3 "Annual Report" packages (2020-21, 2021-22, 2022-23), each bundling the same 10 category files (Administrative and Disciplinary applications/hearings, Alternative Dispute Resolution, Community list applications/automatic-reviews/hearings, Housing list applications/hearings, Internal Review applications/hearings); `license_id: cc-by` confirmed via `package_show` for all three editions. A separate, older set of single-measure packages under the same organisation (e.g. `sacat-annual-report-data-community-hearings-data`) covers only FY2016-17 to 2017-18 and is superseded by the 2020-21 edition — not mirrored separately. No 2023-24 or later edition exists on data.sa.gov.au as of this run (checked via `package_search`, not assumed) — the dataset is stale by roughly 2.5 years, documented plainly rather than treated as current. Downloaded all 30 raw files (10 categories x 3 years) directly over plain HTTPS — no `fetch.sh` needed. Inspected every file directly before writing conversion code: 26 of 30 are already tidy per-row CSVs (financial-year total repeated on every row), but 4 (`community-hearings` 2021-22, `housing-applications` 2021-22, `admin-disc-hearings` 2022-23, `community-applications` 2022-23) are instead pivot-table XLSX exports with a Fin Year -> Act -> Application row hierarchy and a duplicate blank-label "subtotal" row under every real data row — a reporting-tool export artifact, not a second observation. Wrote `convert.py` to reconstruct those 4 into the same tidy shape as their CSV-format sibling years (forward-filling the Act label across its child Application rows, dropping the duplicate blank rows) before merging all 3 years per category into 10 tidy tables. Verified correctness before finalising: summed every category/year's row-level counts and checked each sum against the source's own published financial-year total — all 30 category/year combinations matched exactly, including all 4 reconstructed ones. Added **`datasets/sa-sacat-case-statistics/`** (10 tidy CSVs, 668 combined data rows), a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in every source and converted file is an aggregate count by application/hearing/review type, Act, stream or financial year — no individual party's name, address or case-identifying detail anywhere. 4 domains remain unchecked after this run (Missing persons statistics; Public housing and homelessness services performance; Coastal and marine management; Public sector executive remuneration and senior officer disclosures) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Dog and cat management and animal welfare statistics (Dog and Cat Management Board SA — registration counts, microchipping, seizure/rehoming/euthanasia statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au`'s CKAN API directly reachable this run (HTTP 200). Searched `package_search` for "dog cat management" (181 hits, mostly noise from the common words "dog"/"cat"/"management") and refined to a title search, which surfaced two genuinely distinct candidates from the Department for Environment and Water (the Board's parent department): (1) **Dog and Cat Management Board Annual Report Data**, `license_id: cc-by`, but stale — last modified 2020-01-06, data stops at FY2018-19, superseded by (2); (2) **Dogs and Cats Online Data**, four annual editions (2021-22 through 2024-25), `license_id: cc-by` confirmed via `package_show` on every edition, genuinely current — the 2024-25 edition (temporal coverage to 2025-05-30) was published 4 July 2025, and no 2025-26 edition exists yet (expected later in 2026 once that financial year's data is extracted; checked directly, not assumed). Sourced from DACO (DogsAndCatsOnline.com), the statutory registration system under the *Dog and Cat Management Act 1995*. Downloaded all 20 raw workbooks directly over plain HTTPS (5 files x 4 editions; no `fetch.sh` needed — the 2021-22 and 2022-23 editions ship as zip archives, unzipped locally rather than needing a fetch script since the domains themselves were reachable): the 2021-22 edition's CKAN resource URLs point at `waterconnect.sa.gov.au` zips rather than a data.sa.gov.au-hosted file (an inconsistency in how the Department linked that one edition, not a licensing issue — same CC BY 4.0 applies, confirmed on the CKAN package record regardless of where the file itself is hosted). Inspected every workbook directly with `openpyxl` before writing any conversion code, which surfaced two genuine source-format changes handled explicitly rather than silently: (a) the Incidents workbook republishes the *full cumulative history* each edition (2998 rows in 2021-22 growing to 5563 rows in 2024-25, with even historical years' counts revised upward in later editions as late-reported incidents are added) — so only the current 2024-25 edition is used for Incidents, avoiding duplicate/conflicting rows that merging all 4 editions would have created; (b) the most-popular-breeds workbook's column layout changed from a 3-column `(Primary breed, Secondary breed, Count)` pair-based breakdown (2021-22 to 2023-24) to a 2-column `(Breed, Sum of Count)` layout in 2024-25 where combination breeds are instead encoded as a single `"Breed A - Breed B"` string — caught this via a first-pass conversion bug (the script initially mis-mapped the 2-column sheet's count field) and fixed it by branching on which header shape each edition's sheet actually has, rather than assuming one schema across all 4 years. Animals/Owners/most-popular-breeds/most-popular-names/top-5-breeds-by-council (all point-in-time snapshots as of each edition's ~30 May extraction date, no year field of their own) were merged across all 4 editions into one tidy table each with an `edition`/`extract_date` column; Incidents was left as the single 2024-25 file. Wrote `convert.py`; 6 output CSVs, 94,073 total rows. Sanity-checked before finalising: per-edition animal/owner count totals increase monotonically edition-to-edition (405,027 to 439,311 animals; 295,551 to 314,188 owners) as expected for a growing registration system, and distinct-council counts per edition sit at 69-70 (SA's 68 councils plus Outback Communities Authority, with minor year-to-year presence variation) rather than some obviously-wrong number. Added **`datasets/sa-dogs-and-cats-online-data/`**, a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in every source workbook and every converted file is a council/species/gender/age-band/breed/incident-characteristic aggregate count — no individual owner's name, address, or animal microchip number appears anywhere in source or converted files. **Genuine, disclosed gap:** despite this domain's brief specifically naming "seizure/rehoming/euthanasia statistics," no field in either Board dataset covers animal seizure, impoundment outcome, rehoming or euthanasia counts — DACO's `RegistrationStatus`/animal-status fields cover registration and "At Home" status only; a genuine open dataset for that specific sub-topic does not appear to exist (not force-fitted, not fabricated). Only 2 domains remained unchecked after this run (SACAT case statistics; Missing persons statistics) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Public housing and homelessness services performance, Coastal and marine management, and Public sector executive remuneration and senior officer disclosures.
- 2026-07-08 (scheduled run): checked **Container deposit and recycling scheme statistics (Green Industries SA — container return rates and volumes under SA's container deposit scheme)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. This run used a Workflow (3 parallel search agents across data.sa.gov.au, Green Industries SA/EPA's own websites, and a national data.gov.au fallback, followed by independent per-candidate verification agents) rather than a single sequential search, given the compliance-sensitive nature of licence verification. `data.sa.gov.au` was directly reachable this run (confirmed via live CKAN API calls); `epa.sa.gov.au` and `greenindustries.sa.gov.au` both returned HTTP 403 to the WebFetch tool specifically but were reachable via `curl` with a browser user-agent, consistent with the Cloudflare bot-challenge pattern already documented elsewhere in this repo for other `.sa.gov.au` domains. Found the genuine match on the first pass: EPA South Australia's **Container Deposit Legislation - Return Rates and Compliance** (CKAN package `4ba6676f-8d3d-4495-9260-0ba384f35338`), three CSVs — total containers returned per financial year, material-specific return-rate percentages (Glass/Aluminium/PET/HDPE/LPB/Total), and compliance/inspection counts. Licence independently re-verified directly against the live `package_show` API and dataset page: `license_id: cc-by`, CC BY 4.0 (note: data.gov.au's federated harvest mirror of the same package shows a stale "CC BY 3.0 AU" — the authoritative `data.sa.gov.au` source is 4.0, and that is what's recorded in the dataset README). Also surfaced Green Industries SA's current annual *Circular Economy Resource Recovery Report* (2023-24 edition), which has a more recent Container Deposit Legislation section (Table 46, current return rates by material) — checked directly and excluded: its landing page and disclaimer carry only a bare "© Copyright Green Industries SA" notice, no Creative Commons or other open-reuse grant, so it fails this repo's licensing bar the same way ACARA's My School data does. Downloaded all 3 CSVs directly over plain HTTPS (no `fetch.sh` needed, 334-648 bytes each, byte-for-byte match to each resource's live `Content-Length`) and wrote `convert.py` to reshape the three differently-laid-out source tables (a year/value pair table, a material x year matrix, a year/two-metric table) into one tidy long-format table (125 rows: `financial_year`, `metric`, `material`, `value`, `unit`); one label-only correction applied (source spells one material "Alumnium", standardised to "Aluminium" — no figure altered). Added **`datasets/sa-container-deposit-scheme/`** and a new bullet in the root `README.md`. Privacy check: every row is a statewide aggregate (container count, percentage, or inspection/non-compliance count by financial year and, for return rates, material type) — no individual, retailer, depot or business-identifying field of any kind. **Genuine, disclosed limitation:** despite a `daily` `update_freq` tag in the source metadata, the dataset's `metadata_modified` is 2021-08-20 and no resource has been updated since (data stops at FY2018-19/2019-20) — documented in the dataset README as a discontinued/historical series, with a pointer to the (non-open) GISA PDF report for anyone who needs current-year headline figures. 3 domains remain unchecked after this run (Dog and cat management and animal welfare statistics; SACAT case statistics; Missing persons statistics) — not below the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Working with Children Checks and screening statistics (Department of Human Services Screening Unit — application/clearance volumes)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed both `data.sa.gov.au` and `data.gov.au` CKAN APIs directly reachable this run (HTTP 200 on both). Searched `data.sa.gov.au` for `children check` and `screening`, and separately pulled every one of the Department of Human Services organisation's 40 published packages via `package_search?fq=organization:dept-of-human-services`: exactly one genuine screening-statistics dataset exists, **Screening Clearance Processing Timeframes** (contact `DLDCSIScreeningUnit@dcsi.sa.gov.au` — the Screening Unit itself), `license_id: cc-by` confirmed via `package_show`. Everything else in the org's catalogue is facility/location registers or annual-report governance-disclosure tables (fraud, consultants, whistleblower disclosures), none of it screening-clearance-specific. Downloaded all 3 published DOCX resources directly over plain HTTPS (no `fetch.sh` needed, 14-20KB each) and parsed them with `python-docx`: each is a cumulative superset of the last (2013-14 to 2015-16, then to 2016-17, then to 2017-18) of one table, "DCSI SCREENING UNIT - COMPLETED APPLICATIONS", reporting completed clearance-application counts by processing-timeframe band (30 business days or less vs 31+) per financial year. Web search confirmed no fresher source exists: the Screening Unit's live site (`screening.sa.gov.au`, `dhs.sa.gov.au/how-we-help/screening-and-background-checks`, `dcsiscreening.sa.gov.au`) publishes procedure/policy content (5-year validity, the July 2024 "work on re-application" change, prohibition-notice/SACAT appeals) but no clearance-volume statistics; a second search confirmed no national WWCC statistics collection exists either — each state/territory runs its own screening unit and scheme under different legislation, a gap explicitly named in the Standing Council of Attorneys-General's November 2025 national WWCC reform agreement. Wrote `convert.py`: parses the 2017-18 (superset) DOCX table with `python-docx` into one tidy row per financial year (`financial_year`, `completed_within_30_business_days_count`/`_pct`, `completed_31plus_business_days_count`/`_pct`, `total_completed_applications`); no count or percentage recalculated, only reshaped from the source's one-column-per-year layout. Spot-checked the converted totals directly against the raw table before finalising (2013-14 total = 99,919; 2017-18 total = 155,304 — both match). **Genuine, disclosed limitations, not worked around:** (1) the dataset covers all Screening Unit clearance types combined, not Working with Children Checks specifically — no field anywhere in the source distinguishes WWCC from any other clearance category; (2) no edition has been published since 2018 (temporal coverage stops at FY2017-18) despite an "annually" update-frequency tag, and the live Screening Unit site fills none of that gap. Added **`datasets/sa-screening-clearance-processing-timeframes/`**, a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field is a whole-of-Screening-Unit aggregate count/percentage by financial year and processing-timeframe band — no individual applicant's name, clearance type, or case-level detail anywhere in the source or the converted file. 4 domains remain unchecked after this run (Container deposit and recycling scheme statistics; Dog and cat management and animal welfare statistics; SACAT case statistics; Missing persons statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Local government finances (SA Local Government Grants Commission / council-level revenue, expenditure, rates and grants data)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Searched data.sa.gov.au's CKAN API first (`package_search` for "grants commission", "local government finance", "council rates", plus organisation-scoped searches of `department-of-planning-transport-and-infrastructure` — the DIT/DPTI org, 61 datasets, all transport/roads/vehicles — and `local-government-association-of-sa` — 1 dataset, a dead 2014-era API pointing at the long-discontinued Parse.com backend, covering elected members/events/libraries/parks, not finances): nothing relevant found. Located the genuine council-level source via web search: the SA Local Government Grants Commission's Minister's Sheet (`dit.sa.gov.au/local-government/grants-commission/grant-allocations`, council-by-council FA Grant allocations for all 68 councils + Outback Communities Authority + 5 Aboriginal Communities) and the public "Councils in Focus" tool (`councilsinfocus.sa.gov.au`, repackaging the same Commission data into revenue/expenditure/rates/population indicators per council) — both genuinely exist and would be the ideal fit for this domain. Actually tested both directly with `curl` (not just WebFetch, since a prior run's WebFetch 403 turned out to be a false block): both returned HTTP 403 with `cf-mitigated: challenge` in the response headers — a live Cloudflare bot-challenge, confirmed via header inspection rather than assumed from the status code alone, matching the same pattern already documented for `treasury.sa.gov.au`/`statebudget.sa.gov.au`/`corrections.sa.gov.au`/`safework.sa.gov.au` elsewhere in this repository. A direct static-asset PDF link (a Database Reports PDF) was also tried in case static paths bypass the challenge — same 403. Also tried data.gov.au's CKAN API (found it now lives at `data.gov.au/data/api/3/action/...`, not the bare `/api/3/action/...` path that returns a 404 Drupal page — the portal has migrated to a Drupal front-end since it was last used in this repository) searching for a national Financial Assistance Grants dataset; found only single-council one-off grant-recipient datasets (Townsville, Brisbane) and other states' local-government finance portals (WA's "MyCouncil Data", Victoria's "Know Your Council", Tasmania's local-government CDC) — no SA equivalent and no national FA Grants distribution dataset. Fell back to the same national source already used for the parallel "state government finances" domain: **ABS's Government Finance Statistics, Annual, 2024-25**, which — distinct from the state general-government tables already in `sa-state-government-finances` — separately publishes a **local government sector** data cube per state (Table 334 for South Australia, aggregating all 68 councils; Table 339 for the all-states-local total). Downloaded both workbooks directly from `abs.gov.au` (no block, no `fetch.sh` needed), confirmed valid `Microsoft Excel 2007+` files, and confirmed the same 4-statement structure (Operating Statement/Cash Flow Statement/Balance Sheet/Expenses by Purpose) as the existing state-finances dataset. Re-confirmed CC BY 4.0 licence directly from `abs.gov.au/website-privacy-copyright-and-disclaimer` (unchanged text from the last time this was quoted). Adapted the existing `sa-state-government-finances/data/convert.py` parsing logic (same fixed 6-row header, same "less"/"equals"/"plus" marker-row handling, no value recalculated) into a new `convert.py` for the local-government tables; 5 spot-check assertions against raw workbook cells (SA 2024-25 Total GFS revenue = 3599, GFS Net operating balance = 568, Total Expenses by purpose = 3031, Recreation/culture/religion expenses = 770, GFS NET WORTH = 36551) all matched. Added **`datasets/sa-local-government-finances/`** (5 tidy CSVs: SA operating statement/cash flow/balance sheet/expenses-by-purpose, 710 combined rows, plus an all-states-local operating-statement comparison file), a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field is a whole-of-local-government-sector aggregate (all 68 SA councils combined) — no individual council, agency, contract or employee-level figure anywhere in either source workbook. **Genuine, disclosed gap:** the actual council-by-council breakdown (what this domain's brief specifically asked for) remains unavailable from this working environment due to the live Cloudflare block on `dit.sa.gov.au`/`councilsinfocus.sa.gov.au` — a future run with different network conditions should re-check both directly. Only 2 domains remained unchecked after this run (Working with Children Checks and screening statistics; Container deposit and recycling scheme statistics) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Dog and cat management/animal welfare statistics (Dog and Cat Management Board SA), SA Civil and Administrative Tribunal (SACAT) case statistics, and Missing persons statistics (SAPOL, directly relevant to Rin's own agency).

- 2026-07-08 (scheduled run): checked **Gender pay gap reporting (Workplace Gender Equality Agency — employer-level gender pay gap data covering SA-based organisations)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed both `data.gov.au` and `www.wgea.gov.au` directly reachable this run (curl HTTP 200 on both; an initial `WebFetch` 403 on the data.gov.au dataset page turned out to be WebFetch's own request signature, not a real block — `curl` succeeded immediately after). First checked data.gov.au's "WGEA Dataset" CKAN listing (`license_id: cc-by`, "Creative Commons Attribution 3.0 Australia"): downloaded and inspected its current 2025 edition (74MB zip, 8 CSVs, 952MB uncompressed) and confirmed it is the underlying annual census *questionnaire* microdata (workforce composition by headcount, policies, flexible work, harm prevention) — it does **not** contain the calculated gender-pay-gap percentages themselves, so it doesn't fit this domain on its own. The actual employer-identified pay-gap figures — a genuine, separate, more recent transparency reform (WGEA started publishing named employer pay gaps in February 2024) — turned out to be published via `wgea.gov.au`'s "Employer Gender Pay Gaps Report" page as a companion XLSX, not the CKAN bulk file; fetched that page directly, found the exact download link, and downloaded the 2024-25 edition (2.16MB, 4 sheets: guide + `2. Employers` + `3. Corporate groups` + `4. Corporate group info`). Confirmed licence directly via `wgea.gov.au/copyright-and-disclaimer` (site-wide CC BY 4.0, applies since this is WGEA's own compiled material, not third-party content). Attempted to also check the interactive Employer Data Explorer at `data.wgea.gov.au` (the tool the domain description and WGEA's own site point to for drilling into individual employers) — every connection attempt reset mid-TLS-handshake from this sandbox; noted as a genuine, tested block rather than assumed. Wrote `convert.py`: read the two data-bearing sheets (`2. Employers`, `3. Corporate groups`) plus the group-membership mapping sheet (`4. Corporate group info`) with `openpyxl`, kept as three separate tidy CSVs since they're three different grains (employer / corporate-group / group-membership) rather than the same slice split across files, renamed headers to `lower_snake_case`, and disambiguated the source's repeated-but-differently-scoped 2024-25 vs 2023-24 column headers. No percentage or dollar figure recalculated, re-derived or reinterpreted — GPG percentages (signed decimals, negative = women earn more) and WGEA's own already-rounded-to-nearest-$1,000 remuneration figures are carried through exactly as published; blank year-on-year cells kept as empty strings. 2 spot-check assertions (`101Warehousing Pty Ltd` 2024-25 average total remuneration GPG = 24.4%; `Adelaide Airport Management Limited` = 12.9%) both matched the raw workbook exactly before finalising. **Significant limitation, disclosed rather than worked around:** the source spreadsheet has no employer address/postcode/state field at all, so it cannot be filtered to "SA-based organisations" as this domain's brief specifically asked — resolving ~10,000 employer/corporate-group ABNs to a registered-business state would require either the Australian Business Register's full "ABN Bulk Extract" (confirmed via data.gov.au: ~2.5GB across ~20 parts, ~19 million national ABN records) or the same number of individual ABN Lookup API calls, both well beyond this run's modest scope; documented plainly in the new dataset's README rather than force-fitted with an unreliable employer-name-matching heuristic. Also noted: WGEA's Act covers non-public-sector employers plus Commonwealth agencies only, never SA (or any) state government, so this dataset is a genuine complement to — not an overlap with — `sa-education-workforce`. Added **`datasets/au-employer-gender-pay-gaps/`** (8,617 employer rows, 1,838 corporate-group rows, 4,385 membership rows), a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every row is an organisation-level calculated statistic (pay gap %, industry, size band, quartile gender composition, average remuneration) — no individual employee's name, salary or any personal identifier appears anywhere in the source or the converted files. 3 domains remain unchecked after this run (Local government finances; Working with Children Checks and screening statistics; Container deposit and recycling scheme statistics) — at, not below, the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Cost of living and consumer price index (ABS Consumer Price Index / Selected Living Cost Indexes — Adelaide/South Australia breakdown)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed both `data.sa.gov.au` and `abs.gov.au` directly reachable this run (HTTP 200). This domain named two specific ABS series to check: checked both directly rather than assuming either fits. **Selected Living Cost Indexes (SLCI)** was fetched and inspected table-by-table (`Table 1. All groups...by household type`, `Table 2. Commodity groups...by household type`, `Table 3. Gross insurance, mortgage interest and consumer credit...by household type`) — every table breaks living-cost movement down by household type (Employee, Age pensioner, Other government transfer recipient, Self-funded retiree, Pensioner and Beneficiary) only; none carries any state or capital-city geography dimension at all, so it doesn't fit this domain's "Adelaide/South Australia breakdown" requirement and was excluded outright (not force-fitted). **Consumer Price Index (CPI)**, by contrast, genuinely publishes a per-capital-city breakdown including Adelaide, down to individual expenditure-class items. Fetched the CPI "May 2026" release page directly via `curl` (not WebFetch, which returned truncated/incomplete content for this JS-heavy page) and parsed its raw HTML for the Data downloads section, finding 18 numbered tables plus 2 quarterly tables and 2 appendices. Selected and downloaded 4 (Table 1 all-groups all-cities headline; Table 9 groups index-numbers by capital city; Table 11 annual % change by capital city, full group/sub-group/expenditure-class hierarchy; Table 12 same but monthly % change) directly over plain HTTPS, no `fetch.sh` needed (53KB-488KB each). Confirmed licence directly from `abs.gov.au/website-privacy-copyright-and-disclaimer`: "All material presented on this website is provided under a Creative Commons Attribution 4.0 International licence" — same CC BY 4.0 wording already used for this repository's other ABS datasets. Wrote `convert.py`: parses each workbook's standard ABS Time-Series-Workbook layout (an `Index` sheet describing every series as `<measure> ; <item> ; <city> ;`, `Data1..DataN` sheets with one series per column), filters to Adelaide-only series, decodes each description string into separate `item`/`measure` fields, and merges the three Adelaide-specific tables (index numbers, annual %, monthly %) into one row per `(date, item)` — per this task's "merge files that split one dataset across several" rule. Separately built a small headline-only comparison file across all 8 capital cities + the national weighted average, from Table 1. Added an `is_cpi_group_level` flag (`True` for exactly the 12 items — All groups CPI + the 11 major CPI groups — that source Table 9 publishes; `False` for finer sub-group/expenditure-class items), derived directly from which source table an item appears in rather than an inferred classification, since the finer group→sub-group→expenditure-class hierarchy isn't stated as a discrete field anywhere in the source and guessing at it risked mislabelling. 2 spot-check assertions (Adelaide May 2026 All groups CPI index number = 102.67, matching the same figure independently re-read straight from the raw workbook's `Data2` sheet column 147; annual % change = 4.4%, matching the ABS's own published headline figure for Adelaide that release) passed before finalising. Output: `adelaide-cpi-by-item.csv` (7,006 rows, 124 distinct items, October 2017 to May 2026) and `all-capital-cities-headline-cpi.csv` (234 rows). Deliberately left the equivalent hierarchy for the other 7 capital cities unconverted this run — Adelaide-only is what the domain asked for, and extending to all 8 cities at full expenditure-class granularity would be a substantially larger job better suited to its own future pass if ever needed. Added **`datasets/au-cost-of-living-cpi/`**, a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in both converted files and all 4 source workbooks is a city/item/date-level aggregate price index or percentage change — no individual, household, business or transaction-level record anywhere. 4 domains remain unchecked after this run (Gender pay gap reporting; Local government finances; Working with Children Checks and screening statistics; Container deposit and recycling scheme statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Local government elections (SA Electoral Commission — council election results, turnout and ward representation)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` and `ecsa.sa.gov.au` both directly reachable this run. `data.sa.gov.au`'s own "Election Statistics" CKAN dataset turned out to be City of Adelaide only (ward voter counts, stale 2007-2014) — not a statewide fit. Went directly to ECSA's site instead: its "2022 Council elections results and statistics" article page (the most recent periodic council election; the next is scheduled for 2027) links 9 statewide XLSX summary workbooks via Joomla's edocman component (plus 60+ per-council results/preference-distribution files not pursued this run, given the modest-pass budget) — all 9 downloaded directly over plain HTTPS with no `fetch.sh` needed. **Licensing correction to a prior run:** `au-federal-election-results` (added 7 July 2026) excluded ECSA outright, stating it "carries no stated open licence anywhere on the site" — that check looked at `ecsa.sa.gov.au/disclaimer` only. This run fetched `ecsa.sa.gov.au/copyright` directly and found a genuine, current, site-wide CC BY 3.0 AU statement ("Government of South Australia requests attribution as 'Government of South Australia 2011'"), the same site-wide-copyright-page pattern already accepted for SA Heritage Places. Noted as a correction in the new dataset's README rather than silently overwriting the earlier README's reasoning (that dataset's own gap was genuinely different: no static file/API behind the JS results portal for federal/state counts). Wrote `convert.py`: located each workbook's real header row past ECSA's title/notes block, forward-filled council names where the source uses them as group headers rather than repeating per row, decoded source-specific text markers (literal `"NULL"` ballot positions, `"Uncontested "`/`"N/A"` cells, the failed-election asterisk suffix on Kingston/Robe) into proper empty values or flag columns, and merged 3 of the 9 workbooks (electors-enrolled 2010-2022, HA-roll-vs-council-roll, elector-representation) into one table after verifying all three share an identical, identically-ordered 68-council + "Total" list. Deliberately did **not** merge the informality and voter-participation workbooks despite matching grain (council + election), because the participation file's 2018-vs-2022 comparison annotates ward-boundary-change elections with "(ward established/abolished at 2022 election)" that the informality file doesn't carry, breaking key alignment for roughly a dozen wards — kept as two separate tables rather than risk a silently-wrong join. 2 spot-check assertions passed before finalising (Adelaide's row in the merged roll/representation file: 30,410 electors, 13,844 HA-roll/16,566 council-roll, 11 representatives both years; Kingston and Robe both correctly flagged `is_failed_election=True`). Added **`datasets/sa-local-government-elections/`** (8 tidy CSVs from the 9 source workbooks), a new CC BY 3.0 AU attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: the candidates-and-elected-members file contains candidate names, which is standard Australian electoral-transparency public disclosure (not private personal information) — same reasoning already applied to the named candidate data in `au-federal-election-results`; the companion characteristics file is ECSA's own de-identified release of the same candidates and was kept un-joined to the named file, exactly as published. Only 2 domains remained unchecked after marking this one done (Cost of living/CPI; Gender pay gap reporting) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: local government finances, Working with Children Checks/screening statistics, and container deposit/recycling scheme statistics.
- 2026-07-08 (scheduled run): checked **National parks and reserves visitation statistics (Department for Environment and Water — visitor numbers and park-level usage)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Queried its CKAN API for `national park visitation`/`visitor` and pulled every one of DEW's (`dept-for-environment-and-water`) 255 published packages via `package_search?fq=organization:...` — the only park/visit/camp/reserve-tagged results are spatial boundary layers (Marine Park Network/Zoning/Special Purpose Areas, NPW Regions & Districts, Conservation Reserve Parcels) and an asset-inventory dataset (`parks-listings`, "South Australian Parks – Features and Facilities" — locations/facilities, not usage). Checked `dew-annual-report-data` (genuinely current, 2024-25 edition, CC BY, modified 27 May 2026) directly by downloading and parsing its XLSX: it is mandatory annual-report governance disclosure (fraud, PID, executive employment, consultants, contractors, WHS, complaints) plus one whole-of-department "National park fees and charges" revenue table (Admissions/guided tours, camping/parking/hire fees, etc., $20.3M total 2024-25) — statewide-aggregate revenue, not park-level visitor headcounts, and only a single year per file with no consistent multi-year field to build a series from. Also checked a park-specific co-management board annual report (`ikara-flinders-ranges-national-park-co-management-board-annual-report-data`, covering one of SA's most-visited parks) on the theory it might carry park-level figures: confirmed via direct CSV download it is the same governance-disclosure boilerplate (complaints, fraud, executive employment, consultants, contractors, WHS) with zero visitation/usage fields, and stale (2016-17/2018-19 only). Web search confirmed the pattern holds government-wide: SA park visitor figures are published only periodically via `environment.sa.gov.au`/`premier.sa.gov.au` media releases for a handful of "iconic" sites (e.g. Naracoorte Caves, Seal Bay, Cleland Wildlife Park), not as a structured downloadable dataset; a further search for a national ABS/Productivity Commission RoGS fallback (the pattern used successfully for several prior domains) found none exists for park visitation — each state runs its own visitor survey with inconsistent methodology and coverage (NSW's own survey only started including SA/WA/NT/Tas residents in FY2024-25), so there is no comparable national collection to fall back to with an SA breakout. Nothing added this run. Privacy: n/a, no candidate data was found to check. 3 domains remain unchecked after this run (Local government elections; Cost of living and consumer price index; Gender pay gap reporting) — at, not below, the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Fisheries and aquaculture statistics (PIRSA — wild-catch and aquaculture licence/production data)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Queried its CKAN API for `fisheries`/`aquaculture` and separately pulled every one of PIRSA's 24 published packages via `organization_show`/`package_search` — none is a production or licence-count statistics dataset: `aquaculture-zones` and `aquaculture-leases-and-licences` are spatial boundary layers (shapefile/GeoJSON/KML of *where* zones and leases sit, last touched 2020), and `rock-lobster-sanctuaries`/`aquatic-reserves` are likewise closure-boundary layers, not catch or licence-holder data. A keyword search of the shared national CKAN index surfaced several commercial-fishery-sounding datasets (Southern Rock Lobster, Snook, Bluethroat Wrasse, Purple Wrasse commercial fishery data) that looked promising, but checking each one's `organization`/`license_id` fields directly (not just its title) showed they are **Tasmanian** IMAS/NRE Tas stock-assessment data harvested via the Australian Ocean Data Network — `isopen: false`, `license_id: notspecified` — excluded on both geography and licensing grounds. Checked ABARES' *Australian fisheries and aquaculture statistics* (AFAS) series as the national fallback: the editions indexed on both `data.sa.gov.au`'s shared catalogue and `data.gov.au`'s own CKAN (confirmed via its now-relocated `/data/api/3/action/...` path, since the bare `/api/3/action/...` path 404s) only go up to the 2016 edition — later editions were never published as CKAN datasets, only as report pages directly on `agriculture.gov.au`. Fetched that landing page directly (not from memory) and confirmed the current "Data to 2024-25" edition (released December 2025) has a genuine downloadable statistical-tables workbook (XLSX, not PDF-only), licensed CC BY 4.0 per its own `Index` sheet. Its Table 8, "Fisheries and aquaculture production, South Australia", is itself sourced (per the workbook's own citation) from "Primary Industries and Regions South Australia; South Australian Research and Development Institute" — i.e. PIRSA/SARDI's own underlying data, compiled nationally. Downloaded the workbook directly over plain HTTPS (no `fetch.sh` needed, 822,830 bytes), mirrored verbatim in `raw/`. Wrote `convert.py`: unpivots Table 1 (gross value by state/Commonwealth-fishery) and Tables 5-11 (each state/territory's own Value+Quantity, Wild-caught+Aquaculture, category/commodity breakdown) from their repeating year-column layout into three tidy long-format CSVs — one merged all-states production file, a South-Australia-filtered companion, and the gross-value-by-jurisdiction file — following this task's "merge files that split one dataset across several" and "decode source codes" rules (trailing footnote-letter markers like "Aquaculture b" stripped from labels). Tables 2-4 (national commodity-group aggregates, no jurisdiction split), 12 (Commonwealth fisheries, a structurally distinct by-fishery nested layout) and 13-19 (trade/consumption/employment, Australia-wide only) were deliberately left unconverted since they carry no state-level cut relevant to this domain — disclosed in `data/table-index.csv` and the dataset's own README, not silently dropped. 2 spot-check assertions against the raw workbook passed (South Australia 1998-99 Prawns value = 39615 $'000; South Australia 2023-24 Total production value = 426848 $'000) before finalising. Added **`datasets/au-fisheries-aquaculture-statistics/`**, a new CC BY 4.0 attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every row is a commodity/category/jurisdiction-level production or gross-value aggregate — no vessel, licence-holder or individual-level field of any kind in any of the converted tables or the raw workbook. 4 domains remain unchecked after this run (National parks and reserves visitation statistics; Local government elections; Cost of living and consumer price index; Gender pay gap reporting) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Freedom of Information (FOI) request statistics (SA Ombudsman / individual agencies — requests received, granted, refused and review outcomes)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` reachable this run, though its CKAN API has moved to `/data/api/3/action/...` (the previously-used `/api/3/action/...` path now 404s — noted here in case a future run hits the same surprise). Queried `tags:foi` restricted to the SA jurisdiction facet and found a genuine, rich match: **State Records of South Australia** (the agency administering the FOI Act's own s.42 annual-reporting obligation) publishes 19 separate `sa-foi-*` CKAN datasets — one CSV per statistical breakdown (applications received by year/sector since 1991, by individual agency across 3 sector files, by applicant type, by MPs; determination outcomes; refusal reasons by clause and by other reason; fee waivers; formal and negotiated extensions; processing time; unfinished/overdue applications; internal review and amendment-application outcomes; FTE staff) — all `license_id: "cc-by"` confirmed via `package_show`, 17 of the 19 refreshed 25 November 2025 (through 2024-25), 2 stale since November 2022 (through 2021-22 only). The domain description explicitly also asked for "review outcomes", which State Records' agency-side data doesn't cover — found the complementary piece in **Ombudsman SA's Annual Report 2024-25 data tables** (also CC BY, confirmed via CKAN), which publishes the independent-review layer (external reviews received/completed by sector and by individual department/council/authority/minister, review outcomes, processing-time bands, average days open) mixed in with unrelated Ombudsman Act/Return to Work Act/general-complaint tables that were left out as out of scope for this domain. Downloaded all 19 State Records CSVs directly (plain HTTPS, no `fetch.sh` needed) plus the one Ombudsman SA CSV, mirrored verbatim in `raw/`. Wrote `convert.py`: melts each State Records file from wide (one column per year, sometimes with a constant descriptive phrase or a second dimension folded into the column header, as in the determination-outcome file where each column header is itself `<year> - <outcome>`) into tidy long rows, extracting the year via regex and splitting an embedded `STATE - `/`LOCAL - `/`UNIVERSITIES - ` sector prefix off the row label where present; separately parses just the FOI Act jurisdiction tables out of the much larger Ombudsman CSV by locating each table's own title row. Merged the 3 separate per-sector agency files (state/local/university) into one `applications-by-agency.csv`, and Ombudsman's 4 separate per-entity-type tables into one `osa-external-reviews-by-entity-2024-25.csv` — both per this task's "merge files that split one dataset across several" rule. Discovered mid-conversion that source files are windows-1252 encoded (a raw `0x92` byte — a curly apostrophe — in "agency's resources" isn't valid UTF-8); `convert.py` decodes correctly and writes UTF-8. Two genuine source typos were found and corrected (both label/year fixes, not figure changes, both documented in the script and README): the Universities agency file's column header read "2023-23" instead of "2023-24"; the Ombudsman processing-time-bands table's row label read "2023-25" instead of "2024-25" (confirmed via a cross-check: the corrected row's Total, 175, exactly matches Table 8's own 2024-25 "external reviews completed" total). 5 spot-check assertions (State Government FOI applications 2024-25 = 14,795; MPs applications 2001-02 = 48; State Government Personal full-release determinations 2024-25 = 3,997; Government Departments external reviews received 2024-25 = 104; SA Police external reviews received 2024-25 = 38) all passed against the raw files before finalising. One data anomaly was found and left exactly as published rather than corrected or hidden: STATE financial-disadvantage fee waivers jump from ~3,000/year to 65,426 in 2024-25 in the source's own file — disclosed in the README as a known limitation, not treated as an error to fix. Added **`datasets/sa-foi-statistics/`** (23 tidy tables total: 17 from State Records plus a combined superset `all-tables-long.csv`, and 6 from Ombudsman SA), two new licence-attribution lines in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field across all 23 tables is a whole-of-sector/agency/council/portfolio aggregate (count, percentage, FTE or average-days figure) by year/category/reason/outcome — no FOI applicant's name, no individual case reference, and ministers are identified only by portfolio title (already-public office information), not personal name, in any of the 20 source files. Only 2 domains remained unchecked after this run (Fisheries and aquaculture statistics; National parks and reserves visitation statistics) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added to the list: Local government elections (SA Electoral Commission), Cost of living and consumer price index (ABS), and Gender pay gap reporting (Workplace Gender Equality Agency).
- 2026-07-08 (scheduled run): checked **Weather, climate and rainfall statistics (Bureau of Meteorology — SA rainfall, temperature and climate station data)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run and queried its CKAN API for `rainfall`/`climate`/`weather station`, both unrestricted and restricted to the SA jurisdiction facet: the only SA-published climate-adjacent datasets found were **future climate projections** (`climate-projections-for-south-australia`, `south-australian-climate-projections-narclim-1-5`, the Goyder Institute's downscaled projections — all CC BY, all Department for Environment and Water) and the EPA air-quality network's own on-site "meteorology" resources, already covered by `sa-epa-air-quality-monitoring` and explicitly out of scope per this domain's own note. No genuine SA-published *historical observed* rainfall/temperature station dataset exists on data.sa.gov.au. Pivoted to the Bureau of Meteorology directly: a `curl` request to `bom.gov.au` (including its own Data Catalogue licence page) returned HTTP 403 with an explicit anti-scraping message pointing to a paid "Registered User service ... charges apply to most data products" — a genuine, confirmed access block, not assumed; the same page states BOM data is offered "under a range of conditions" rather than one uniform licence, so even absent the block, a clean CC BY grant wasn't established for bulk station data. Found and verified an equivalent, genuinely open route instead: **SILO**, the Queensland Government's climate database (Queensland Treasury, originally a joint project with BOM since 1996), whose "Patched Point" product is built directly from named BOM station observations. Confirmed its licence directly from its own About page: *"SILO products are provided free of charge to the public for use under the Creative Commons Attribution 4.0 license."* Tested and used its `PatchedPointDataset.php` web API live (no scraping — a documented, intended endpoint; needs only a syntactically valid email, no real authentication), plus its station name-search endpoint to find real BOM station numbers rather than guessing them. Selected 8 stations spanning South Australia's climate diversity (Adelaide Airport, Mount Gambier Aero, Renmark Aero, Port Augusta Aero, Ceduna AMO, Coober Pedy Airport, Nuriootpa PIRSA, Robe) and fetched daily rainfall + max/min temperature for 2016-2025 (10 years, 3,653 days each). Two originally-considered stations were checked and rejected on data-quality grounds after inspecting their actual source-flag codes rather than assuming currency: Adelaide (Kent Town, station 23090) stopped returning official BOM observations after mid-2020 (0% observed 2021-2025, all "interpolated"); plain Nuriootpa (station 23312) sources almost all its readings from a *different, nearby* station rather than its own. Adelaide Airport (23034) and Nuriootpa PIRSA (23373) were substituted instead, both confirmed 95-99.9% genuinely observed across the full 2016-2025 window per station per variable — a disclosed substitution, not a silent one. Wrote `convert.py` merging the 8 raw per-station files into one tidy table, adding station name/coordinates/elevation inline (from the same SILO station register used to select the stations) and decoding SILO's numeric data-source flag (0/13/15/23/25/26/35/42/75) into a readable label alongside the raw code per the source's own "Interpolation issues and data codes" documentation (code 23 required a second SILO page — the "File formats and samples" sample pages — since it isn't listed on the main data-codes page). The script's spot-check assertions (row count = 8 × 3,653 = 29,224; Adelaide Airport's first row exactly matches the raw file's 1 January 2016 values) passed before finalising. Added **`datasets/sa-climate-station-data/`**, a new SILO/BOM CC BY 4.0 attribution line in `LICENSE-DATA.md` (documenting the BOM-direct-access block and why SILO was used instead), and a new bullet in the root `README.md`. Privacy check: every field is a station location (name/coordinates/elevation), a daily rainfall/temperature measurement, or a data-provenance flag — no individual, personal or business-identifying field anywhere in any of the 8 fetched files. 3 domains remain unchecked after this run (Freedom of Information (FOI) request statistics; Fisheries and aquaculture statistics; National parks and reserves visitation statistics) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run, consistent with how this repository has treated the same boundary case in prior runs.
- 2026-07-08 (scheduled run): checked **Population and demographic projections (ABS / SA Government population estimates and projections by LGA)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Its CKAN API surfaced a genuine SA-specific match: **Population Projections for SA** (`population-projections-for-sa`, currently attributed to the Department for Housing and Urban Development though the workbooks themselves carry a 2019 "Department of Planning, Transport and Infrastructure" copyright line — a machinery-of-government rename), `license_id: "cc-by"` confirmed via the live API. Its CKAN record's `metadata_modified` is 2024-07-16, but every one of its 9 resources still dates to 2019/2020 and is based on the **2016 Census**. Web search found a newer edition — *"Local Area (SA2 and LGA) Population Projections for South Australia, 2021 to 2041"* — hosted at `plan.sa.gov.au`, which returned HTTP 403 (Cloudflare bot-challenge) to direct fetching this run, the same block pattern already documented for `treasury.sa.gov.au`, `statebudget.sa.gov.au`, `corrections.sa.gov.au` and `safework.sa.gov.au`; even setting the block aside, that newer edition is published only as a PDF report, not a machine-readable file, and nothing newer has been uploaded to the CKAN record itself. Consistent with this repository's existing "stale-but-genuine CC BY dataset, current edition PDF-only/blocked" precedent (`sa-primary-industries-scorecard`, `sa-tourism-visitor-statistics`), mirrored the older CKAN-hosted 2016-Census edition rather than leaving the domain unfulfilled: downloaded the LGA-level (age/sex to 2036), SA2-level (to 2036) and State/11-region Medium-series (to 2041) workbooks directly from `data.sa.gov.au` over plain HTTPS (no `fetch.sh` needed), plus DPTI's own explanatory-notes PDF. Wrote `convert.py` reshaping each workbook's wide year-column layout into long tidy tables, explicitly flagging each source's own embedded state/region aggregate rows (blank area code) as `is_total_row` rather than dropping them; 4 spot-check assertions against raw workbook cells (Adelaide (C) Persons 2016 = 23,552; Adelaide (C) Females 2016 age 0-4 = 584; TOTAL SOUTH AUSTRALIA Females 2016 = 865,966 cross-checked in both the LGA and SA2 files; SOUTH AUSTRALIA Medium-series 2016 = 1,712,844) all passed before finalising. Deliberately left the SA2 age/sex breakdown and the High/Low projection series unconverted this run (disclosed scope decisions — see the dataset's own README). Paired this with a second dataset for the "actual, currently measured" counterpart the domain description also asked for: ABS's **Regional population, 2024-25 financial year** release (three data cubes: the 2001-2025 national LGA-level ERP time series, the 2021-25 national LGA-level population-components file, and the 2024-25 South-Australia-and-states-and-territories snapshot with area/density), CC BY 4.0 confirmed from ABS's own copyright page, downloaded directly from `abs.gov.au` (reachable over plain HTTPS, no block). Derived a `state_name` column for the 2001-2025 time series from the ABS ASGS LGA-code leading-digit convention (`1`=NSW...`4`=SA...`9`=Other Territories), verified against every distinct code in the file (not assumed) before use. 5 spot-check assertions passed, including an independent cross-check that South Australia's 71-LGA sum (1,902,665 in 2025) agreed exactly across all three source workbooks. Deliberately left six other states'/territories' 2024-25 LGA-level component/area/density tables unconverted (Tables 1-3, 5-7 of the third workbook) since the national 2001-2025 time series and the states/territories roll-up already cover the same ground without duplication — disclosed scope decision, not a gap. Added **`datasets/sa-population-projections/`** and **`datasets/au-regional-population-estimates/`**, two new licence-attribution lines in `LICENSE-DATA.md`, and two new bullets in the root `README.md`. Privacy check: every field in both new datasets is a geography (LGA/SA2/region/state) x sex x age-group x year population count, or a births/deaths/migration/area/density aggregate — no name, address, or individual-level record in any of the four source workbooks, confirmed by directly inspecting the full downloaded files. 4 domains remain unchecked after this run (Weather, climate and rainfall statistics; Freedom of Information (FOI) request statistics; Fisheries and aquaculture statistics; National parks and reserves visitation statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-08 (scheduled run): checked **Public sector procurement and contracts transparency (SA Government Contracts and Tenders register — supplier, value, agency and category for state government contracts)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run (HTTP 200). Queried its CKAN API with free-text terms (`contract`, `tenders`, `procurement`) both unrestricted and restricted to `jurisdiction:"Government of South Australia"` — the unrestricted queries surfaced thousands of other states'/territories' and Commonwealth contract-disclosure datasets (ACT `data-act`, Queensland health-service contract disclosures, AusTender exports), confirming this portal's search federates well beyond SA's own listings, so results were filtered to the SA jurisdiction facet rather than read at face value. Restricted to SA, exactly one dataset actually matches "contract": **State Procurement Board Annual Report Data** (`state-procurement-board-annual-report-data-public-complaints`, Dept of Treasury and Finance, CC BY) — downloaded and inspected its most recent workbook (`2020-21-spb-reporting-data.xlsx`) directly rather than trusting the title alone, and its `Contractors`/`Consultants` sheets turned out to be the Board's own governance-disclosure data (external contractors/consultants engaged *by the Board itself*, plus supplier-complaint counts and fraud-detection stats) — not a whole-of-government contracts/tenders register — and stale at FY2020-21 (5 years old), last resource added 2021-10-25. No other SA-jurisdiction dataset matched `procurement` or `tenders` either. The actual SA Government Tenders and Contracts platform (`tenders.sa.gov.au`, `contracts.sa.gov.au`) and `treasury.sa.gov.au`'s open-data page all returned HTTP 403 (Cloudflare bot-challenge) to direct fetching this run — confirmed via `curl` and `WebFetch`, the same block pattern already documented for `treasury.sa.gov.au`/`statebudget.sa.gov.au` in the previous run and for `corrections.sa.gov.au`/`safework.sa.gov.au` in earlier ones — and in any case that platform is a live tender-notice/registration system for suppliers, not a bulk open-data export. Independent corroboration found via web search: Australia's Regional Open Data Census (`australia.census.okfn.org/entry/sa/contracts.html`) has its own SA/Procurement-Contracts entry noting "Can't find data on data.sa.gov.au by searching for 'contract' or 'procurement'" — the same negative result reached independently. No genuine national substitute exists for this domain either (unlike other "no SA-specific dataset" cases resolved via an ABS/AIHW/Productivity-Commission fallback elsewhere in this repo): AusTender publishes Commonwealth government contracts only, not state government contracts, so it isn't a genuine proxy for SA state procurement data. Conclusion: this remains a genuine, documented gap — no dataset added. Only 2 domains would have remained unchecked after this run (below the "fewer than 3" threshold), so 3 new candidate domains were proposed and added to the list: Freedom of Information (FOI) request statistics, Fisheries and aquaculture statistics (PIRSA), and National parks and reserves visitation statistics.
- 2026-07-08 (scheduled run): checked **State government finances (SA Budget Papers / Department of Treasury and Finance — portfolio-level expenditure and revenue estimates, actual-vs-budget outturns)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run (HTTP 200). Queried its CKAN API restricted to `organization:department-of-treasury-and-finance` (a precise, definitive 8-dataset result, rather than a noisy free-text "budget" search which returned thousands of false-positive matches across every state's portal): `budget-2015-16-data` ("Budget 2015-16 Tables and Data") is genuinely 117 XLSX budget tables, CC BY 4.0, but a decade stale (2015-16 only, not updated since 2018); `south-australian-state-budget-2017-18` is 8 **PDF** budget papers only, no machine-readable tables, also stale; `general-government-sector-expenses-by-function-and-by-economic-type` ("South Australian State Government Finance Statistics") has only 2 resources, one stale 2017 XLSX and one link to a discontinued 2014-15 ABS catalogue number — its data.gov.au harvested mirror shows a newer package-level `metadata_modified` (2025-06-25) than the SA-portal original (2017-03-17), investigated directly via `package_show` on both portals rather than assumed to be fresh content, and confirmed to be a harvest-sync timestamp only (the actual resource `last_modified` fields are unchanged); the remaining 5 Treasury datasets are narrow single-scheme items (payroll tax rebate, stamp duty concession, first home owner grant — the latter already checked and excluded in last run's grants-domain entry) or governance-disclosure annual-report data. Searched further data.sa.gov.au terms ("portfolio statement", "budget papers", "state finances", "financial statement", "mid-year budget review") — nothing else SA-specific and current surfaced. Confirmed via web search that a real, current **2026-27 State Budget** was handed down 4 June 2026 (Treasurer Tom Koutsantonis), with Budget Papers 1, 3, 4 (Volumes 1-4) and 5 — but every one is hosted as a PDF on `treasury.sa.gov.au`/`statebudget.sa.gov.au`, both of which returned HTTP 403 (Cloudflare bot-challenge) to direct fetching this run, the same block pattern already documented for `corrections.sa.gov.au`, `safework.sa.gov.au` and SA Ambulance's waiting-times page in earlier runs — confirmed directly via `curl`, not assumed from memory. Pivoted to the national fallback already used successfully for several other "no current SA-specific dataset" domains: ABS's **Government Finance Statistics, Annual, 2024-25** (released 21 April 2026), which publishes a dedicated South Australia general-government data cube (Table 234) built from data SA's own government reports to the ABS, plus a "Total State" all-jurisdictions-combined cube (Table 239) for comparison. Licence confirmed directly from ABS's own copyright page (`abs.gov.au/website-privacy-copyright-and-disclaimer`): CC BY 4.0, identical wording already used for this repository's other ABS datasets. Downloaded both XLSX workbooks directly from `abs.gov.au` (reachable over plain HTTPS, no `fetch.sh` needed) — `55120DO006_202425.xlsx` (Table 234, South Australia, 4 sheets: Operating Statement, Cash Flow Statement, Balance Sheet, Expenses by Purpose) and `55120DO011_202425.xlsx` (Table 239, Total State, same 4-sheet layout) — and mirrored them verbatim in `raw/` under descriptive filenames. Wrote `convert.py` to parse each sheet's fixed 6-row header block, then classify every subsequent row as either a header-only section label (dropped — not itself a data row) or a data row (label + one value per financial year, 2015-16 to 2024-25), also dropping the source's "less"/"equals"/"plus" marker rows (used only to show additive structure between sections on the printed page, carrying no figures of their own). Rather than trying to model the source's section hierarchy semantically — which turned out to be inconsistent (some headline aggregate rows, like `GFS Net operating balance` or `GFS NET WORTH`, sit directly after "equals" markers with no interceding header row, while others don't have even that marker) — the script instead preserves the exact source row order via a `row_order` column and flags aggregate/subtotal lines via a keyword-based `is_total_row` column (label starts with "Total", or matches one of the six known headline-result keywords: net lending, net worth, net financial worth, net operating balance, cash surplus, net change in the stock of cash), verified against every non-"Total"-prefixed aggregate row across all 4 sheets before finalising — a deliberately simpler, more defensible design than inventing a semantic grouping that could subtly mislabel a row. Output: 4 per-statement SA CSVs (`sa-operating-statement.csv`, 300 rows; `sa-cash-flow-statement.csv`, 120 rows; `sa-balance-sheet.csv`, 230 rows; `sa-expenses-by-purpose.csv`, 280 rows), a combined `sa-all-tables-long.csv` (930 rows), one Total State comparison file (`all-states-total-operating-statement.csv`, 300 rows) and a `table-index.csv`. The script's own 5 spot-check assertions (SA 2024-25: Total GFS revenue = 27208, GFS Net operating balance = 65, Total Expenses by purpose = 27143, Total health = 9297, GFS NET WORTH = 63165) all passed against the raw workbook cells before accepting the output; a sixth manual spot-check caught and corrected a fabricated comparison figure in an early README draft (the all-states Total GFS revenue for 2024-25 is $398,820m, not the $196,834m first written from memory — corrected after checking the actual converted CSV rather than left uncorrected). Deliberately did **not** mirror the Total State's Cash Flow Statement, Balance Sheet or Expenses-by-Purpose sheets this run (only its Operating Statement, for revenue/expense comparison context) — a disclosed scope decision to keep the addition proportionate to one scheduled pass, not a gap; the same `convert.py` pattern could extend to them in a future run. Added **`datasets/sa-state-government-finances/`**, a new ABS (CC BY 4.0) attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: every field in both workbooks is a whole-of-government financial aggregate (revenue, expense, asset, liability or cash-flow line item, or its COFOG-purpose reclassification) — no agency, program, contract, supplier or individual-level figure anywhere in either source file, confirmed by directly inspecting the full downloaded workbooks. 3 domains remain unchecked after this run (Public sector procurement and contracts transparency; Population and demographic projections; Weather, climate and rainfall statistics) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run, consistent with how this repository has treated the same boundary case in prior runs.

- 2026-07-08 (scheduled run): checked **Government grants and funding transparency (SA Government Grants Register / GrantConnect — recipient, amount, purpose and administering agency for state government grant payments)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Queried its CKAN API for `tags:grant` (13 results) — this search actually federates in results harvested from other states' portals (Brisbane City Council, Victorian State Revenue Office) alongside SA's own, a genuine curiosity worth flagging rather than a bug. Only two SA-attributable entries surfaced: (1) **Adelaide City Council's "Grants & Sponsorship"** dataset (CC BY 4.0, daily-refresh CKAN record, last content update Nov 2022) — genuinely open, but single local-council scope (one of 68 SA councils, City of Adelaide only), not a state government register, and falls under the "Local council services" domain already explored in an earlier run rather than this one; and (2) the **Department of Treasury and Finance's "First Home Owner Grant"** dataset — CC BY 4.0 confirmed via the CKAN API, but its CKAN record is stale (created and last metadata-modified 2016), it covers one specific housing-payment scheme rather than a general grants register (aggregate application counts by month/financial year and top-20 postcodes, not per-grant recipient/purpose/administering-agency records), and its sole resource is a link to `revenuesa.sa.gov.au/resources/statistics`, which returned HTTP 403 to every fetch attempt from this sandbox (both the WebFetch tool and direct `curl` with a standard browser user-agent) — a domain-level block distinct from the data.sa.gov.au block noted in earlier runs, so the underlying live file (reportedly updated 12 May 2026 per an indexed search snippet) could not be independently confirmed or mirrored this run. No consolidated whole-of-SA-government grants/payments-over-threshold disclosure register exists as open data: DPC, DHS, DIT and SAFA each publish only current-grants-to-apply-for pages (not historical recipient/payment data), and the GRANTassist portal (grantassist.sa.gov.au) is the same — a directory of open grant rounds, not a payments register. Checked the national fallback used successfully for several previous "no SA-specific dataset" domains: Commonwealth GrantConnect (`grants.gov.au`) explicitly **excludes State and Territory grant programs** by design (per its own help documentation), so a GrantConnect-sourced dataset could never carry a genuine SA state-government angle regardless of a recipient-location breakdown; the sole GrantConnect-derived entry harvested onto data.gov.au, "Grants Awarded Data", is limited to one Commonwealth department's (Department of Industry, Science and Resources) own awards, and its single CKAN "resource" is not a data file at all but a link to a GrantConnect help page — confirmed directly via the CKAN API (`package_show`), not assumed. GrantConnect's own Reports section supports ad-hoc report generation and export, but only as an interactive, session-based report builder (no stable bulk-download URL or documented API endpoint), so it isn't a source this repository's mirror-and-convert pattern can reproducibly point at. Conclusion: nothing added this run — a genuine, expected "no qualifying dataset" outcome, not a gap papered over. 4 domains remain unchecked after this run (State government finances; Public sector procurement and contracts transparency; Population and demographic projections; Weather, climate and rainfall statistics) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-07 (scheduled run): checked **Vehicle registration and driver licensing statistics (Department for Infrastructure and Transport — registered vehicle counts by type, fuel/energy source, age and region, and driver licence holder counts)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Given this domain's inherent privacy shape (vehicle/driver data, adjacent to the individual), ran this as a background research workflow with independent adversarial verification before writing anything — one research agent, then two separate agents independently re-fetching the live CKAN API/pages themselves (not trusting the prior pass's summary), re-checking licence text, individual-identifying fields, distinctness from `sa-road-crash-data`/`sa-expiation-notices`, and currency — same pattern used previously for firearms, sentencing, youth justice and domestic/family/sexual violence statistics. Found **two** genuine, live, current DIT datasets on data.sa.gov.au, both independently confirmed via the CKAN API and full-file downloads by all three agents: **`registered-vehicles-by-postcode`** (14 annual editions, 2012–2025, statewide vehicle counts by owner postcode/make/body-type/cylinders/manufacture-year) and **`drivers-licences-by-postcode-age-and-sex`** (34 quarterly editions, Q3 2017–Q4 2025, licence-holder counts by postcode/age/sex) — both `license_id: "cc-by"` / CC BY 4.0, confirmed verbatim from the live API response by every agent independently. One verifier caught a genuine gap in the initial research pass: the latest driver-licence resource is actually **Q4 2025** (published 2026-02-13), one quarter more recent than the Q3 2025 edition the first pass had found — corrected before finalising. Both verifiers converged on "recommend add, with caveats" rather than a clean pass: neither dataset contains a name, home address, or vehicle/licence identifier (satisfying `COMPLIANCE.md`'s literal individual-level-data test), but both are granular enough that a large share of cells have a count of exactly 1 (62.6% of vehicle rows, 14.9% of licence rows) — a small-cell statistical-disclosure pattern flagged explicitly in the README as a caveat (consistent with how the repo already treats `sa-expiation-notices` and the NDIS/DVA small-cell-suppression datasets) rather than treated as a blocker, since it doesn't meet the repo's actual name/address/plate exclusion test and matches disclosure risk already accepted in ABS Census-style small-area cross-tabulations. Confirmed distinct from both existing DIT/SAPOL road-transport datasets: `sa-road-crash-data` (incident-level crash/unit/casualty records) and `sa-expiation-notices` (offence-level infringement records) — neither publishes fleet-composition or licence-holder-population counts. Downloaded the latest vehicle-registration snapshot (30 June 2025, 30,059,018 bytes) and all 34 driver-licence quarterly files (33 CSV + one genuine XLSX despite a `.csv`-looking source filename, confirmed via `file`) directly from `data.sa.gov.au` over plain HTTPS — no `fetch.sh` needed, the portal was directly reachable this run. Wrote `convert.py`: for vehicles, renamed columns to `snake_case`, added an explicit `snapshot_date` column, and separated out the source's stray trailing grand-total value (2,043,732, embedded only in the file's last data row) into documentation rather than a spurious per-row column, asserting the sum of per-row counts exactly matches it before writing; for licences, merged all 34 quarterly files (each with its own title block, inconsistent trailing blank columns, and one quarter — Q1 2021 — with a one-off 5th `Total(TOTAL_CLIENT)` grand-total column) into one tidy long time series with an explicit `quarter` column, asserting the latest quarter's sum matches the source's own footer total (1,385,858) before writing. Deliberately did **not** merge the vehicle-registration dataset's 13 earlier annual editions (2012–2024, ~383 MB combined raw) into a multi-year time series this run — doing so would have made this single dataset larger than every other dataset in the repository combined bar one (`sa-crime-statistics`), which felt disproportionate for one scheduled run; disclosed explicitly in the README as a scope decision, not a silent gap, with the same `convert.py` pattern left ready to extend in a future run. Added **`datasets/sa-vehicle-registrations-and-licences/`** (bundling both companion DIT datasets in one folder, following the precedent already set by `sa-crime-statistics`'s own companion-breakdown bundling), a new DIT (CC BY 4.0) attribution line in `LICENSE-DATA.md`, and a new bullet in the root `README.md`. Privacy check: both files are aggregate postcode-level statistical counts, not row-level personal records — no name, address beyond postcode, licence number, or vehicle identifier (VIN/plate) in either source, confirmed by directly inspecting the full downloaded files (750,508 and 1,549,746 rows respectively) rather than trusting the landing-page description; the small-cell caveat above is noted but doesn't meet this repository's actual exclusion criteria. Only 2 domains remained unchecked before this run (Government grants and funding transparency; State government finances) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Public sector procurement and contracts transparency (SA Government Contracts and Tenders register), Population and demographic projections (ABS/SA Government estimates and projections by LGA), and Weather, climate and rainfall statistics (Bureau of Meteorology SA station data).

- 2026-07-07 (scheduled run): checked **Disability services and NDIS participant statistics (NDIS Quality and Safeguards Commission / SA breakdown)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Searched `data.sa.gov.au` / `data.gov.au` first: the only SA-attributable candidate is a "Disability Support Services" listing (mirrored on data.gov.au from a South Australian source) summarising people who used disability support services under the National Disability Agreement in **2013-14** — pre-dates the NDIS entirely and is long superseded, so not added. No current SA Government agency (Department of Human Services' disability programs or its successor) publishes an open, structured NDIS/disability-service dataset of its own. Pivoted to the national NDIA's own **NDIS Data Research** site (`dataresearch.ndis.gov.au`), reachable directly over plain HTTPS this run — no `fetch.sh` fallback needed. Its "Participant datasets" page lists dozens of quarterly CSV releases; selected **"Participants split by Local Government Areas (LGA)"** as the closest match to this repository's existing geography-based conventions (same shape as `au-veteran-population-by-lga`), over alternatives like the by-SA2/SA3/SA4 or by-diagnosis files, because LGA is already this repository's established join key back to ABS geography. Licensing required care: the *main* `ndis.gov.au` website is licensed CC BY-**NC** (non-commercial) 3.0, which would have excluded it — but the *data research* site (`dataresearch.ndis.gov.au`, where this dataset actually lives) carries its own, separate copyright statement, confirmed by live-fetching its Copyright page directly (via raw HTML, stripped of markup, not just a summarised fetch) and quoting it verbatim: **"licensed under Creative Commons CC BY licence, version 4.0... you may download, display, print and reproduce the written material on this website, even commercially..."** — genuinely open, no non-commercial carve-out, distinct from the main site's terms. Downloaded the raw CSV (467,004 bytes, confirmed via `file` as CSV text) directly from its `/media/4237/download?attachment` link, plus the NDIA's own two data-dictionary Word documents (for this dataset and for the sibling "Participant numbers and plan budgets" dataset, the latter mirrored specifically because it's the only source document that defines the `OT` and `MIS` state-code categories used across the NDIA's whole participant-by-geography dataset family). The raw file turned out to be a genuine time series, not a single snapshot: 18 quarterly extracts from 31 December 2021 through 31 March 2026, 9,643 rows total, 548 in the latest quarter alone (70 of them South Australian, 66,339 SA participants of 774,236 nationally that quarter, excluding suppressed cells) — richer than the single-point-in-time DVA/ABS releases this repository has favoured recently. Wrote `convert.py` to rename columns to `lower_snake_case`, standardise the source's `DDMONYYYY` dates to ISO 8601, and add a decoded `state_name` column (including plain-language explanations for the `OT`/`MIS` codes, sourced from the NDIA's own data-rules glossary) alongside the source's own `state_code` — no participant count recalculated or reinterpreted, and the source's own `<11` small-cell privacy-suppression marker (804 of 9,643 rows, 8.3%) preserved literally rather than converted to a number. Verified by spot-check against the raw CSV cell-for-cell. Added **`datasets/au-ndis-participants-by-lga/`** and a new bullet in the root `README.md`. Privacy check: every field is a place name or an aggregate participant count; no individual is identified, and the NDIA's own small-cell suppression (rather than any modification made here) is what prevents re-identification in low-population LGAs — same shape already accepted for `au-veteran-population-by-lga` and `au-prisoners-in-australia`. 3 domains remain unchecked after this run (Vehicle registration and driver licensing statistics; Government grants and funding transparency; State government finances) — at, not below, the "fewer than 3" threshold, so no new candidate domains were proposed this run.
- 2026-07-07 (scheduled run): checked **Communicable disease and public health surveillance (SA Health — notifiable disease/condition statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` reachable this run, but a `package_search` for "notifiable" returned no match for a weekly communicable-disease surveillance dataset — the only hit in that domain family was an unrelated one-off "2020-21 Food Notifiable Contaminants Data" CKAN package, itself CC BY, from the same SA Health notifiable-conditions program (supporting evidence that this data family is treated as open by the publisher, but not itself the target dataset). Pivoted to `sahealth.sa.gov.au` directly and found the Communicable Disease Control Branch's **Surveillance of notifiable conditions** page, which links two live, weekly-refreshed Power BI-export PDFs: a "5 Year and YTD Comparison Report" and a "Last 8 Weeks Report", both dated 6 July 2026 and showing data "as at 4/07/2026" — i.e. published the day before this run and covering data only 3 days old, making this the first genuinely live/weekly-refreshed (not frozen-historical) SA Health dataset added to this repository, in contrast to `sa-health-ed-performance` and `sa-mental-health-services`, both stalled at 2017-18. Licence confirmed by live-fetching SA Health's site-wide Copyright page and quoting it verbatim: CC BY 4.0, "free [to] share and adapt the material in any medium or format for any purpose, even commercially, provided: you give appropriate credit..." — no non-commercial or no-redistribution carve-out. Disclosed as a caveat rather than treated as fully equivalent to this repository's usual gold standard: unlike the repo's other SA Health datasets, these two PDFs have no `data.sa.gov.au` CKAN record and no per-dataset `license_id` field, so the licence rests on the site-wide statement alone — the first sahealth.sa.gov.au-direct (non-CKAN) dataset in the repository. Downloaded both PDFs directly over plain HTTPS (no `fetch.sh` needed): "20260706_DSIS+5yr+YTD+Report.pdf" (335,604 bytes, 3 pages) and "20260706_DSIS+Public_Last+8+Weeks.pdf" (291,153 bytes, 3 pages), both confirmed valid PDF v1.4 files, and mirrored verbatim in `raw/`. Neither PDF has visible cell borders in its table, so a lattice/stream-based table-extraction library (`pdfplumber`) was tried first and returned single merged-text blobs per page — abandoned in favour of parsing `pdftotext -layout`'s whitespace-aligned column output directly, splitting each disease row's trailing numeric tokens (after stripping comma thousands separators) from the — sometimes multi-word — disease name via regex. Wrote `convert.py` to produce three CSVs: a wide 5-year/YTD comparison table (63 diseases × 11 year/YTD columns), a wide last-8-weeks table (63 diseases × 8 ISO-dated week columns), and a long reshape of the latter (504 rows). The script sanity-checks itself against the raw PDF text before finishing (e.g. Campylobacter YTD 2026 = 1,373; COVID-19 Total 2022 = 489,909; RSV week-ending 4/07/2026 = 528) and fails loudly if fewer than 60 disease rows are parsed from either report — all checks passed. Investigated two apparent disease-list mismatches between the two reports before treating them as genuine rather than a parsing bug: "HIV - Newly acquired" appears only in the 5-year/YTD report (consistent with the source's own explanatory note that HIV newly-acquired cases have been excluded from weekly surveillance updates since March 2022), and "Suspected Food Poisoning" appears only in the 8-week report; both disclosed explicitly in the README rather than silently reconciled. Distinctness confirmed directly against both existing SA Health datasets in the repository: `sa-health-ed-performance` covers ED wait times/4-hour length-of-stay, and `sa-mental-health-services` covers acute mental-health ward performance — neither overlaps with weekly/annual notifiable communicable-disease case counts. Added **`datasets/sa-notifiable-conditions-surveillance/`**, a new SA Health (CC BY 4.0) attribution line in `LICENSE-DATA.md` explicitly noting the site-wide-vs-CKAN licence-provenance caveat, and a new bullet in the root `README.md`. Privacy check: both reports publish a single statewide aggregate case count per disease per time period, with no geographic, demographic or facility-level cross-tabulation of any kind in either file; several rare diseases show small counts (e.g. Botulism 0, Cholera 0, Diphtheria 11 YTD 2026), but because these are undifferentiated state-level totals with zero further dimensionality, no small-area or small-subpopulation re-identification risk applies — confirmed by inspecting the full extracted text of both PDFs directly. 4 domains remain unchecked after this run (Disability services and NDIS participant statistics; Vehicle registration and driver licensing statistics; Government grants and funding transparency; State government finances) — above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Waste and resource recovery (Green Industries SA — statewide waste diversion, recycling and landfill statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run (HTTP 200 on a live CKAN query). Ran a series of targeted `package_search` queries ("waste", "resource recovery", "recycling", "landfill", "Green Industries", "waste diversion", "kerbside", "solid waste") across the CKAN API. Green Industries SA's own CKAN organisation (the state agency directly responsible for this domain, formerly Zero Waste SA) publishes exactly one dataset — its 2023-24 Annual Report, a governance disclosure, not a statistics dataset. Zero Waste SA's legacy organisation (now marked inactive) publishes only two stale packages: `household-hazardous-waste` (a 2008-2013 collection-quantity table) and a recycling-guidance search-engine tool — neither a current tonnage series. The Department for Energy and Mining's `urban-waste` package is a single one-off 2015 biomass-residue estimation model built from 2013-14/2014-15 source data (Zero Waste SA's 2015 Recycle Activity report and 2014-15 council kerbside-collection data), not a maintained series. The one genuinely current SA-specific candidate that did surface was the EPA's **Waste Levy Rates** dataset (`waste-levy-rates`, metadata last modified 8 May 2025, CC BY 4.0 confirmed both via the CKAN API and the live page's own `DCTERMS.License` meta tag and "License" sidebar module) — a historical schedule of the statewide landfill levy (metro/country solid-waste $/tonne, statewide liquid-waste $/kilolitre) from its 1994 introduction through the 2023-24 rate. Downloaded directly (4,433 bytes, byte-for-byte match to `Content-Length`, no `fetch.sh` needed) and mirrored verbatim in `raw/`. This is real and current but narrow — a rate schedule, not the diversion/recycling/landfill tonnage statistics this domain also calls for — so it was paired with a national fallback rather than left to stand alone, matching this repository's precedent for narrow-but-genuine SA sources (e.g. the tourism-visitor-statistics pairing). Found DCCEEW's **National Waste and Resource Recovery Database 2024** (data.gov.au CKAN package `nwd2024`, released December 2024 per its own cover sheet, covering FY2006-07 to 2022-23): a single already-tidy 21,904-row table (Year, Jurisdiction, Category, Type, Source stream, Management, Fate, Tonnes, Headline scope?, Core waste?, Classification) with a dedicated `SA` jurisdiction value in 3,057 rows — exactly the statistics this domain description asked for. Licence required care: the CKAN API states `license_id: cc-by-2.5` / `Creative Commons Attribution 2.5 Australia`, but the dataset's own landing page (data.gov.au's newer Drupal-based redesign, distinct from the old CKAN theme used elsewhere) shows no per-dataset licence badge at all — only the site's generic footer text claiming "CC BY 4.0" for the website generally. Confirmed this is boilerplate, not a dataset-specific override, by checking the same footer text appears verbatim on DCCEEW's own unrelated publications page; treated the CKAN API's per-dataset field (CC BY 2.5 AU) as authoritative, consistent with how this repository has resolved the same site-wide-vs-per-dataset ambiguity for `au-veteran-population-by-lga` and `au-federal-election-results`. Downloaded the XLSX directly (1,315,615 bytes, byte-for-byte match to `Content-Length`, no `fetch.sh` needed) and mirrored verbatim in `raw/`. Wrote `convert.py` for each dataset: the SA levy-rates CSV lays out three side-by-side, differently-shaped tables on one sheet (two solid-waste series in parallel column blocks, then a separate liquid-waste block below a blank spacer row), reshaped into one tidy long table with ISO-standardised dates — caught and fixed an off-by-one during development that silently dropped the final 2023-24 solid-waste rate row (hardcoded row-count slice instead of detecting the blank separator row programmatically), re-verified the fix restored exactly 34 solid-waste periods x 2 zones (68 rows) plus 30 liquid-waste periods (98 total) before finalising. The national database's `Database` sheet needed only an XLSX-to-CSV conversion (no reshaping — it's already one flat table) plus an SA-filtered companion file. Investigated an apparent anomaly in the `tonnes` column (many values with long decimal tails, e.g. `216731.46000000002`) before treating it as a bug: inspected the raw XLSX's own XML directly and confirmed the exact same long-decimal string is stored in DCCEEW's source file, not introduced by this conversion or by Python's float handling — left unrounded, documented in the README as a genuine (if unusual) source characteristic rather than silently "cleaned". Added **`datasets/sa-waste-levy-rates/`** and **`datasets/au-waste-resource-recovery/`**, expanded the existing EPA attribution line and added a new DCCEEW (CC BY 2.5 AU) line in `LICENSE-DATA.md`, and added both to the root `README.md`. Privacy check: every row in both datasets is a statewide/jurisdiction-level rate or aggregate tonnage figure by year/category/type/management/fate — no individual, business, property or facility-level field in either source, confirmed by directly inspecting both downloaded files in full. Only 2 domains remained unchecked before this run (Communicable disease and public health surveillance; Disability services and NDIS participant statistics) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Vehicle registration and driver licensing statistics (Dept for Infrastructure and Transport, distinct from the crash/expiation-notice angles already covered), Government grants and funding transparency (SA Government Grants Register/GrantConnect), and State government finances (SA Budget Papers/Treasury, portfolio-level expenditure and revenue). Commit and push to `origin master` both succeeded.

- 2026-07-07 (scheduled run): checked **Building and construction approvals (ABS Building Approvals, Australia — number and value of residential/non-residential building approvals by state/LGA)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Ran a series of targeted `package_search` queries ("building approvals", "building rules", "development approval statistics", "construction statistics", "dwelling approvals", "building activity", "PlanSA approvals", "building consent", "building rules assessment", "development approval decision") across the CKAN API: the only two SA-portal candidates were `building-approvals` (org `ABS (SA Data)`, metadata last modified 23 January 2017) and `building-approvals1` (a `data.gov.au`-harvested duplicate of the same package, `original_harvest_source` present, `remote_last_updated: 2017-01-23`). `package_show` on `building-approvals` confirmed its only two resources are an XLS pointing at the old ABS catalogue number `8731.0` and a November-2016 HTML explanatory-notes page; fetching the `8731.0` link directly this run returned an HTTP 301 redirecting straight to the current ABS Building Approvals landing page, confirming the SA-portal record is a stale mirror of a discontinued ABS reference, not a live SA-published dataset. Also re-checked Housing and Urban Development's CKAN organisation (47 datasets) and PlanSA: the only approvals-adjacent record, `development-application-public-register`, was already checked and excluded in `sa-land-division-applications`'s own README (links only to a search webpage, not a downloadable dataset) — re-confirmed still the case. Pivoted to the national fallback: ABS's **Building Approvals, Australia, May 2026** release (released 1 July 2026, 11:30am). Fetched the live release page and its Data downloads listing directly (not from memory) to get real table numbers and URLs — the release publishes 88 tables in total; this repository mirrors 5: Table 07 (total dwelling units approved by state, July 1983–May 2026), Tables 39/40/41 (value of total/residential/non-residential building approved by state, back to 1970/1973), and Table 10 (dwelling units by Greater Capital City Statistical Area, including Greater Adelaide, July 2001–May 2026) — the closest genuine regional breakdown this release publishes as a static file; confirmed directly that SA2/LGA-level figures require a live ABS Data Explorer/TableBuilder query rather than a bulk download on this release page, so no LGA-level table was force-fitted into scope. Licence confirmed directly from ABS's own copyright page (`abs.gov.au/website-privacy-copyright-and-disclaimer`): CC BY 4.0, identical wording already used for `au-criminal-courts-sentencing-outcomes`. Downloaded all 5 workbooks directly from `abs.gov.au` (reachable over plain HTTPS, no `fetch.sh` needed) and mirrored them verbatim in `raw/`. Wrote `convert.py` to unpivot each ABS Time Series Workbook's wide (one column per series) `Data1` sheet into long-format rows, reading the source's own header metadata (semicolon-separated column descriptions, Unit/Series Type/Series ID rows) to label each observation — no totals recalculated, no rates re-derived. Output: 5 per-table CSVs, a combined long file (55,349 rows), a South-Australia/Greater-Adelaide-filtered file (8,265 rows) and a table index. Spot-checked 5 values against the raw workbook cells: SA Table 07 dwelling units approved, May 2026, Original series = 1,506; SA Table 39 value of total building approved, May 2026, Original = $1,153,652,000; SA Table 40 value of residential building, May 2026, Original = $773,918,000; SA Table 41 value of non-residential building, May 2026, Original = $379,734,000; Greater Adelaide Table 10 houses approved, July 2001 (series' first data point) = 552 — all matched the source cells exactly. An initial version of the converter mis-parsed Tables 40/41's 3-part semicolon header (measure ; building-sector-type ; state) as if it were Table 10's (measure ; dwelling-type ; GCCSA) shape, wrongly attributing "Total Residential"/"Total Non-residential" to the `geography` column instead of `dwelling_type` — caught during spot-checking when the SA filter returned zero rows for Tables 40/41, fixed by detecting the 2-part vs 3-part header shape generically rather than by table/geography level, then re-verified all 5 spot-check values matched before finalising. Distinctness confirmed directly against both existing planning-adjacent datasets: `sa-land-division-applications` is an application-level land-subdivision register (splitting one title into many), and `sa-planning-zones` is zoning-boundary classification (what's permitted where) — this dataset is a third, distinct thing, a monthly statistical count/dollar-value of building-approval permission granted, with no overlap in unit of observation. Added **`datasets/au-building-approvals/`** and an ABS CC BY 4.0 attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. Privacy check: directly inspected all 5 downloaded workbooks — every column is a geography, dwelling/sector-type, series-type, unit, ABS series ID, reference month or count/dollar value; no applicant, builder/certifier or property-address field of any kind. 3 domains remain unchecked after this run (Waste and resource recovery; Communicable disease and public health surveillance; Disability services and NDIS participant statistics) — at the "fewer than 3" threshold boundary (3 remaining, not fewer than 3), so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Threatened species and biodiversity conservation listings (Department for Environment and Water — threatened flora/fauna listings and conservation status)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed `data.sa.gov.au` directly reachable this run. Ran a series of targeted `package_search` queries ("threatened", "conservation", "species", "flora", "fauna", "biodiversity", "rare endangered", "schedule threatened", "conservation status", "national parks and wildlife act", "SA Seed", "naturemaps") across all 292 packages: the Department for Environment and Water's `flora-survey-sites` and `fauna-survey-sites` packages ("Flora/Fauna Species Observations") are CC BY 4.0 but stale (CKAN metadata last modified July 2016), and their sole resource is a link to an Atlas of Living Australia biocache search — species presence/observation records, not a conservation-status listing, with no bulk file to mirror. `naturemaps` is a CKAN pointer to the interactive NatureMaps map viewer (naturemaps.sa.gov.au), not a downloadable dataset. The one genuinely current SA-specific candidate, `biodiversity-mapping-interim` (metadata modified February 2026, CC BY 4.0, 8 Landscape SA regional GeoJSON/KMZ/SHP archives mapping areas of known EPBC/National Parks and Wildlife Act biodiversity value), is a habitat-area layer rather than a species-level list — a different angle from what this domain called for — and was left as a documented candidate for a future pass rather than pursued this run. No dedicated SA statutory schedule (e.g. National Parks and Wildlife Act 1972 Schedules 7-9 of endangered/vulnerable/rare species) surfaced as open structured data anywhere on the portal. Pivoted to the national fallback: DCCEEW's **Threatened Species and Ecological Communities of National Environmental Significance** (data.gov.au package `threatened-species-state-lists`), which publishes two genuinely current CSVs — Threatened Species State Lists (extracted 6 February 2026, 2,208 rows) and Ecological Communities State Lists (extracted 20 March 2026, 111 rows) — each with an indicative-occurrence column per state/territory, including a dedicated `SA` column. Licence confirmed two independent ways: the CKAN `package_show` API response (`license_id: cc-by`, `license_title: Creative Commons Attribution 3.0 Australia`, `license_url: .../by/3.0/au/`) and a direct fetch of the live dataset page, whose own "License" sidebar module shows the identical licence text and link. Downloaded both CSVs directly from `data.gov.au` (reachable over plain HTTPS, no `fetch.sh` needed) and confirmed byte-for-byte via `Content-Length` match before mirroring them verbatim in `raw/`. Wrote `convert.py` to rename columns to `snake_case` (spelling out the nine external-territory abbreviations — ACI/CKI/CI/CSI/JBT/NFI/HMI/AAT/CMA — in the header names since they aren't self-explanatory), standardise the `Date extracted` field from the source's `YYYY-Mon-DD` format to ISO `YYYY-MM-DD`, and produce a South-Australia-filtered file alongside each full national file; no other value was recalculated or reinterpreted, confirmed by spot-checking rows (e.g. `Acacia anomala`, `Iron-grass Natural Temperate Grassland of South Australia`) cell-for-cell against the raw CSVs. 319 of the 2,208 listed species occur in South Australia (185 fauna, 134 flora) and 18 of the 111 listed ecological communities. Added **`datasets/au-threatened-species-ecological-communities/`** and a DCCEEW CC BY 3.0 AU attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. Privacy note: this dataset describes species and ecological communities, not people — no individual-level personal data applies at all, so no redaction question arose. 4 domains remain unchecked after this run (Building and construction approvals; Waste and resource recovery; Communicable disease and public health surveillance; Disability services and NDIS participant statistics) — at or above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Ambulance services and response times (SA Ambulance Service — call volume, response time performance and ramping statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed via the CKAN `package_search` API that the `sa-health` organisation's ambulance-tagged datasets on data.sa.gov.au (22 results for "ambulance", 7 tagged "SA Ambulance") are exclusively the standard whole-of-government annual-report disclosure template (fraud, contractors, consultants, complaints, executive employment, whistleblowers' disclosure, WHS/return-to-work performance) — none is an operational statistics series. Two datasets that genuinely did cover this domain once — "SA Ambulance Service reported ambulance responses by priority" and "...incidents by priority" — were found via web search and their Research Data Australia metadata records (confirming CC BY 4.0), but both now 404 on their data.sa.gov.au landing page and return `"Authorization Error: not authorised to read package"` from the CKAN API (package ID `f616ef5e-49b9-483b-9bc4-0276adc8afea`), confirmed independently via a direct page fetch and a raw API call — a genuine "used to be open, now isn't" gap, not a fabricated exclusion. SA Health's "Ambulance waiting times" webpage returned HTTP 403 (Cloudflare bot-challenge) to direct fetching, consistent with the same block pattern already documented for `corrections.sa.gov.au` and `safework.sa.gov.au`; search snippets describe it as a live monthly chart page rather than a downloadable bulk file. Pivoted to the national fallback: Productivity Commission's **Report on Government Services 2026, Part E, Section 11 — Ambulance services**, which publishes 12 data tables (revenue, human resources, registered paramedics, incidents/responses/patients/transport, response times, call-answering time, pain management, patient experience, workforce, training enrolments, expenditure, cardiac-arrest survival rate), every one with an explicit `SA` column. Licence confirmed directly from `pc.gov.au/copyright` (CC BY 4.0, same wording already used for `au-child-protection-services`). Downloaded both the XLSX and companion CSV directly from `assets.pc.gov.au` (reachable over plain HTTPS, no `fetch.sh` needed) and mirrored them verbatim in `raw/`. The companion CSV was already a single well-structured file (not split across per-table files needing merging), but wide-format (one column per jurisdiction: NSW/Vic/Qld/WA/SA/Tas/ACT/NT/Other/Aust) rather than tidy long format, so wrote `convert.py` to unpivot it into one row per jurisdiction per observation — the same treatment already applied to `au-work-health-safety-jurisdictional-comparison`'s Safe Work Australia release. Output: 12 per-table CSVs, a combined long file (10,323 rows), an SA-pre-filtered file (1,150 rows) and a table index. Spot-checked SA's Table 11A.5 2024-25 response times in the converted output against the raw CSV cell-for-cell (90th percentile 35.8 min capital city / 33.8 min statewide; 50th percentile 13.4 min capital city / 12.8 min statewide) — exact match, no recalculation. Noted explicitly in the README that this release has no dedicated hospital-ramping metric (a genuine scope limit in the available open data, not introduced here). Added **`datasets/au-ambulance-services-performance/`** and a Productivity Commission CC BY 4.0 attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. Privacy check: every table is a jurisdiction-level (or jurisdiction-by-age-band/gender) aggregate — count, rate, percentage, dollar amount or time duration — no patient, paramedic or case-record identity of any kind, confirmed by directly inspecting the real downloaded CSV across all 12 tables. Only 2 domains remained unchecked before this run (Threatened species and biodiversity conservation listings; Building and construction approvals) — below the "fewer than 3" threshold, so 3 new candidate domains were proposed and added: Waste and resource recovery (Green Industries SA); Communicable disease and public health surveillance (SA Health); Disability services and NDIS participant statistics (NDIS Commission, SA breakdown).

- 2026-07-07 (scheduled run): checked **Court sentencing outcomes (Courts Administration Authority / SA Sentencing Advisory Council — sentence type and length statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran this as a background research workflow with independent adversarial verification given how inherently individual-level sentencing data is (one research agent, then two separate agents independently re-checking the licence, privacy-safety, reachability and distinctness of the candidate that surfaced) — same pattern used for firearms, youth justice and domestic/family/sexual violence statistics. Confirmed directly via the CKAN API that the Courts Administration Authority's `caa` organisation on data.sa.gov.au publishes only its 10 standard annual-report administrative datasets (At A Glance, WHS/RTW, Sheriffs Office, Whistleblowers, Library, Fraud, Executive Employment, Contractors, Consultants, Complaints) — already covered by `sa-courts-at-a-glance` — with nothing on sentencing. A portal-wide `package_search` for "sentencing"/"sentence" surfaced sentencing datasets from Victoria, NSW, ACT and Queensland, but nothing SA-specific. Separately confirmed a "Sentencing Advisory Council of South Australia" does genuinely exist (established 2012, under the Attorney-General's Department), but it publishes narrative research reports and "sentencing snapshots," not structured open data — not a viable dataset source. Pivoted to the national fallback: ABS's **Criminal Courts, Australia, 2024-25** release, which publishes a dedicated South Australia data cube ("7. Defendants finalised, South Australia (Tables 39 to 44)") plus a companion national cube with a state/territory breakdown for sentence length and fine amount ("13. Sentence length and fine amount, Australia (Tables 78 to 84)"). Licence independently confirmed twice by fetching ABS's own copyright page directly (CC BY 4.0, with unit-record/microdata explicitly excluded from the grant — meaning ABS itself doesn't publish individual defendant records), corroborated by a stale 2016-metadata CKAN mirror on data.sa.gov.au recording the same `license_id: cc-by`. One of the two parallel verification agents hit an unrelated infrastructure failure (`StructuredOutput retry cap (5) exceeded`) rather than producing a verdict — noting this plainly since it's the second time this task type has hit a transient agent-infrastructure issue (the first being the "Overloaded" API error during the multicultural-affairs run) — so a replacement verification agent was run directly afterward rather than treating the technical failure as a rejection; it independently downloaded both live files, inspected real cell contents (an identifier-pattern regex scan across every sheet found zero name/DOB/case-number matches), and reached the same "add" recommendation as the first successful verifier, with both fully corroborating rather than merely repeating each other (e.g. the second verifier independently confirmed the companion national cube and cross-checked all three existing court/crime/prisoner datasets' own READMEs for genuine non-overlap). Confirmed distinct from all three existing SA justice datasets: `sa-courts-at-a-glance` (workload/staffing only), `sa-crime-statistics` (police-reported incidents, pre-court stage) and `au-prisoners-in-australia` (a point-in-time custodial census, a stock measure, versus this dataset's sentencing-outcome flow measure covering non-custodial dispositions a prisoner census never captures). Downloaded both xlsx files directly (`abs.gov.au` reachable over plain HTTPS this run, no `fetch.sh` needed) and mirrored them verbatim in `raw/`. Wrote `convert.py` to parse both workbooks: rather than forcing three structurally distinct ABS table layouts (a nested-section time series, a flat offence x method-of-finalisation grid, and a repeated year-block layout) into one artificial schema, it produces two tidy long-format schemas (`section`-based and `financial_year`-block-based) plus, for the five state-broken-out tables (79-84), both an all-states comparison file and a South-Australia-filtered file from the same source rows. Caught and fixed one real bug during self-testing (a state-marker detection routine that silently produced 0 SA-filtered rows for Tables 79-84 before the fix) and spot-checked multiple converted values cell-for-cell against the raw workbooks (e.g. SA "01 Homicide" custodial sentences: mean 93.8 / median 89 months, matching source exactly) before accepting the output. Output as 19 files under `data/`: 13 per-table CSVs (some split into all-states/SA-filtered pairs), two combined long files (12,357 and 2,012 rows) and a `table-index.csv` lookup describing which file covers which table/geography. Table 78 is flagged explicitly in both the README and `table-index.csv` as national-only (no SA breakdown exists in the source for that one table) rather than silently including it as if it were SA data. Added **`datasets/au-criminal-courts-sentencing-outcomes/`** and an ABS CC BY 4.0 attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. Privacy check: every table is aggregate defendant counts or mean/median age/sentence-length/fine-value statistics by offence/outcome/court-level/state/sex — no defendant name, DOB, address or case number in either workbook, independently confirmed by two separate agents actually opening and scanning the real downloaded files rather than trusting the landing page's description. 3 domains remain unchecked after this run (Ambulance services and response times; Threatened species and biodiversity conservation listings; Building and construction approvals) — at the "fewer than 3" threshold boundary (not below it), so no new candidate domains were proposed this run; expect the next run to need 2-3 more added.

- 2026-07-07 (scheduled run): checked **Firearms licensing and registration statistics (SAPOL Firearms Branch — firearm licence/registration counts by category and region)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual (this domain sat at the top specifically because it's directly relevant to Rin's own agency). Ran a background research workflow with independent adversarial verification given the sensitivity of firearms data (one research agent, then two separate agents independently re-checking the licence and the privacy-safety/reachability of the sole candidate that surfaced) — same pattern used previously for youth justice and domestic/family/sexual violence statistics. Confirmed directly via the CKAN API that the `south-australia-police` organisation on data.sa.gov.au publishes exactly 4 packages (`expiation-notice-system-data`, `crime-statistics`, `annual-reporting-data`, `road-safety`) and none is a firearms dataset; SAPOL's own `annual-reporting-data` package — a comprehensive 30-resource suite of statutory-act CSVs (Controlled Substances Act, Liquor Licensing Act, Road Traffic Act, Summary Offences Act sections, etc.) — conspicuously has no Firearms Act 1977 resource, suggesting this data is deliberately not released through that channel rather than merely overlooked. `package_search` for firearms/weapons/firearm licence/firearm registration/gun/SAFR across all 292 packages on data.sa.gov.au surfaced no SA-specific candidate (only NSW gazette noise, Victorian hunting layers and Geoscience Australia Woomera/Maralinga-era seismic data). police.sa.gov.au's Firearms Branch pages are behind Cloudflare bot-challenge protection (HTTP 403, confirmed by two independent fetch attempts, curl and WebFetch alike) and, per WebSearch snippets, appear to be compliance/process pages (self-audit info, mySA GOV digital licence info, SA Firearms Register modernisation project) rather than a downloadable statistics table regardless. National fallback checked and rejected: the National Firearms Register (Home Affairs/ACIC) is explicitly restricted to law enforcement and select government agencies only, not public, with no public release on its roadmap (targeted ~2028); the Productivity Commission's Report on Government Services 2026 police-services chapter has no firearms-licensing indicator (firearms appear only in an unrelated asset-depreciation footnote); the AIC publishes narrative firearms research only, no structured open dataset with state breakdowns. The one dataset that did surface, `seized-or-surrendered-firearms` (Creative Commons Attribution 2.5 Australia, `http://creativecommons.org/licenses/by/2.5/au/`, published by the Commonwealth's SDG reporting programme and syndicated from data.gov.au), was downloaded and inspected in full independently by two separate agents: both of its CSV resources are single-row national totals (a 2017 amnesty count of 57,000 firearms; a 2004-16 cumulative firearm-trace-request count of 6,874) with no licence category, no region/LGA and no genuine time series — real, correctly licensed and privacy-safe (no individual-level fields of any kind — nothing to redact), but too trivial and stale to be a genuine "firearm licence/registration counts by category and region" dataset as this domain called for, so adding it would be force-fitting rather than a real fit. One additional wrinkle surfaced only during verification: this dataset's own page and CKAN record on data.sa.gov.au now return 404 (it survives there only as a stale Solr search-index entry), even though it remains fully live at its original data.gov.au source — noted for the record since it reinforces, rather than changes, the "don't add" conclusion. Nothing added. Privacy note: no individual-level licence-holder or registration data was found anywhere in this search, so no redaction question arose — this domain, like use-of-force data (`sa-police-oversight-gap`), appears to be one SAPOL treats as non-published rather than open, consistent with how most Australian jurisdictions handle firearms data. 4 domains remain unchecked after this run (Court sentencing outcomes; Ambulance services and response times; Threatened species and biodiversity conservation listings; Building and construction approvals) — at or above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Youth justice and youth offending statistics (Department of Human Services Youth Justice)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran this as a background research workflow with independent adversarial verification (one research agent, then two separate agents independently re-checking the licence and the privacy-safety of the recommended candidate), given how sensitive youth-offender data is. Confirmed directly via the CKAN API that `data.sa.gov.au`'s `dept-of-human-services` organisation (~40 packages) publishes no youth-justice statistics dataset — only a facility-locations layer that lists a Youth Justice office as a location type, plus unrelated packages; searches for "youth justice", "youth detention", "Kurlana Tapa", "Cavan Training Centre", "youth offending" and "reoffending" surfaced no SA-published result. Pivoted to AIHW's **Youth Justice National Minimum Data Set**, published as the annual "Youth justice in Australia" report. The research pass initially found the 2023–24 edition, but a direct page fetch of its "Data" tab surfaced a "Newer release available (12 May 2026)" banner pointing to the 2024–25 edition — used that instead, since currency matters for this repo's standards. Both AIHW's site-wide `/copyright` page (independently re-fetched: *"Material that can be copied or downloaded from this website has been released under a Creative Commons BY 4.0 (CC-BY 4.0) licence"*) and a separate privacy-focused pass (confirming AIHW's technical notes describe internal small-cell suppression — rates not calculated under a cohort of 5; smallest SA figure found in this dataset is 16 — and that the underlying client-level YJ NMDS extract states submit is never itself published) independently verified the candidate before it was added. The report's own "Data" tab renders its five download links via client-side JavaScript, so a static `curl`/`WebFetch` couldn't recover them (as both research and verification agents found); opened a real browser session (Chrome extension) and read the resolved `<a href>` values directly to get all five working XLSX URLs, then downloaded them over plain HTTPS (`data.sa.gov.au`'s blocked/unblocked status doesn't apply here since this is a national aihw.gov.au source) — no `fetch.sh` fallback needed. The five workbooks (`s1-s33.xlsx` through `s142-s151.xlsx`) contain 290 distinct sub-tables (Table S1 through S151, many bundling several lettered sub-tables per sheet, e.g. S1a/S1b). Wrote `convert.py` to parse all 290 programmatically: it locates each sub-table's title (handling titles with an embedded colon, e.g. "Table S33a: ... 2024–25: Young people ..."), locates the real header row (skipping single-cell subtitle continuation lines that aren't the header), splits each header into leading dimension columns vs. trailing pivoted value columns (states/territories, ages, or years) via a token-recognition heuristic, forward-fills merged-cell row labels, and distinguishes genuine mid-table section headings (e.g. Table S111's "Unsentenced"/"Sentenced" split, preserved as an extra `section` column in 17 of 290 tables) from footnote/source lines (matched via `Note`/`Source`/footnote-marker/dash patterns) even where the source has no blank separator row between the last data row and its footnotes. Caught and fixed three real parsing bugs during development by spot-checking output against the raw workbook cell-for-cell (e.g. South Australia age 12 under supervision on an average day, Table S1a: `1.7` raw → `1.7` processed): sub-tables with an extra one-cell subtitle line before their header (S33 series, initially produced 0 rows), tables with no blank row before their footnotes (S114/S116/S122, initially ingested footnote text as garbage data rows), and one genuine spreadsheet artifact — two placeholder rows of literal hyphens with no row label at all at the end of Table S25 — correctly identified and dropped as not being real data rather than kept. Output as `data/tables/<table-code>.csv` (290 files, one per source sub-table, native shape preserved) plus `data/all-tables-long.csv` (58,985 rows, all 290 tables stacked with a generic `dimensions` key=value column, same pattern as the FDSV dataset) and `data/table-index.csv` (290 rows). 182 of the 290 tables carry an explicit `SA` column; two supplementary tables (S134, S135) are SA-only trend series. Added **`datasets/au-youth-justice-supervision-statistics/`** and an AIHW CC BY 4.0 attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. Identified the Productivity Commission's Report on Government Services 2026 Youth justice services chapter as a genuine, current, CC BY 3.0 AU-licensed corroborating source with its own SA breakdown, but didn't pursue it this run to keep scope modest — documented in the new README as a candidate for a future pass. Only 2 domains remained unchecked after this run (Firearms licensing and registration statistics; Court sentencing outcomes) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Ambulance services and response times (SA Ambulance Service, distinct from the SES/CFS/MFS angle already covered), Threatened species and biodiversity conservation listings (Dept for Environment and Water, distinct from the native vegetation floristic-type angle already covered), and Building and construction approvals (ABS, distinct from the land-division/planning-zone angles already covered).

- 2026-07-07 (scheduled run): checked **Domestic, family and sexual violence statistics (SA Attorney-General's Department / Office for Women — reported incidents, intervention orders)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran this as a background research workflow with independent adversarial verification, same pattern as the multicultural-affairs run: one research agent, then two separate agents independently re-checking the licence and the privacy-safety/reachability of the recommended candidate rather than trusting the research pass's own self-report — appropriate given how sensitive this domain is (any row-level victim/respondent data would be a genuine privacy problem, not just a documentation nicety). Checked `data.sa.gov.au`'s `attorney-general-s-dept` organisation directly via the CKAN API: only a legislative database and Public Trustee administrative data, nothing on domestic/family/sexual violence. Checked the `caa` (Courts Administration Authority) organisation in full: only the standard annual-report administrative disclosures already documented repeatedly elsewhere in this log — no intervention-order statistics; `courts.sa.gov.au`'s own statistics page has an "Intervention orders" process-information page but no downloadable statistics dataset. Confirmed via the full `organization_list` that no "Office for Women" organisation or unit publishes on the portal at all. Targeted `package_search` queries ("domestic violence", "intervention order", "sexual violence", "family violence", "protection order", "office for women", "abuse") surfaced only out-of-jurisdiction Victoria/NSW datasets and this repo's own already-covered `sa-crime-statistics` — no SA-published dataset exists for this domain. Pivoted to AIHW's **"Family, domestic and sexual violence"** supplementary data-tables workbook (national), confirmed genuinely current (workbook `Last-Modified` 9 April 2026) and CC BY 4.0 two independent ways: verbatim text match against AIHW's live `/copyright` page, and confirmation that the licence URL resolves to the genuine `creativecommons.org/licenses/by/4.0` deed rather than a redirect elsewhere — plus a full 49-sheet scan finding no embedded dataset-specific licence override. Privacy independently re-verified by inspecting all 40 data sheets directly (not sampled): every table is pre-aggregated counts/rates/proportions/mean-scores by year, jurisdiction, sex, age group and similar categories; zero individual-identifying fields found; ABS Recorded Crime - Victims figures are additionally randomly adjusted by the ABS itself to prevent re-identification. Downloaded the workbook directly (968,368 bytes, byte-for-byte match to AIHW's reported content-length, no `fetch.sh` fallback needed) — a 49-sheet workbook compiling 10 source collections (ABS Personal Safety Survey, ABS Recorded Crime - Victims, AIC National Homicide Monitoring Program, AIHW Child Protection NMDS, AIHW National Hospital Morbidity Database, AIHW Specialist Homelessness Services Collection, ANROWS National Community Attitudes Survey, Services Australia Crisis Payment claims, Kids Helpline, 1800RESPECT) across 40 distinct tables, each with its own dimension columns rather than one uniform schema. Wrote `convert.py` to programmatically locate each table's header row (position varies row 4-17 depending on how many notes/footnotes precede it, rather than assuming a fixed offset), convert headers to `snake_case`, strip thousands-separator commas from genuine numeric values while preserving the source's own data-quality flags (`n.a.`, `n.p.`, `—`, `*`, `**`) exactly as published, and melt the one wide-format table (Kids Helpline 2, one column per year) into the same tidy shape as the other 39 — verified several converted values cell-for-cell against the raw workbook after conversion (e.g. South Australia sexual-assault victims 2010, RCV 6: `1,363` raw → `1363` processed; children substantiations 2023-24, CPNMDS 2, Girls: `21154` in both). Output as `data/tables/<table-code>.csv` (40 files, one per source table) plus `data/all-tables-long.csv` (14,971 rows, all 40 tables stacked with a generic `dimensions` key=value column rather than forcing incompatible dimension names like RCV's `Jurisdiction` and NHMD's `Geography` into one shared column) and `data/table-index.csv` (40 rows). Documented plainly in the new README that only RCV 6/9/10 and the CRP tables carry a jurisdiction/SA breakdown — the rest (PSS, NHMP, CPNMDS, NHMD, SHSC, NCAS, both helplines) are national-only per the source's own publication scope, not a gap introduced here. Added **`datasets/au-family-domestic-sexual-violence-statistics/`** and an AIHW CC BY 4.0 attribution line to `LICENSE-DATA.md`. Root `README.md` updated with a new bullet. 3 domains remain unchecked after this run (Youth justice and youth offending statistics; Firearms licensing and registration statistics; Court sentencing outcomes) — at the "fewer than 3" threshold exactly, so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Mental health services (SA Health / Mental Health Commission of SA)**. Career context re-checked directly: the Career Path `MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Found and confirmed SA Health's **"SA Health Mental Health data"** package on data.sa.gov.au directly via `package_show?id=mental-health-data`: `license_id: cc-by`/`license_url: .../by/4.0`. It's a genuine, real (non-boilerplate) dataset — three financial-year time series (average length of stay, 28-day readmission rate, 7-day post-discharge community follow-up rate) broken down by 8 acute ward types — but the portal's own metadata hasn't changed since 2018-08-07 and all three source files stop at 2017-18, matching the same staleness pattern already documented for `sa-health-ed-performance`. `data.sa.gov.au` was directly reachable this run (no `fetch.sh` needed); downloaded all 3 XLSX resources verbatim into `raw/`. Wrote `convert.py` to reshape each file from wide (one column per ward-type breakdown) to long/tidy format and stack all three indicators into one CSV with an `indicator` column, normalising the 3 files' slightly different column-header wording onto one consistent `ward_type` label set and preserving the 2017-18 partial-year footnote (1 June 2017-31 May 2018, not a standard financial year) in a dedicated column rather than dropping it. Added `datasets/sa-mental-health-services/` (README, `raw/`, `convert.py`, `data/sa-mental-health-services.csv`, 120 rows) and one line to the root README's "What's here" list. Checked the SA Mental Health Commission's own 7 datasets on data.sa.gov.au (Contractors, Consultants, Public Complaints, Executive Employment, Whistle-blowers Disclosure, Fraud Detected in the Agency, WHS/RTW Performance) — all are the standard whole-of-government governance-disclosure template already excluded for other agencies elsewhere in this repo, not service-usage data; excluded on the same grounds. Also identified AIHW's national **Mental Health Services in Australia** data tables (2023-24 reference year, current, CC BY 4.0 per AIHW's site-wide copyright page) as a strong candidate to pair with this stale SA series in a future pass — its state/territory-breakdown files (e.g. *State and territory community mental health care 2023-24*) are served through a dynamic JavaScript-rendered listing rather than static download URLs, so reliably fetching the exact file wasn't completed within this run's modest scope; documented as "Not pursued this pass" in the new dataset's README rather than silently dropped. No fabricated data, no unresolved blocked domain.

- 2026-07-07 (scheduled run): checked **Crime and offence statistics (SAPOL recorded crime/reported offences by offence type and LGA)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual (this domain sat at the top specifically because it's directly relevant to Rin's own SAPOL analytics remit). Confirmed the `south-australia-police` organisation's **"Crime statistics"** package (CKAN ID `860126f7-eeb5-4fbc-be44-069aa0467d11`) directly via `package_show`: `license_id: cc-by`/`license_url: .../by/4.0`, and genuinely current — the newest resource (2025-26 Q1-Q3) was last modified 28 May 2026, six weeks before this run. This is exactly the general recorded-crime series this domain description called out as missing, distinct from `sa-expiation-notices` (traffic infringements only) and `sa-road-crash-data` (crashes only): suburb/postcode-level offence counts by offence type, published as 16 annual/partial-year files (financial years 2010-11 to 2025-26 Q1-Q3), plus a companion set of 16 files for the Family & Domestic Abuse-related-offences subset (postcode/quarter only, no suburb/exact date) — SAPOL's own notes state explicitly the two series "must not be added together" since the FDV files are a subset already counted in the main series. Downloaded all 32 resources directly (`data.sa.gov.au` reachable over plain HTTPS this run, no `fetch.sh` needed) — 163MB combined raw, mirrored verbatim in `raw/crime-statistics/` and `raw/family-domestic-abuse-offences/`. Wrote `convert.py` to merge each 16-file group into one tidy table: standardised `Reported Date` from source `DD/MM/YYYY` (inconsistently zero-padded across years) to ISO, normalised column names to `snake_case`, and dropped a fully-empty `,,,,,,` trailing export-artifact row present at the end of every single one of the 32 source files (9 rows across the 16 crime files, 2 across the 16 FDV files) — confirmed no real value was altered by spot-checking that the merged table's total `offence_count` (1,783,976) exactly matches a row-for-row sum of the raw files. The merged Crime statistics table came to 1,485,365 rows and ~185MB unsplit — over GitHub's 100MB single-file push limit — so it's published as four contiguous financial-year chunks (`-2010-11-to-2013-14.csv` through `-2022-23-to-2025-26.csv`, ~40-44MB each, identical columns, documented in the README as concatenable) rather than one file; the FDV table (46,745 rows, 5.2MB) didn't need splitting. Investigated a genuine data-quality question rather than taking the source's own resource description at face value: one resource note claims location is excluded specifically "to ensure confidentiality for victims" of sexual assault and related offences, but checking the actual merged data directly, all 12,596 sexual-offence rows do carry a suburb value, while the 4,547 blank-suburb rows (0.3% of the table) instead concentrate in fraud, theft, assault and property-damage categories — documented as an unresolved discrepancy between the publisher's stated rationale and the observed data, rather than silently assuming the stated reason holds. Also noted, without silently merging, that some offence-category labels change wording across years for what's evidently the same offence type (e.g. `FRAUD AND RELATED OFFENCES` vs `FRAUD DECEPTION AND RELATED OFFENCES`) — left exactly as SAPOL published each year. Added **`datasets/sa-crime-statistics/`** and expanded the existing "SA Police data" line in `LICENSE-DATA.md` to cover this dataset alongside expiation notices. Root `README.md` updated with a new bullet. Privacy check: every row in both tables is an aggregate offence count for a suburb/postcode + offence-type + period combination — no name, date of birth, address or other individual-identifying field in either table, consistent with this repo's standing "no individual-level identifying fields" rule. Only 2 domains remained unchecked after this run (Mental health services; Domestic, family and sexual violence statistics) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Youth justice and youth offending statistics (Department of Human Services Youth Justice, distinct from the adult-corrections angle already covered nationally), Firearms licensing and registration statistics (SAPOL Firearms Branch — directly relevant to Rin's own agency), and Court sentencing outcomes (Courts Administration Authority / SA Sentencing Advisory Council, distinct from the workload/staffing angle already covered by `sa-courts-at-a-glance`).

- 2026-07-07 (scheduled run): checked **Multicultural affairs and migration settlement (SA multicultural affairs / humanitarian and skilled migration settlement statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` file timestamp is unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran this as a background research workflow with independent adversarial verification (separate agents re-checked the licence, privacy-safety and file-reachability of the recommended dataset rather than trusting the research pass's own self-report) — one workflow run hit a transient "Overloaded" API error on the research agent and had to be resumed after a null-handling fix; noting this since it's a first for this task, not a recurring issue. Checked data.sa.gov.au's CKAN `organization_list` in full (127 organisations): no organisation exists for Multicultural Affairs, SAMEAC (South Australian Multicultural and Ethnic Affairs Commission) or ethnic affairs of any kind. `package_search` for "multicultural", "migration", "settlement", "humanitarian" and "ethnic" surfaces only three one-off historical releases from the department's predecessor unit (Dept of Human Services / Communities and Social Inclusion): a single 2012 SASP Target 5 multiculturalism indicator (frozen since 2015, re-verified this run as CC BY 4.0 and genuinely privacy-safe but too sparse/stale to be useful — one whole-of-state percentage per year, no geographic or demographic breakdown), and two one-off FY2013-14/2014-15 multicultural grants CSVs, one explicitly marked `data_state: inactive` in CKAN. SA's current Multicultural Affairs unit (within the Department of the Premier and Cabinet) has never published a successor. Pivoted to the national fallback matching this domain's own "humanitarian and skilled migration settlement statistics" framing: the old `settlementreporting.homeaffairs.gov.au` interactive Settlement Reporting Facility has been formally decommissioned per DSS's own notice; its replacement, the Department of Home Affairs' `settlement-reports` CKAN package on data.gov.au, publishes genuine downloadable Excel extracts under CC BY 3.0 AU (verified independently via the CKAN API, the landing page's licence module, and resolution of the licence URL to the genuine CC BY 3.0 AU deed). Downloaded and mirrored the "last 10 Calendar Years by Migration Streams" edition (2016-2025, 563,921 bytes, confirmed genuine XLSX), covering Humanitarian, Family and Skilled streams each broken down by 13 dimensions (Visa Subclass, Country of Birth, Ethnicity, Religion, Language, Gender, Year/Financial-Year of Settlement, Age, Age Band, Marital Status, English Proficiency, Local Government Area) against all 8 states/territories, including a full South Australia LGA breakdown (Salisbury, Playford, Port Adelaide Enfield top the Humanitarian-stream list). Privacy independently verified two ways: the Department's own stated small-cell suppression policy (values under 5 masked as `<5`) was confirmed actually applied — a full programmatic scan of the 40,095-row processed CSV found zero unmasked values between 1 and 4 anywhere, including in the highest-risk LGA × ethnicity cells. Wrote `convert.py` to flatten the workbook's 39 stacked per-dimension crosstabs (13 dimensions × 3 streams) into one tidy long-format CSV, joining in a plain-English description for the one dimension (Visa Subclass) published as a bare numeric code. Added as `datasets/au-migrant-settlement-statistics/`. 3 domains remain unchecked after this run (Crime and offence statistics; Mental health services; Domestic, family and sexual violence statistics) — at the "fewer than 3" threshold boundary (not below it), so no new candidate domains were proposed this run; expect the next run to need 2-3 more added.

- 2026-07-07 (scheduled run): checked **Sport and recreation (Office for Recreation, Sport and Racing — participation statistics, facility and grant funding)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so no reordering of the candidate list was needed; proceeded top-of-list as usual. Confirmed the `office-for-recreation-sport-and-racing` organisation exists on `data.sa.gov.au` via `organization_list`, then pulled its full dataset list via `organization_show?id=office-for-recreation-sport-and-racing&include_datasets=true`: only 2 packages exist, both CC BY 4.0 — `sports-and-activity-providers-registered-for-sport-vouchers` ("Club Venues in the Sports Voucher Program") and `sports-vouchers-issued` ("Sports Vouchers Claimed"). Both packages' own `notes` field states verbatim that ORSR "are now separate from the Department for Infrastructure and Transport and they now have their own public facing page available with the most up-to-date data" at `sportsvouchers.powerappsportals.com/voucher-stats/` — i.e. ORSR itself flags this CKAN data as no longer maintained. Confirmed this isn't just stale CKAN metadata: a direct `curl -I` on the actual CSV resource files showed HTTP `Last-Modified: Mon, 21 Aug 2017` headers, so the underlying data is genuinely frozen at 2017 (the 2025-03-26 `metadata_modified` timestamp was just a housekeeping edit, not a data refresh). Followed ORSR's own "most up-to-date data" link: it resolves (HTTP 200) to a Microsoft Power Apps portal page with an embedded Power BI iframe dashboard — an interactive visualisation, not a downloadable open dataset, API, or file with a stable licence/URL, so not usable as a repo source. Ran broader `package_search` queries (q=recreation, q=sport, q=facility+grant) across all of `data.sa.gov.au`: no other ORSR facility-funding, grant-funding, or participation-statistics dataset exists anywhere on the portal; all other hits are unrelated local-council park/trail/BBQ-table datasets (Adelaide City Council, Port Adelaide Enfield, etc.), not ORSR-published and not statewide. Attempted to check ORSR's own site (`ors.sa.gov.au`) directly for a self-hosted open-data/grants page; it returned HTTP 403 (bot-blocked), but this doesn't change the conclusion since the authoritative CKAN catalogue had already been exhaustively checked and the org's own dataset descriptions confirm no current open dataset is published there. Conclusion: this domain produces no genuine, current, open dataset — the only ORSR-published records are stale by 9 years and explicitly superseded by a non-open interactive dashboard, matching the same stale/superseded pattern already documented for Consumer and Business Services, SA Electoral Commission and SafeWork SA. Nothing added. 4 domains remain unchecked after this run (Multicultural affairs and migration settlement; Crime and offence statistics; Mental health services; Domestic, family and sexual violence statistics) — at or above the "fewer than 3" threshold, so no new candidate domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Veterans affairs and ex-service support (Office for Veterans' Support SA / Dept of Veterans' Affairs — veteran population statistics and support service usage)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Checked the `defence-sa` organisation on `data.sa.gov.au` directly via the CKAN API: only 10 datasets exist, every one the same mandatory whole-of-government annual-report disclosure template documented repeatedly elsewhere in this run log (WHS, whistleblowers, public complaints, fraud detection, executives, contractors, consultants) — nothing about veteran population or support-service usage. A broader `data.sa.gov.au` keyword search for "veteran" (318 hits) surfaced only historical/archival material (WWI oral history interviews, WWI-era photograph collections) plus SA Health's "Veterans' Health Advisory Council" annual report data (2017-18/2018-19 governance disclosure, not population statistics); **Office for Veterans' Support SA** has no dataset presence on the portal at all. Pivoted to the national fallback: the Department of Veterans' Affairs' own **"Local Government Area (LGA) profile (Veteran profile by LGA)"**, found both via web search and confirmed directly on `data.sa.gov.au`'s federated CKAN catalogue (`package_search?q=veteran`), which harvests from `data.gov.au` — cross-checked the licence two ways (the `package_show` API response and a direct fetch of the live `data.gov.au` dataset page) and both independently show `license_id: cc-by-4.0` / "Creative Commons Attribution 4.0 International". This is worth flagging as a genuine discrepancy, not silently resolved: `dva.gov.au`'s own site-wide copyright footer (fetched directly from `/copyright`) states a much more restrictive personal/non-commercial/unaltered-form-only licence. Treated the dataset-level CC BY 4.0 catalogue declaration — which DVA itself submitted to the national open data portal specifically for this statistical release — as authoritative, consistent with how this repo already treats other agencies with the same two-tier setup (e.g. `au-work-health-safety-jurisdictional-comparison`, `au-tourism-satellite-account`); documented the reasoning and both sources in the new README rather than picking one silently. The catalogue's linked resource was itself just an HTML pointer to DVA's general statistics page, so the actual current workbook was located directly on the live DVA site instead (`lgas_dec2025.xlsx`/`.pdf`, "as at 2 January 2026", published March 2026 — the catalogue's own stale metadata still referenced an earlier April-June 2025 reference period) and downloaded directly; `dva.gov.au` was reachable over plain HTTPS this run, no `fetch.sh` fallback needed. The workbook has one sheet per state/territory (534 LGA rows total, 69 for `SA`), each with net DVA client, veteran/dependant, Disability Compensation Payment, War Widow, Service Pension, SS Age Pension and Gold/White Card holder counts; small cells are suppressed by DVA itself as the literal text `Under 5`, preserved as-is rather than estimated. Wrote `convert.py` to stack all 8 sheets into one tidy CSV with an added `state`/`state_abbrev` column (534 rows) — no figure recalculated, reinterpreted or had its suppression marker altered; spot-checked SA's Onkaparinga row (2,654 net clients, 897 Gold Card holders) against the source workbook cell-for-cell. Added **`datasets/au-veteran-population-by-lga/`** (62KB raw, 40KB processed) and a DVA CC BY 4.0 attribution line (noting the licence discrepancy) to `LICENSE-DATA.md`. Privacy check: every field is either a place name (LGA) or an aggregate count with DVA's own small-cell suppression already applied — no individual named or identifiable anywhere. Only 2 domains remained unchecked after this run (Sport and recreation; Multicultural affairs and migration settlement) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Crime and offence statistics (SAPOL recorded crime by offence type/LGA, directly relevant to Rin's own analytics remit), Mental health services (SA Health/Mental Health Commission of SA), and Domestic, family and sexual violence statistics (SA Attorney-General's Department/Office for Women).

Newest entry first.

- 2026-07-07 (scheduled run): checked **Child protection and family services (Department for Child Protection: notifications, investigations, out-of-home care and reunification statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Checked the `department-for-child-protection` organisation on `data.sa.gov.au` directly via the CKAN API: only 8 datasets exist, every one the same mandatory whole-of-government annual-report disclosure template already documented repeatedly elsewhere in this run log (public complaints, WHS/return-to-work performance, public interest disclosures, fraud, executive employment, contractors, consultants, whistleblowers' disclosure) — no notification, investigation, out-of-home-care or reunification statistics series. Broader `data.sa.gov.au` searches ("child protection" 684 hits, "out-of-home care" 52, "notifications children" 912) surfaced nothing further from any SA agency. Checked the traditional national fallback, AIHW's "Child Protection Australia" report, directly: its 2023-24 edition (current as of this run) publishes narrative PDF only across every report sub-page checked (about, child-protection-system-in-australia, indicators, technical-notes, safety-of-children-in-care, pathways-from-out-of-home-care, aboriginal-and-torres-strait-islander-children) — no separate downloadable data-tables workbook is linked anywhere in the report; the underlying figures live only in an interactive dashboard (`viz.aihw.gov.au`, itself backed by a JS-rendered Kentico CMS "downloadable resources" widget with no directly guessable API endpoint), so it wasn't usable as a mirrorable dataset this run — a genuine, disclosed limitation of this AIHW edition, not a fabricated gap. Pivoted to the Productivity Commission's **Report on Government Services 2026**, Part F Section 16 ("Child protection services"), whose data-tables workbook was found directly on the chapter's live page (`pc.gov.au/ongoing/report-on-government-services/community-services/child-protection/`) and downloaded from `assets.pc.gov.au` (731,344 bytes, confirmed via `file` as "Microsoft Excel 2007+") — reachable without a `fetch.sh` fallback. Licence confirmed as CC BY 4.0 directly from the Commission's own copyright page (same licence wording pattern as `datasets/au-work-health-safety-jurisdictional-comparison/`). This workbook is substantially more complex than any multi-table source handled in this repo so far: 44 sheets (`Table 16A.1`–`16A.44`), each a deeply indented row-label hierarchy (up to 6 levels: year > category > sub-category > Indigenous-status breakdown) rather than a flat wide grid, with the "Unit" header consistently at column L (index 11) across every sheet regardless of hierarchy depth — verified this programmatically across all 44 sheets before writing the parser, rather than assuming it from a sample. Wrote a generic `convert.py` that walks each sheet tracking the active row-label at each indentation depth in a stack, and emits one record per non-empty value (with the full label path, unit, column-header "breakdown" value, and figure) whenever a row carries both a unit and data — handling, without special-casing, both jurisdiction-column tables (33 of 44) and the nine per-jurisdiction activity-group unit-cost tables (16A.27–16A.35, each already scoped to one state/territory named in its own title, with financial year as the column header instead) plus 16A.11 (indicator-name column headers) and 16A.39 (national-total-only, also year-columned). Spot-checked converted values against the source workbook directly after conversion: SA's Aboriginal and Torres Strait Islander children in notifications, 2024-25 (Table 16A.1) — `5536`, exact match; SA's "Population at 31 December, 2024, All children" (Table 16A.44) — `378636`, exact match; both confirmed against the raw workbook cells, not just the parser's own output. Documented plainly in the new README that RoGS has no exit-*destination* breakdown (reunification vs guardianship vs adoption) — Table 16A.43 (exits to a permanency arrangement, and whether the child returned to care within 12 months) is the closest available proxy, not a full substitute for "reunification statistics" as named in the domain description. Added **`datasets/au-child-protection-services/`** (716KB raw, 15MB processed across 44 per-table CSVs + a combined long file (40,816 rows) + a table index) and a Productivity Commission CC BY 4.0 attribution line to `LICENSE-DATA.md`. Privacy check: every field across all 44 tables is a jurisdiction-level (or jurisdiction-by-year, for the unit-cost tables) aggregate count, rate, percentage, ratio or dollar amount — no child, case or family-level data of any kind. Root `README.md` updated with a new bullet in the "What's here" list. 3 domains remain unchecked after this run (Veterans affairs and ex-service support; Sport and recreation; Multicultural affairs and migration settlement) — at the "fewer than 3" threshold exactly, so no new domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Arts and cultural institutions (Arts South Australia / State Library, Art Gallery of SA, SA Museum, History Trust of SA — visitation and attendance statistics)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Checked every plausible SA organisation directly via the `data.sa.gov.au` CKAN `organization_show` API rather than relying on web search alone: Art Gallery of South Australia (7 datasets, all annual-report PDFs), South Australian Museum (10 datasets: WHS/whistleblowers/fraud/executive-employment/contractors/complaints/consultants disclosures, one annual-report PDF, two collection catalogues), State Library of South Australia (10 datasets: historical photo/map collections plus "Libraries Board of South Australia Annual Report data"), History Trust of SA, Carrick Hill, Adelaide Festival Centre Trust and Adelaide Festival Corporation — all following the same standard mandatory-disclosure pattern. Downloaded and inspected the one dataset whose name suggested attendance figures — "Libraries Board of South Australia Annual Report data" (state-library-of-south-australia org, CC BY 4.0) — directly (both its 2022-23 and 2023-24 CSV resources): confirmed it contains only the standard governance disclosures (fraud instances, complaints by category, consultant/contractor spend, executive employment counts), not visitor or loan figures, so it doesn't fit this domain despite the promising title. No genuine SA-published visitation/attendance dataset exists as structured open data for any of these institutions — actual attendance numbers exist only inside narrative PDF annual reports, not as an extractable table. Fell back to a national source per this repo's standing exception: the ABS's **"Cultural and Creative Activities, 2021-22"** survey (first release of this survey cycle, reference period 2021-22, "Next release: Unknown") publishes Table 14, "Attendance at cultural venues and events, by state or territory" — libraries/archives, art galleries, museums, cinemas and performing-arts sub-types, each with an explicit SA column alongside every other state/territory, across four measure blocks (estimate, attendance rate, RSE, margin of error). Licence confirmed as CC BY 4.0 directly via the ABS's own site-wide copyright statement (same evidentiary basis as `au-prisoners-in-australia`); checked tables 15-21 in the same workbook too but found they cut the same attendance data by region-type/age/sex/family/labour-force/education/income at the **national** level only, with no per-state column, so they were left out of scope rather than force-fitted into a state-shaped schema. Downloaded the workbook directly from `abs.gov.au` (116,424 bytes, confirmed via `file` as "Microsoft Excel 2007+") — reachable without a `fetch.sh` fallback. Reshaped Table 14's four stacked measure blocks into one tidy long CSV (432 rows: 9 states/territories × 12 venue-type rows × 4 measures) via `convert.py`; spot-checked converted values against the source workbook directly (SA Museums attendance rate `19.1`, SA "Total attending at least one venue or event" attendance rate `67.8`, SA Libraries or archives estimate `358.9` '000 — all exact matches). Added **`datasets/au-cultural-venue-attendance/`** (114KB raw, 22KB processed) and an ABS CC BY 4.0 attribution line to `LICENSE-DATA.md`. Privacy check: every field is a state/territory-level population estimate, percentage or survey-precision statistic (persons aged 15+, by state, by venue/event type) — no individual-level data of any kind. 4 domains remain unchecked after this run (Child protection and family services; Veterans affairs and ex-service support; Sport and recreation; Multicultural affairs and migration settlement) — at or above the "fewer than 3" threshold, so no new domains were proposed this run.

- 2026-07-07 (scheduled run): checked **Aboriginal affairs and native title (Department for Aboriginal Affairs and Reconciliation / South Australian Native Title Services — native title determinations and Indigenous Land Use Agreements)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Delegated initial research to a sub-agent (search data.sa.gov.au for SA-native candidates, plus the National Native Title Tribunal for a national fallback), then independently re-verified every claim myself rather than trusting the sub-agent's report alone: confirmed via direct API calls that no genuine SA-specific open dataset exists (SANTS and the SA Attorney-General's Aboriginal Affairs and Reconciliation area publish no downloadable register; the one SA-portal hit, "SASP Target 44 – Aboriginal Land – Native Title", is a stale 2004-2014 policy-KPI spreadsheet, not a determination register), and confirmed the NNTT's own national spatial data (Native Title Determinations + Indigenous Land Use Agreements layers) is genuinely CC BY 4.0 two independent ways — the DCAT metadata feed's `license` field, and the ArcGIS item's own `licenseInfo` text, fetched directly (*"All material presented on this page is provided under a Creative Commons Attribution 4.0 International licence, with the exception of: 1) the Commonwealth Coat of Arms 2) this organisation's logo 3) content supplied by third parties"* — none of which apply here). Confirmed record counts directly against the live FeatureServer (`returnCountOnly`): 678 determinations and 1,537 ILUAs nationwide, 44 and 130 respectively for `Jurisdiction='SA'`. Fetched both layers directly via paginated FeatureServer queries with `returnCentroid=true` (rather than the source's own asynchronous CSV-export tool, which didn't complete for the larger ILUA layer within this run, or full polygon geometry, which would have added tens of megabytes for boundary data this repository's tabular use doesn't need — a footprint decision consistent with `sa-heritage-places`/`sa-mineral-tenements`/`sa-native-vegetation-floristic-areas`). This domain surfaced a genuine, non-trivial privacy question this repo hadn't hit in quite this form before: the ILUA layer's `Applicant` field is a free-text party-name field that, for a large share of agreements, names the specific individual(s) authorised under s.61 of the Native Title Act to act as the registered native title claimant for their claim group — not incidental data, but real named individuals in row-level data. Applying this repository's standing "no individual-identifying fields in row-level data" rule (same check as `sa-expiation-notices`/`sa-mineral-tenements`), wrote a keyword-and-pattern classifier in `convert.py` that keeps government/corporate/RNTBC applicant values as-is and replaces bare personal-name segments with a fixed marker (`[individual applicant(s) - name(s) withheld for privacy]`), tested and refined against several tricky mixed government-plus-individual strings (e.g. a NSW minister named alongside private co-claimants in the same cell) until it correctly separated the two rather than either over-trusting or wholesale-blanking the field; 401 of 1,537 ILUA rows nationally (6 of 130 in the SA subset) had at least one name redacted this way. Documented two remaining nuances plainly in the new README rather than silently smoothing them over: office-holders (ministers, Attorneys-General) named alongside their title are generally left unredacted since that's closer to naming a public office than a private individual, though the heuristic doesn't always catch this consistently across differently-punctuated source strings (14 rows nationally); and the same heuristic occasionally over-redacts non-name text it mistakes for a name (one row's "...and Urban Planning..."/"...and Heritage..." portfolio-title fragments) — a cosmetic accuracy limitation, not a privacy failure, since the bias throughout is toward redacting when ambiguous rather than ever leaving a real name exposed. The determinations layer's `federal_court_case_name` field (e.g. "De Rose v State of South Australia") was deliberately left unredacted despite naming individuals, since it's the Federal Court proceeding's own formal citation — the same category of already-public disclosure as any published judgment's title — not an incidental party-name field; `rntbc_name` was checked across all 297 distinct values and confirmed to always be a corporate entity, never an individual, consistent with the Native Title Act requiring title to vest in an incorporated body. Added **`datasets/au-native-title-determinations/`** (1.6MB raw, 712KB processed) and an NNTT CC BY 4.0 attribution line to `LICENSE-DATA.md`. Only 2 domains remained unchecked after this run (Arts and cultural institutions; Child protection and family services) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Veterans affairs and ex-service support (Office for Veterans' Support SA), Sport and recreation (Office for Recreation, Sport and Racing), and Multicultural affairs and migration settlement (SA multicultural affairs/humanitarian settlement statistics).
- 2026-07-07 (scheduled run): checked **Native vegetation, land clearance and biodiversity (Department for Environment and Water — distinct from the conservation-reserve boundary/national-parks data already flagged as unexplored in the EPA air quality run)**. Searched `data.sa.gov.au` for native vegetation clearance/register datasets first (web search plus a direct check of the DEW clearance-application-register page), but found no genuine downloadable clearance-register dataset — the only clearance-adjacent CKAN package (`native-vegetation-council-annual-report-data`) is mostly PDF annual reports with just one usable CSV year (2017-18), too thin to be worth adding on its own. Pivoted to **"Native Vegetation Floristic Areas - NVIS - Statewide"** (DEW, `data.sa.gov.au` CKAN package `native-vegetation-floristic-areas-nvis-statewide`), which covers the biodiversity/vegetation-mapping half of the domain: statewide extant native vegetation extent mapped as polygon features, classified by floristic/structural vegetation type against the National Vegetation Information System (NVIS) Major Vegetation Group/Subgroup scheme. Licence confirmed as **Creative Commons Attribution 4.0** via two independent sources — the CKAN `package_show` API (`license_id: "cc-by"`, `license_title: "Creative Commons Attribution"`, `isopen: true`) and a `CC_BY.txt` file bundled inside the CSV resource zip itself, stating verbatim "Information contained in this file is licensed under a Creative Commons By Attribution 4.0 Australia Licence" — no 3.0-vs-4.0 discrepancy this time. All five published resources were tested live and confirmed reachable: three statewide polygon spatial archives (Shapefile ~1.80GB, KMZ ~1.69GB, GeoJSON ~1.90GB, all served via a 301 redirect from `waterconnect.sa.gov.au` to `apps.waterconnect.sa.gov.au`, HTTP 200 with matching Content-Length on HEAD requests) plus a small CSV vegetation-type lookup table (100,087 bytes, served directly from `data.sa.gov.au`) and an HTML metadata report via `location.sa.gov.au`. Given only ~17GB free disk in this environment, the three multi-gigabyte polygon archives were deliberately **not downloaded** — this is a footprint decision, not a reachability failure, and is documented plainly in the new dataset's README (Access method / Known limitations) alongside a `raw/fetch.sh` script (pattern-matched to `sa-expiation-notices/fetch.sh`) so anyone with more disk/bandwidth can pull them from the same tested URLs. The CSV lookup table (`VEG_SAVegetation_LUT.csv`, 1,046 data rows keyed by `VEG_ID`/`SA_VEG_ID`, covering structural formation, dominant species composition, floristic description, and NVIS group codes) was downloaded, verified byte-for-byte against its Content-Length, and mirrored as-is in `raw/` alongside the bundled `CC_BY.txt` licence file and the fetched metadata HTML. Added **`datasets/sa-native-vegetation-floristic-areas/`**, following the `sa-mineral-tenements`/`sa-heritage-places` README template; `convert.py` standardises the LUT's column names to lower_snake_case and cleans `veg_id` out of the source's floating-point string format (e.g. `"6.000000000000000"` → `"6"`) with no other values altered. Privacy check: all 15 fields in the processed CSV are botanical/vegetation classification data (species composition, structural formation, environmental description, NVIS group names) — no individual- or organisation-identifying field of any kind, no redaction needed. Root `README.md` updated with a new bullet in the "What's here" list. Remaining unchecked candidates after this run: Aboriginal affairs and native title, Arts and cultural institutions, Child protection and family services — exactly three, so per the standing rule no new domains were appended this run.

- 2026-07-07 (scheduled run): checked **Tourism and visitor statistics (South Australian Tourism Commission: visitor numbers, expenditure, regional breakdowns)**. Career context re-checked directly: the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` still carries a file timestamp of 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track is unchanged and no reordering of the candidate list was needed; proceeded top-of-list as usual. Ran a two-stage research-then-independent-verify workflow (one agent to find candidates, a second to independently re-fetch every licence page and download URL itself rather than trust the first pass), consistent with how this repo has handled licensing-sensitive domains before. Found two genuinely open candidates and added both, since they're complementary rather than overlapping: **SA Tourism Commission's "Tourism Visitor Statistics"** (`data.sa.gov.au`, CKAN `license_id: cc-by`, confirmed live via `package_show`) is the only tourism-tagged dataset published under the SA Tourism Commission organisation (confirmed via a `q=visitor` catalogue search — the only other hit was ATDW's business-directory API, which has no stated licence and was excluded) — genuinely SA-published, expenditure/visitor/jobs figures with a South Australia + Regional South Australia split, but its single XLSX resource has not been refreshed since its `DCTERMS.Published` date of 2017-07-10 (most recent quarter: "Year ending March 2017"), even though SATC's own `tourism.sa.gov.au/insights` pages publish far more current (to March 2026) figures — as PDF narrative only, no downloadable structured file and no explicit per-page licence, so not a substitute. To cover the currency gap without force-fitting a stale dataset as if it were current, also added **Tourism Research Australia's State Tourism Satellite Account 2023-24** (`tra.gov.au`, an Austrade/Commonwealth publication) as a second, national dataset — its CC BY 4.0 licence is asserted via Austrade's site-wide disclaimer (independently re-fetched and confirmed to explicitly cover `tra.gov.au`, with no non-commercial/no-redistribution carve-out) rather than a per-dataset CKAN badge, a weaker but still genuine and explicit open-licence grant; all 13 of its tables carry South Australia as its own column, not folded into a national total, refreshed annually through FY2023-24. Both raw XLSX files were re-fetched directly via `curl` (SATC: 19,757 bytes; TRA: 976,379 bytes, plus a 1,038,225-byte PDF fact sheet) and confirmed byte-identical in size to what the verification pass reported — `data.sa.gov.au` and `tra.gov.au` were both directly reachable this run, no `fetch.sh` fallback needed for either. Processed both into tidy long-format CSVs rather than leaving the XLSX as the only usable form: the SATC workbook's six sheets were merged into five metric tables plus one combined long file (`datasets/sa-tourism-visitor-statistics/convert.py`), normalising each sheet's "Year ending \<Month\> \<Year\>" label into an ISO end-of-quarter date; the TRA workbook's 13 tables (a mix of multi-year state-by-year grids and single-year state-by-industry breakdowns, some with paired level+share sub-columns) were parsed by a general row-classifier (`datasets/au-tourism-satellite-account/convert.py`) into one CSV per table plus a combined long file, a table index and a footnote file — every numeric value spot-checked against the source workbook directly (e.g. SA GVA 2016-17 `2574.0678`, SA "Tourism – total" filled-jobs share 2023-24 `7.002483163952491`, both exact matches) after two classifier bugs surfaced and were fixed during testing: table 7's `'000`/`%` sub-column measure row wasn't being recognised (measure tokens were hardcoded to `$m`/`%` only), and single-line labels like "Tourism characteristic industries" were wrongly overwriting the `category` field instead of being treated as a section grouping — both fixed and re-verified against source figures before finalising. Added **`datasets/sa-tourism-visitor-statistics/`** (20KB raw / 100KB processed) and **`datasets/au-tourism-satellite-account/`** (1.9MB raw / 1.0MB processed) — a combined ~3MB footprint — plus SA Tourism Commission and Tourism Research Australia/Austrade attribution lines in `LICENSE-DATA.md`. Privacy check: every field in both datasets is a statewide, regional or industry-level aggregate (expenditure totals, visitor/night counts, job counts, GVA/GSP/output/consumption figures, percentage shares) — no individual, business or establishment-level data in either source. 4 domains remain unchecked after this run (Native vegetation/land clearance/biodiversity; Aboriginal affairs and native title; Arts and cultural institutions; Child protection and family services) — at or above the "fewer than 3" threshold, so no new domains proposed this run.

- 2026-07-07 (scheduled run): checked **Elections and electoral statistics (SA Electoral Commission: enrolment figures, election/referendum results, turnout by district)**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` — file timestamp still 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track and 12-month goal are unchanged and no reordering was needed; proceeded top-of-list as usual. Ran a research-and-adversarial-verify pass rather than a single linear search, given how much this domain turns on getting the licensing question right: one pass searched `data.sa.gov.au` CKAN directly (confirmed reachable; `organization_list` of 106 orgs contains no Electoral Commission of South Australia entry at all — `organization_show` for every plausible ECSA slug returned `Not Found`), and a second pass fetched `ecsa.sa.gov.au` and the AEC directly. ECSA's own site genuinely publishes current, aggregate, district-level enrolment data — monthly CSVs (most recent snapshot 30 April 2026, confirmed live: 1,322,042 total SA electors across 47 House of Assembly districts) — but a direct fetch of its only footer-linked legal page, `https://ecsa.sa.gov.au/disclaimer`, confirmed (grepped for "copyright"/"licence"/"creative commons" — zero matches) that it's a pure liability disclaimer with no reuse grant anywhere on the site; a second, independent adversarial-verification pass re-fetched the same page itself (rather than trusting the first pass) and reproduced the identical finding, including a verbatim typo on the live page ("so not represent"). This fails the same "no stated open licence" bar that already excluded ReturnToWorkSA and SafeWork SA for the Workplace Health and Safety domain — excluded on licensing grounds, not force-fitted in. The two SA-specific CKAN datasets that do touch this topic are off-target: `state-electorates` (DHUD, CC BY 4.0) is district boundary geometry only, and `election-statistics` (City of Adelaide, CC BY 4.0) covers only City of Adelaide council-ward voter counts 2007–2014 with ECSA cited merely as a source, not the publisher. Fell back to the AEC per this repo's standing exception (national source acceptable with a genuine state breakdown, not a buried total): the AEC's own site-wide copyright page, fetched and independently re-verified, confirms CC BY 4.0 International (*"the AEC has applied the Creative Commons Attribution 4.0 International Licence... to all material on this website"*), while the data.gov.au CKAN record for the same dataset separately states CC BY 3.0 AU — both attribution-only, a discrepancy documented in the new README rather than silently picking one. Found the actual downloadable CSVs (not just the CKAN landing page, which only points to a "Virtual Tally Room" link) by navigating the AEC Tally Room directly to the 2025 federal election's CSV downloads menu (event ID `31496`, confirmed as the most recent event via the results-portal homepage) and fetching 11 files nationwide: enrolment, turnout, informal votes, two-party-preferred, nominations, members-elected and non-classic-divisions by division (150 divisions, 10 in SA), plus candidate-level first preferences (1,276 rows, 87 in SA), a party registry lookup and a seats-changed-hands list. Wrote `convert.py` to merge the seven division-level files (joined on `DivisionID`) into one tidy `data/au-federal-election-2025-division-summary.csv`, flagging the one SA division (Mayo, held by Centre Alliance) where the AEC's own non-classic-divisions file shows the final two-candidate-preferred contest wasn't actually ALP-vs-Coalition, and flagging the AEC's own synthetic `CandidateID 999` "Informal" placeholder row in the first-preferences file rather than silently leaving it ambiguous — no vote counts or percentages recalculated anywhere. Spot-checked all 10 SA divisions and their elected members (Georganas/Adelaide, Pasin/Barker, Miller-Frost/Boothby, Venning/Grey, Butler/Hindmarsh, Rishworth/Kingston, Zappia/Makin, Sharkie/Mayo, Burnell/Spence, Clutterham/Sturt) against the AEC's own certified 2025 result — exact match. Added **`datasets/au-federal-election-results/`** (264KB raw, 172KB processed — a genuinely modest footprint) and an AEC CC BY 4.0 attribution line to `LICENSE-DATA.md`. Privacy check: confirmed directly that neither the SA nor Commonwealth electoral roll (individual elector names/addresses) is open data at all — ECSA states the roll "is not available online", and Commonwealth roll disclosure/commercial use is prohibited under s.91B of the *Commonwealth Electoral Act 1918* — nothing from either roll is included; every field here is a division/candidate-level aggregate or already-public nomination fact. SA-specific ECSA election data remains a real, undisclosed gap (worth re-checking if ECSA states an explicit licence in future). Only 2 domains remained unchecked after this run (Tourism and visitor statistics; Native vegetation/land clearance/biodiversity) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Aboriginal affairs and native title (Dept for Aboriginal Affairs and Reconciliation / SA Native Title Services), Arts and cultural institutions (State Library, Art Gallery of SA, SA Museum, History Trust of SA visitation statistics), and Child protection and family services (Dept for Child Protection notifications/out-of-home-care statistics).

- 2026-07-07 (scheduled run): checked **Mining and resources tenements (PIRSA/Dept for Energy and Mining: mineral exploration licences and tenement register)**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` — file timestamp still 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track and 12-month goal are unchanged and no reordering was needed; proceeded top-of-list as usual. Searched `data.sa.gov.au` CKAN for `mining tenement` and `mineral exploration licence`; found and confirmed **Mineral Tenements** (CKAN package `mineral-exploration-production-tenements`, Department for Energy and Mining/SARIG) directly via `package_show` — `license_id: cc-by`/`license_url: .../by/4.0`, `update_freq: daily`. Unlike most datasets in this repo, CKAN's own resource list offers no static file download at all — only a Map Viewer link, an HTML metadata page and live WMS/WFS endpoints — so fetched the live `WFS_Capabilities` document directly to enumerate all 16 available GeoServer layers before picking which to mirror, rather than guessing from the CKAN listing. Confirmed the service is genuinely live and current (not a stale feed) by fetching real feature samples: the exploration-licence layer's newest record was granted January 2025, six months before this run. Mirrored the 13 current/active layers (granted tenements: Exploration Licence, Mining Lease, the pre-2021 Mineral/Extractive Mineral Lease classes, Retention Lease, Mineral Claim, Miscellaneous Purposes Licence, the Olympic Dam Special Mining Lease, and Private Mines — 2,585 records; pending applications — 172; released/relinquished exploration areas — 156) as GeoJSON straight from the WFS `GetFeature` endpoint (`services.sarig.sa.gov.au`), leaving the three historic/non-active layers (~10,900 further superseded records) live-queryable-only rather than mirrored, a footprint decision consistent with how this repo has handled other large statewide GIS layers (e.g. `sa-heritage-places`' polygon layer). Wrote `redact_and_convert.py` to merge the 13 layers into three tidy CSVs (`data/sa-mineral-tenements-granted.csv`, `-applications.csv`, `-released-areas.csv`) with a `*_category` column identifying the source layer, per-layer field names harmonised into common columns, tenement/application type codes (`EL`, `ML`, `EML`, `RL`, `MC`, `MPL`, `SML`, `PM`/`PMA`, `ELA`, `MCA`, `MPLA`) decoded via SA Dept for Energy and Mining's own "Types of mining tenure" page, and polygon geometry reduced to a bounding-box-centre longitude/latitude pair. **Privacy check surfaced a genuine first for this repository**: unlike every other source here, this register legitimately mixes company/organisation tenement holders with **named individual people** — under the Mining Act 1971, a Private Mine or a small Mineral Lease/Extractive Mineral Lease/Mineral Claim can be held directly by an individual landholder, not only a company. A keyword-based scan across all 2,913 granted/application/release rows found 266 holder/applicant values naming a specific person (or a deceased person's estate), concentrated in `private_mines` (95), `extractive_mineral_leases` (117) and `mineral_leases` (30). Applying this repo's standing "no individual-identifying fields in row-level data" rule (the same check used for `sa-expiation-notices`), every non-corporate-looking holder/applicant value was replaced with a fixed marker (`[individual holder - name withheld for privacy]`) — two ambiguous values (`DEM - Regulation and Compliance`, `Wangka Wangka`) were manually confirmed as a government unit and a native title/community group respectively, not a person, and left as-is. Because the redaction had to happen before the source data touched disk at all, **`raw/` for this dataset is not a byte-for-byte WFS mirror**, the one deliberate exception to this repo's usual "raw is untouched" convention — disclosed plainly in the new README rather than silently deviating from it; the un-redacted register remains separately, fully publicly queryable (no login) at the live SARIG endpoint itself, so nothing has been placed behind a barrier that wasn't already there. Added **`datasets/sa-mineral-tenements/`** and a DEM CC BY 4.0 attribution line to `LICENSE-DATA.md`. A genuine open SA mining-production-volume/royalty dataset was not searched for this run (budget was spent confirming and processing the tenure register and resolving the redaction question) — left as a documented gap. 3 domains remain unchecked after this run (Elections and electoral statistics; Tourism and visitor statistics; Native vegetation/land clearance/biodiversity) — at, not below, the "fewer than 3" threshold, so no new domains proposed this run.

- 2026-07-07 (scheduled run): checked **Heritage and cultural places (SA Heritage Register: state and local heritage-listed places, Dept for Environment and Water)**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` — file timestamp is still 25 May 2026, unchanged from every prior run's check, so Rin's ASO7 SAPOL Senior Data Analyst track and 12-month goal are unchanged and no reordering was needed; proceeded top-of-list as usual. Searched `data.sa.gov.au` and the wider web for "SA Heritage Register", "heritage places dataset" and "Department for Environment and Water heritage". Found and confirmed **SA Heritage Places** (CKAN package `sa-heritage-places`, jointly published by the Department for Housing and Urban Development and DEW's Heritage Branch) directly via `package_show` — `license_id: cc-by`/`license_url: .../by/4.0`, `update_freq: daily`, `temporal_coverage_to: Current`, 7 resources. Downloaded and unzipped the GeoJSON archive directly (`www.dptiapps.com.au/dataportal/SAHeritagePlaces_geojson.zip`, 15,126,406 bytes, byte-for-byte match to the live `content-length`, last-modified 28 June 2026 — 9 days before this run, genuinely current) rather than trusting the CKAN listing: 24,479 point features and 23,229 polygon features, each duplicated across GDA94/GDA2020 datums, covering State Heritage Places (2,356 distinct, closely matching DEW's own published count of 2,351 confirmed State Heritage Places), Local Heritage Places (7,622 distinct) and Representative Buildings/former Contributory Items (12,300 distinct) across 83 LGAs and 832 suburbs. Found the same CKAN-vs-bundled-licence discrepancy already documented for `sa-land-division-applications` and `sa-planning-zones`: the archive's own `License.txt`, and a separately-fetched metadata report from Location SA's Location Metadata System (`location.sa.gov.au/LMS/Reports/ReportMetadata.aspx?p_no=1576`), both state **CC BY 3.0 AU**, not the CC BY 4.0 the CKAN record implies — followed the bundled/metadata text again, consistent with precedent. Read the full field dictionary directly from that metadata page (not just inferred from GeoJSON property names) and mirrored it verbatim in `raw/`. Wrote `convert.py` to read the points layer (GDA2020) directly out of the mirrored zip and produce a single tidy CSV (`data/sa-heritage-places.csv`, 24,479 rows) — no figures recalculated, only JSON properties flattened to columns plus `longitude`/`latitude` extracted from geometry, with two source codes (`shrstatuscode`, `sourceofdata`) decoded into new description columns since the source's own export doesn't populate a paired desc field for either (documented plainly, including an unresolved `DPLG`-vs-`DPTI` code/label mismatch discovered in the publisher's own dictionary). The polygon layer (23,229 features, ~39MB per datum) was deliberately not duplicated into `data/` — it's already unprocessed-but-complete inside the mirrored `raw/` zip, and the points CSV already covers the same attribute set in a far more directly analysable tabular form; a footprint decision, not a network block, consistent with how this repo has handled other large statewide GIS layers. Total new footprint: ~15MB raw zip + ~11MB processed CSV. Privacy check: every field is a property/place identifier (address, suburb, LGA, certificate-of-title/plan-parcel reference, 2006 valuation number) — no owner or occupant name, no individual-identifying field of any kind, same treatment as `sa-school-locations`/`sa-retirement-villages-register`. Added **`datasets/sa-heritage-places/`** and a CC BY 3.0 AU attribution line to `LICENSE-DATA.md` (folded into the existing DHUD entry alongside Land Division Applications and Planning and Design Code Zones). 4 domains remain unchecked after this run (Mining and resources tenements; Elections and electoral statistics; Tourism and visitor statistics; Native vegetation/land clearance/biodiversity) — above the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Workplace health and safety (WorkSafe SA: workplace injury/incident claims statistics, dangerous occurrence notifications)**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` (still last updated 25 May 2026, unchanged) — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search` responded normally). Searched `worksafe`, `workplace injury`, `safework`, `return to work`, `work health safety`, `dangerous occurrence` and `workers compensation` — every SA-native hit is one of two patterns already documented elsewhere in this repo's run log: (a) per-agency "Work Health and Safety and Return to Work Performance" annual-report disclosures (DefenceSA, DPC, DPTI, Renewal SA, Riverbank Authority, ESCOSA, SAAS, Infrastructure SA, CFS, SA Museum, South Australian Housing Trust, Windmill Theatre, History Trust of SA, etc. — the standard whole-of-government annual-report template, not scheme-wide statistics), or (b) off-topic/federated noise (Victorian CFA WorkSafe claims data, NSW WorkCover News back-issues). The Attorney-General's Department CKAN organisation (which houses SafeWork SA) was pulled in full (77 packages via `organization_show`) — none is a WHS incident/injury statistics dataset. Checked `safework.sa.gov.au` directly — it returned HTTP 403 (Cloudflare bot-challenge), the same block already documented for `corrections.sa.gov.au`. Checked `rtwsa.com` (ReturnToWorkSA) directly: its scheme-statistics page offers only an interactive Tableau dashboard (not an extractable file) plus narrative PDF annual reports/actuarial valuations; its copyright page was fetched directly and confirms standard all-rights-reserved terms (*"Any other use of the material including alteration, transmission or reproduction for commercial use is not permitted without the written permission of ReturnToWorkSA"*) — no CC BY or equivalent, excluded on licensing grounds, same treatment as ACARA My School and the Aged Care Star Ratings extract documented previously. With no genuine SA-specific open dataset available, checked the national fallback per this repo's standing rule (acceptable only with a genuine state breakdown, not a buried national total): Safe Work Australia's own site-wide copyright statement, fetched directly, confirms CC BY 4.0 (*"All material on our website is under a Creative Commons Attribution 4.0 International Licence (CC BY 4.0), except for: the Safe Work Australia logo... content supplied by third parties... publications on our website that have their own copyright permissions"* — no exception applies to tabular data). Found and downloaded two candidate detailed-data files directly from `data.safeworkaustralia.gov.au`: the National Dataset for Compensation Based Statistics (NDS) file — inspected with `openpyxl` and rejected, since its 35 tables break claims down by industry/occupation/injury-type with no jurisdiction split at all — and the **Jurisdictional Comparison detailed data file** (2023-24 edition, 703,244 bytes, confirmed via `file` as a genuine XLSX), whose own dataset page states directly *"Publicly available data? ... Yes – see Resources"* under the same CC BY 4.0 licence. Read all 32 tables directly: every one carries an explicit `SA` row alongside NSW/VIC/QLD/WA/TAS/NT/ACT/Aus Gov/Comcare/Seacare/AUST, covering serious-claim incidence/frequency rates, long-term claims, self-insured claims, claim duration, traumatic-injury and disease fatalities, mechanism of incident, industry, WHS compliance/enforcement activity (inspections, infringement/improvement/prohibition notices, enforceable undertakings, prosecutions, court fines), premiums, funding ratios and disputes, 5-6 year trends to 2023-24 (preliminary). Wrote `convert.py` to unpivot each of the 32 sheets (27 flat jurisdiction-only tables, plus 5 that additionally nest a duration/mechanism/industry category above the per-jurisdiction rows) into tidy long-format CSVs — no totals recalculated or rates re-derived, only reshaped — and spot-checked SA's Table 1.1 incidence-rate series (8.891, 9.038, 9.225, 8.530, 8.015, 7.669 for 2018-19 through 2023-24p) against the source workbook cells, exact match. Added **`datasets/au-work-health-safety-jurisdictional-comparison/`**, with `raw/` holding the untouched source workbook and `data/` holding 32 per-table CSVs plus a combined `all-tables-long.csv` (4,609 rows) and `table-index.csv`. Also added a Safe Work Australia CC BY 4.0 attribution line to `LICENSE-DATA.md`. Privacy check: every figure is a jurisdiction-level (or jurisdiction-by-category) aggregate count, rate, percentage or dollar amount — no individual, employer or claimant-level data of any kind. Only 2 domains remained unchecked after this run (Heritage and cultural places; Mining and resources tenements) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Elections and electoral statistics (SA Electoral Commission), Tourism and visitor statistics (South Australian Tourism Commission), and Native vegetation/land clearance/biodiversity (Department for Environment and Water, distinct from the conservation-reserve boundary data already flagged as unexplored in the EPA air-quality run).
- 2026-07-06 (scheduled run): checked **Energy and utilities (SA Government renewable/solar uptake and energy consumption data)**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` (still last updated 25 May 2026, unchanged) — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search` responded normally), but its search results now visibly mix in datasets harvested from other states' and the Commonwealth's own open-data portals — confirmed directly via each hit's `original_harvest_source`/`search_federation_portal` fields (e.g. a "Renewable Energy Storage" result turned out to be Victorian wind-farm cabling data, harvested via `discover.data.vic.gov.au`) — so every candidate this run was checked for whether it's genuinely SA-native before being considered, not just taken on title/search-rank. Searched `solar`, `renewable`, `energy consumption`, `rooftop solar`, `photovoltaic`, `distributed energy` and `SA Power Networks`, and pulled every dataset directly from the Department for Energy and Mining organisation (`organization_show`, 23 packages confirmed) — none cover solar/renewable/battery uptake or energy consumption; they're bioenergy-feedstock estimates, mining/petroleum/geothermal tenements and annual-report micro-datasets. The two SA-native hits naming "renewable energy" directly, "SASP Target 64 - Renewable Energy" and "SASP Target 65 - GreenPower" (Dept of the Premier and Cabinet), are single-purpose State Strategic Plan target trackers last genuinely updated 2015 despite one carrying a re-touched 2025 `metadata_modified` timestamp — the same "metadata re-touched, data not" pattern documented previously for OCPSE. No genuine current SA-specific dataset exists for either half of this domain (renewable/solar uptake or energy consumption). Fell back to a national source for the uptake half only, per this repo's standing exception (state-specific breakdown, not a buried national total): the Clean Energy Regulator's **Small-scale Renewable Energy Scheme postcode data** — confirmed genuinely current (data as at 31 May 2026, monthly updates) via the live source page, and CC BY 4.0 confirmed verbatim on the CER's own copyright page (`cer.gov.au/about-us/our-policies/copyright`), separate from the dataset page itself which carried no licence statement. Downloaded both "Installations" (6,262,448 bytes) and "Capacity" (3,700,354 bytes) workbooks directly — turned out the CER's `/document/<slug>` URLs serve the XLSX binary directly rather than an HTML landing page, so no `fetch.sh` fallback was needed. Read both workbooks directly with `openpyxl`: six equipment-category sheets (rooftop solar panels, solar water heaters, air-source heat pumps, wind, hydro, and — newly eligible from 1 July 2025 — batteries), each a wide postcode-by-month grid back to 2001. Melting the *full* Australia-wide monthly series would produce roughly 700,000 (installations) and 380,000 (capacity) rows — tens of megabytes, far larger than any file already in this repository (largest so far: 17MB) — so `convert.py` instead produces (a) a compact national `*-totals-by-postcode.csv` using only the source's own precomputed all-time "Total" column (11,118 / 5,516 rows, no recalculation), and (b) the full historic/monthly/grand-total time series filtered to SA postcodes (5000-5999) only (`sa-installations-long.csv` / `sa-capacity-long.csv`, 77,088 / 48,812 rows) — the full-Australia monthly series is reproducible on demand from the already-mirrored `raw/` workbooks (no network needed) by flipping a flag in the script, documented plainly in the README rather than silently dropped. Spot-checked postcode 5000 (Adelaide CBD) SGU-Solar all-time total = 1,355 installations, exact match against the source cell. Aggregate national-vs-SA figures pulled directly from the converted CSVs: 4,437,269 national rooftop-solar installations vs 458,189 in SA; 401,185 national battery installations vs 45,744 in SA. Privacy check: every row is a postcode-and-category-level aggregate count/capacity figure — no name, address, meter number or individual/household identifier of any kind, a coarser shape than several already-accepted datasets in this repo. Added **`datasets/au-small-scale-renewable-installations/`** and a Clean Energy Regulator CC BY 4.0 attribution line to `LICENSE-DATA.md`. Documented energy *consumption* data (as distinct from installation/uptake) as a real, undisclosed gap — not searched exhaustively this run given the 2-4 search budget, worth a dedicated future pass. 3 domains remain unchecked after this run (Workplace health and safety; Heritage and cultural places; Mining and resources tenements) — at, not below, the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Agriculture and primary industries (PIRSA: crop/livestock statistics, water licensing)**. Career context re-checked directly against the Career Path `MEMORY.md` (still last updated 25 May 2026) — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged, so proceeded top-of-list as usual, no reordering needed. Ran three parallel research passes covering this domain's two distinct halves plus a national-fallback comparison, rather than one linear search: (1) **Crop/livestock statistics** — `data.sa.gov.au` was directly reachable this run; PIRSA's own `organization_show` lists only 10 packages (mostly governance/compliance micro-datasets, not agricultural statistics), so searched by keyword instead (`agriculture`, `livestock`, `crop`, `scorecard`, `primary production`). Found and added **`datasets/sa-primary-industries-scorecard/`** — PIRSA's "South Australia Primary Industries Scorecard — Primary Production 5 Year Summary Statistics" (2016-17 to 2020-21), confirmed CC BY 4.0 three ways (CKAN `license_id`/`license_url`, the live rendered page, and an in-workbook licensing statement), statewide, 91 commodities across 7 sectors (Dairy, Field Crops, Forestry, Horticulture, Livestock, Seafood, Wine) × 5 financial years. Downloaded and inspected both the XLSX (31,856 bytes) and PDF (79,693 bytes) directly — both byte-for-byte matches to the CKAN-declared resource sizes. Found the same "metadata re-touched, data not" trap documented elsewhere in this repo: the package's `metadata_modified` has been bumped to 2025-06-25 and 2025-08-18, but both resources' own `last_modified` timestamps are frozen at 2022-08-01 and the actual data content still ends at FY2020-21 — genuinely ~4 years stale despite looking fresh. Rejected several stale/off-target alternatives found alongside it (Crop Reporting Districts — boundary geometry, not statistics; SA Farmgate Production 5-Year Summary — superseded predecessor edition; Field Crop Production Estimates and Food and Beverage Scorecard — both untouched since 2014, data ending 2011-12/2012; Pastoral Board Annual Report Data — rangeland-only, not statewide, stale since 2020-21; a data.sa.gov.au pointer to a stale 2016 ABS snapshot). Unpivoted the source's wide year-block layout into a tidy long-format CSV via `convert.py` (455 rows = 91 commodities × 5 years), spot-checked against the source (Dairy 2020-21 value_aud = 262695805, exact match) — no totals recalculated, only reshaped. (2) **Water licensing** — searched `data.sa.gov.au` across the Department for Environment and Water and PIRSA organisations for licence/allocation/extraction/irrigation datasets; every SA-specific hit (Prescribed Well Areas, Prescribed Surface Water Areas, Prescribed Watercourses, six Far North Prescribed Water Area layers) is a spatial regulatory-boundary layer showing *where* extraction is regulated, not a licence register or volume dataset showing *how much* — and all are stale (2016-2021). PIRSA's only water-adjacent dataset (Aquaculture Leases and Licences) is marine/fisheries leasing, out of scope. No genuine open SA water-licensing/allocation dataset exists on data.sa.gov.au — documented as a real, undisclosed gap (the data, if open anywhere, more likely sits on the separate WaterConnect SA portal, not crawled this run) rather than force-fitted or silently dropped. (3) **National fallback comparison** — independently checked ABS's "Value of Agricultural Commodities Produced, Australia" (2021-22 edition, CC BY 4.0, confirmed via live page and direct XLSX download: 21 SA commodity rows with real dollar values) as a candidate in case the SA source proved unusable — it's one year more current (2021-22 vs 2020-21) but far thinner (21 commodities, 1 year, vs the PIRSA dataset's 91 commodities × 5 years) and is itself stale (no 2022-23/2023-24 edition exists on abs.gov.au, both return 404). Preferred the SA-specific PIRSA dataset per this repository's standing rule to favour a genuine state source over a national one where both exist and are comparably current — documented the direct comparison in the new dataset's README rather than silently picking one. Privacy check: every row is an aggregate sector/commodity-level statistic (total volume, average price, total value) statewide — no farm, business or individual-level identifiers. Also added a PIRSA CC BY 4.0 attribution line to `LICENSE-DATA.md`. 4 domains remain unchecked after this run (Energy and utilities; Workplace health and safety; Heritage and cultural places; Mining and resources tenements) — above the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Corrections and rehabilitation**. Career context re-checked directly against the Career Path `MEMORY.md` at `/Users/rinh/Documents/Rin : Sunchuangyu Huang/Life HQ/Career Path/MEMORY.md` (still last updated 25 May 2026, unchanged) rather than trusting the project memory summary alone — Rin remains ASO7 Senior Data Analyst at SA Police on the public-sector analytics track, so proceeded top-of-list as usual, no reordering needed. Ran three research passes rather than one: (1) `data.sa.gov.au` CKAN search for `corrections`, `prisoner`, `recidivism` and `correctional services`, plus a direct `organization_show` call against the "Department for Correctional Services" org itself, which independently confirmed `package_count: 0` — the department has never published a single dataset on the SA portal. (2) Followed the only SA-portal hit that resembles this domain, "Prisoner Characteristics" (an ABS-sourced mirror filed under the `abs-sa-data` organisation, not DCS) — re-fetched its `package_show` record directly and confirmed the actual XLS resource's `last_modified` is 2016-08-29, pointing to a retired/discontinued ABS catalogue number (4517.0); a second resource (HTML explanatory notes) carries a deceptively fresh 2026-07-03 timestamp, independently confirmed to be only the portal's periodic link-check crawler re-touching that resource, not a real data update — same "metadata re-touched, data not" pattern documented previously for OCPSE. Also checked and excluded "SASP Target 19 - Repeat Offending" (single 2012 file, abandoned since 2015), "Criminal Courts", "Recorded Crime - Offenders" and "Criminal and Civil Matters" (all stale 2014-2017 and off-topic — court/police data, not corrections population/recidivism). (3) Checked `corrections.sa.gov.au` itself directly — it publishes only narrative PDF annual reports and policy documents with no extractable data tables, and blocks direct fetching via a Cloudflare bot-challenge. With no non-stale genuine SA-specific alternative found, evaluated the national fallback per this repo's standing exception rule (acceptable only if it provides a genuine SA-specific breakdown, not a buried national total): independently downloaded ABS's actual "Prisoners in Australia, 2025" XLSX directly (not just the landing page) and inspected it with `openpyxl`, confirming 21 structured data tables (Tables 15-35) each broken out by state/territory with a dedicated South Australia column — e.g. SA total prisoners = 3,416 at 30 June 2025, with further SA-specific breakdowns by sex, Indigenous status, legal status, offence type, sentence length, age, prior imprisonment and security classification. Licence verified verbatim on the live ABS site as CC BY 4.0 International (the standard exceptions — Coat of Arms, ABS logo, microdata, etc. — none of which apply here). Currency independently confirmed inside the downloaded workbook's own Contents sheet ("Released at 11:30 am (Canberra time) Thursday 11 December 2025"); the build pass further refined this to an exact next-release date (10 December 2026) via the ABS release calendar. All data is aggregate counts/rates/means by demographic category — no names or individual identifiers, matching this repo's existing accepted data shape (e.g. `sa-education-workforce`). Also noted the Productivity Commission's Report on Government Services "Justice" chapter (Table CA.4, two-year return-to-prison recidivism rate by jurisdiction including SA) as a same-licence (CC BY 4.0) complementary source for the recidivism half of this domain, for a possible future second-dataset addition rather than pursued this run. Added **`datasets/au-prisoners-in-australia/`**: the 21-table XLSX mirrored whole in `raw/` (185,197 bytes, byte-for-byte match to the confirmed download), plus a `convert.py` script unpivoting each table into tidy long-format CSVs (`data/table-15.csv` … `data/table-35.csv`, a combined `data/all-tables-long.csv` of 8,608 rows, and a `data/table-index.csv` summary) — no totals recomputed, only layout reshaping, with SA total prisoners (3,416) spot-checked to round-trip exactly against the source workbook. Also added an ABS "Prisoners in Australia" CC BY 4.0 attribution line to `LICENSE-DATA.md`. Only 2 domains remain unchecked after this run (Agriculture and primary industries; Energy and utilities) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Workplace health and safety (WorkSafe SA injury/incident claims statistics), Heritage and cultural places (SA Heritage Register of state/local heritage-listed places), and Mining and resources tenements (PIRSA/Dept for Energy and Mining mineral exploration licences and tenement register, distinct from the crop/livestock angle already in "Agriculture and primary industries").
- 2026-07-06 (scheduled run): checked **Consumer affairs and business regulation**. Career context re-checked directly against the Career Path `MEMORY.md` (still last updated 25 May 2026) rather than trusting the project memory summary alone — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged (amusingly, his own career history includes an earlier ASO4 role at CBS itself, per `[[project-career-prioritized-domains]]` — noted for interest only, not used to shape framing), so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search` responded normally, HTTP 200 confirmed directly). Searched `consumer` (2,698 results) and `business licensing` (1,662 results), then pulled every dataset owned by the Attorney-General's Department (which houses Consumer and Business Services in SA) directly via `organization:attorney-general-s-dept` (77 results) rather than trusting search relevance ranking. Found every CBS-specific candidate — **Occupational Licences** (last resource Sept 2018), **Liquor & Gaming Licences** (last resource Aug 2019), **Charity Licences** (2018), **Justice of the Peace** (2013, and its only "data" resource is a link to a live search webpage, not a downloadable file), and **Annual Report CBS** (FY2017-18 Act-by-Act complaint/licence counts, itself a narrow annual-report micro-dataset, same pattern as OCPSE/DCSI documented in earlier runs) — all CC BY 4.0-licensed but 7-8 years stale with no newer resource added since, confirmed by reading each resource's actual `last_modified` timestamp rather than the package's `metadata_modified` field (which can be re-touched without the underlying data changing, as seen previously with OCPSE). A web search independently confirmed no newer CBS public-register dataset exists on data.sa.gov.au or linked from `cbs.sa.gov.au`/`agd.sa.gov.au`'s own data pages. Considered a national fallback per this repo's standing rule: **ASIC's Business Names Dataset** (data.gov.au, genuinely current — weekly updates, latest extract 1 July 2026) — confirmed CC BY 3.0 AU directly both via CKAN `package_show` and by fetching the live dataset page (`<a href="http://creativecommons.org/licenses/by/3.0/au/">Creative Commons Attribution 3.0 Australia</a>` in the page's own Licence field) and downloaded a byte-range sample of the actual 245MB CSV to check its real columns (`REGISTER_NAME`, `BN_NAME`, `BN_STATUS`, `BN_REG_DT`, `BN_CANCEL_DT`, `BN_STATE_NUM`, `BN_STATE_OF_REG`, `BN_ABN`). Excluded it rather than force-fitting: it's the entire Australia-wide business name register with no reliable South-Australia-specific slice (`BN_STATE_OF_REG` is populated only for legacy pre-2012 state-registered names migrating into the national scheme, leaving almost every current row blank), and mirroring the whole 245MB national file for a domain framed around SA consumer/business regulation would be a poor-fit, oversized addition rather than a genuine improvement over pointing readers to the live source. No individual-identifying data was inspected as part of this evaluation since nothing was added. Conclusion: a genuine, current, open SA-specific dataset for this domain does not exist as of this run — documented here rather than silently skipped or force-fitted; nothing added. 3 domains remain unchecked after this run (Corrections and rehabilitation; Agriculture and primary industries; Energy and utilities) — at, not below, the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Environment — EPA ambient air/water quality monitoring and national parks**. Career context re-checked directly against the Career Path `MEMORY.md` (still last updated 25 May 2026) rather than trusting the project memory summary alone — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search`/`package_show` all responded normally). Searched `air quality` (47 results), `national park` (4,612 results, mostly noise), `protected areas` (20 results) and `ambient water quality` (117 results). The EPA's own `recent-air-quality` package looked like the obvious "ambient air quality" fit but turned out to be a live hourly RSS feed with `temporal_coverage_from: Active` and no historical archive — same treatment as the GTFS-Realtime feed already excluded from `sa-adelaide-metro-gtfs`. Digging past it surfaced something better: EPA publishes **one CKAN package per monitoring station per pollutant category** (gaseous/particle/meteorology), each with a genuine multi-year CSV archive back to 2013 and several updated as recently as 17 June 2026 (three weeks before this run) — confirmed via CKAN `license_id: cc-by` on every package checked, and independently re-verified by fetching the live public dataset page for one package (Christie Downs gaseous data), which renders the CC BY 4.0 badge directly. Found and added **`datasets/sa-epa-air-quality-monitoring/`**: downloaded and unzipped sample files directly (not just trusting the CKAN listing) to derive the real field list — hourly gaseous pollutant CSVs (O3/NOx/SO2/CO), hourly particle CSVs (PM10/PM2.5), and 10-minute meteorology CSVs (temperature/wind/humidity/pressure), plus the network-wide `Air Quality Monitoring Sites` GeoJSON (16 stations, downloaded whole — 4,696 bytes). To keep footprint modest against a network this size (16 sites × up to 3 categories × 13 years), mirrored only calendar-year 2025 gaseous+particle+meteorology for one regional/industrial station (Port Pirie, Oliver Street — near the Nyrstar lead smelter) and gaseous+particle for one metro reference station (Christie Downs), all byte-for-byte matching CKAN's declared sizes; documented the full per-station archive URLs in the README for anyone who wants more. Privacy check: pure instrument readings (pollutant concentrations, weather), no individual or business-identifying fields of any kind — the least privacy-sensitive dataset added to this repository so far. Did not pursue the other two angles of this domain within the 2-4 search budget: **ambient (non-drinking) water quality** — EPA's own water-related CKAN items were narrow (e.g. `epa-orders`, a statutory-orders register) rather than a monitoring-station network like air quality has, worth a proper look next time; and **national parks** — found genuine statewide boundary/reserve datasets (`conservation-reserve-boundaries`, `national-parks-and-wildlife-npw-regions-districts`, both CC BY 4.0, large statewide shapefiles) but these are administrative boundaries, not visitor/usage statistics, and pursuing them would have meant a second large-shapefile dataset in one run — left as a documented candidate for a future pass rather than force-fitted alongside the air quality dataset. Also added an EPA CC BY 4.0 attribution line to `LICENSE-DATA.md`. Struck this domain's checkbox since a genuine, current, open dataset now exists for the "EPA ambient air quality monitoring" half of it, while flagging the water/national-parks halves as a real remaining gap in the checkbox note itself rather than treating this as fully resolving the original three-part ask. 4 domains remain unchecked after this run (Consumer affairs/business regulation; Corrections and rehabilitation; Agriculture and primary industries; Energy and utilities) — above the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Road safety and traffic crash statistics**. Career context re-checked directly against the Career Path `MEMORY.md` (last updated 25 May 2026) rather than trusting the project memory summary alone — Rin's ASO7 SAPOL Senior Data Analyst track is unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_show` responded normally, HTTP 200). Both parallel researchers converged on the same primary candidate — DIT's **Road Crash Data** (CKAN package `road-crash-data`) — and it was independently re-verified rather than taken on trust: live `package_show` confirms `license_id: cc-by`, `license_title: Creative Commons Attribution`, `license_url: http://creativecommons.org/licenses/by/4.0`, and the live public dataset page independently renders "Creative Commons Attribution 4.0 License" — no bundled licence file inside the archive to contradict the portal this time (unlike `sa-land-division-applications`/`sa-planning-zones`, where a bundled `License.txt` said CC BY 3.0 AU against the portal's CC BY 4.0 claim; here all sources agree). Downloaded and unzipped the actual 2020-2024 CSV bundle directly (4,981,599 bytes, exact match to CKAN's declared size) and the metadata PDF (249,787 bytes, exact match) rather than trusting the listing: three linked tables — Crash.csv (63,240 records), Units.csv (134,982 records), Casualty.csv (23,893 records) — sourced from SAPOL's Traffic Accident Register (TARS) and processed by DIT, covering all crash severities (fatal/serious injury/minor injury/property-damage-only) statewide. Privacy-checked by reading real rows directly: no name fields, no street addresses (suburb/postcode/LGA only), no full vehicle registration plates (`Veh Reg State` is state-abbreviation only — SA/NSW/VIC/etc — paired with manufacture year only), ages are integer years not DOB, and the `Hospital` field is a treating-facility name (e.g. "ROYAL ADELAIDE") not a person — passes the repo's individual-identifying-field exclusion bar, and is the same class of data already accepted in `sa-expiation-notices`. Confirmed not a duplicate of `sa-expiation-notices` by re-reading that dataset's README directly: expiation notices are SAPOL traffic-offence/fine/camera-detection data, structurally distinct from this crash/collision-incident dataset. Both files mirrored whole in `raw/` (small enough — under 5MB combined-ish — that a `fetch.sh` script wasn't needed, unlike the large shapefile datasets). Noted one real caveat in the new dataset's README: the publisher's own metadata PDF documents that the "reportable" property-damage-only crash threshold/definition changed in 1998, 2003, 2013 and twice in 2016-2017, so pre-2020 extracts shouldn't be treated as a single consistent time series without accounting for that. Excluded **Road Crash Locations in SA** (same TARS source, geometry-only repackaging with a reduced attribute set — would duplicate this domain rather than add to it) and the BITRE **Australian Road Deaths Database** (genuine, CC BY 3.0 AU, but national-with-SA-as-filter and fatalities-only, not SA-specific/statewide-authoritative in the way this repo's criteria prefer over a state source). Added **`datasets/sa-road-crash-data/`** and a DIT/Road Crash Data CC BY 4.0 attribution line to `LICENSE-DATA.md`. Only 2 domains remain unchecked after this run (Environment/EPA ambient monitoring; Consumer affairs/business regulation) — below the "fewer than 3" threshold, so proposed and added 3 new candidate domains: Corrections and rehabilitation (SA Dept for Correctional Services prisoner population/recidivism statistics), Agriculture and primary industries (PIRSA crop/livestock/water licensing statistics), and Energy and utilities (SA Government renewable/solar uptake and energy consumption data).
- 2026-07-06 (scheduled run): checked **Planning and development (development application approvals, zoning)**. Career context re-checked against memory (`project-career-prioritized-domains`/`reference-career-path-docs`) — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search` responded normally). Searched `development application` (4,997 results, mostly noise) and `planning zoning` (24 results). The **Development Application Public Register** (DHUD) looked like the obvious statewide DA fit by name, but its only resource is a link to a search webpage (`saplanningportal.sa.gov.au/public_register`), not a downloadable dataset — same dead-end already documented in `sa-land-division-applications`'s known-limitation section, re-confirmed this run rather than assumed. City of Adelaide's `Development Applications` and City of Playford's `Development Application Register` remain single-council, not statewide (consistent with prior findings that per-council DA registers stopped being the state's DA-processing system of record around 2021). Found and added **`datasets/sa-planning-zones/`** — DHUD's **Planning and Design Code Zones**, a statewide GIS layer of all 65 zone types (5,391 zone polygons) under the *Planning, Development and Infrastructure Act 2016*, fortnightly-updated per its own description and confirmed still current by `legalstartdate` values as recent as 14 May 2026. Downloaded and unzipped the actual GeoJSON archive directly (74,877,982 bytes) rather than trusting the CKAN listing, and derived the field dictionary from the GeoJSON's own properties, since the CKAN-linked metadata page returned only a bare stub with no field documentation. Found the same CKAN-vs-bundled-licence discrepancy already seen on `sa-land-division-applications` — the CKAN record claims CC BY 4.0 but the archive's own `License.txt` states **CC BY 3.0 AU** — followed the bundled licence text again, consistent with that precedent. The archive duplicates its 5,391 records across GDA94 and GDA2020 datums (~240 MB decompressed); applied the same repo-footprint treatment as `au-suburbs-councils`/`sa-land-division-applications` — License.txt and a derived field dictionary mirrored in `raw/`, full geodata left to `fetch.sh`, documented plainly as a footprint call (the host responded directly and quickly) rather than a network block. No individual-level fields: pure zone-boundary polygon, name, code and effective-date data, no landowner/applicant/address fields. Noted several finer-grained DHUD zoning layers (Subzones, Overlays, Variations, Historical Versions) as candidates for a possible future pass, not pursued given the 2-4 search budget. Confirmed again that no genuine current statewide development-application-approvals dataset (beyond land division) exists as open data. 3 domains remain unchecked after this run (Road safety/traffic crash statistics; Environment/EPA ambient monitoring; Consumer affairs/business regulation) — at, not below, the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Employment and training (Workforce Australia regional statistics, TAFE SA)**. Career context re-checked directly against the Career Path `MEMORY.md` (last updated 25 May 2026, before this run) rather than trusting the project memory summary alone — Rin's ASO7 SAPOL Senior Data Analyst role and 12-month goal are unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search`/`package_show` responded normally). Searched `TAFE` (15 results) and `vocational education training` (19 results) — most TAFE SA results were the same "narrow annual-report micro-dataset" pattern seen elsewhere in this repo (complaints, WHS, fraud, consultants, executive employment historical trends), plus a few one-off/stale course-catalogue snapshots (2015–2017, `update_freq: never`/`asRequired`). Found and added **`datasets/sa-apprenticeships-traineeships/`** — the South Australian Skills Commission's **Training Contract** data (Commencements, Completions, In-training), plain CC BY 4.0 confirmed via CKAN `license_id: cc-by`, genuinely current (metadata and resources both refreshed 1 July 2026, five days before this run) and statewide, covering every SA apprenticeship/traineeship since 2012/2013. Downloaded and read the actual XLSX files directly (not just the CKAN listing or metadata PDF) across multiple years to check schema stability — good thing, since this surfaced something unusual: the source's own metadata PDF states that, **effective 1 July 2026**, four new row-level fields were added to every file — learner age at commencement, Indigenous status, self-declared injury/disability status, and school-based status — confirmed by diffing actual 2025 files (six columns: training type, date, postcode, vocation, qualification, gender) against the Q1 2026 commencement/completion files and the 2026-07-01 in-training snapshot (ten columns, now including those four). Age, Indigenous status and disability status are each "sensitive information" under the Privacy Act/APPs, and at individual-Training-Contract row level combined with an exact date, employer postcode and specific vocation/qualification, this is a materially higher re-identification risk than anything else row-level in this repo to date (small rural postcode + rare trade + specific age/Indigenous/disability status could narrow to one person). Rather than silently mirroring it or silently skipping the domain, made an explicit editorial call: mirrored only the last pre-July-2026 releases — full calendar-year 2025 for Commencements (10,035 rows) and Completions (5,324 rows), and the 2026-04-01 snapshot for In-training (21,364 rows), all six-field only — and documented the field-addition finding prominently in the new dataset's README, noting the source still publishes the fuller current-quarter files openly under the same licence for anyone who wants them with that trade-off in view. Also added a South Australian Skills Commission CC BY 4.0 attribution line to `LICENSE-DATA.md`. 4 domains remain unchecked after this run (Planning and development; Road safety and traffic crash statistics; Environment/EPA ambient monitoring; Consumer affairs and business regulation) — at or above the "fewer than 3" threshold, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Aged care and disability services**. Career context re-checked against memory (`project-career-prioritized-domains`/`reference-career-path-docs`) — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search`/`package_show` responded normally). Searched `aged care` (12 results) and `disability` (17 results) tags. Most results were narrow annual-report micro-datasets (Equal Opportunity Commission complaints, DSD employee-with-disability counts) or disguised-stale snapshots — notably **"Communities and Social Inclusion Service Centers and Office Locations"** (DHS), whose `metadata_modified` field claimed 2025-04-01 but whose own bundled filename (`dcsi-offices-10-06-2014.zip`) and defunct-agency naming (Department for Communities and Social Inclusion, renamed years ago) showed it's an unchanged 2014 snapshot — same "metadata re-touched, data not" pattern as OCPSE, documented rather than force-fitted. Found and added **`datasets/sa-retirement-villages-register/`** — SA Health's **Retirement Villages Register**, plain CC BY 4.0 confirmed via CKAN `license_id: cc-by`, statewide, 520 registered villages in the current (2023) edition across 58 LGAs, facility name/address/unit-counts only (operator is a business entity, not an individual — no privacy concern). Downloaded and read the actual 2023 XLSX plus its field-dictionary metadata directly rather than trusting the CKAN listing. Documented plainly that retirement villages (SA-regulated, independent/assisted living) are distinct from Commonwealth-regulated residential aged care (nursing homes) — this dataset covers the former, not the latter. Separately pursued the "quality ratings" half of this domain: the Commonwealth Aged Care Quality and Safety Commission's "Star Ratings" quarterly data extract (Dept of Health, Disability and Ageing) was directly reachable and downloaded (May 2026 XLSX, `health.gov.au`) — but that department's own copyright page (fetched directly) explicitly prohibits commercial use and reproduction on another website without written permission, which is not CC BY or equivalent; excluded on licensing grounds, same treatment as ACARA My School and CFS/SAFECOM, and noted in `LICENSE-DATA.md`'s exclusion list. Also checked `data.gov.au`'s CKAN API directly (now at `data.gov.au/data/api/3/action/...`, migrated from the old `/api/3/action/...` path, and slower/less relevant-ranked than data.sa.gov.au's instance) — it did not surface the Star Ratings extract as a registered CKAN package either, consistent with it being distributed only via health.gov.au/AIHW GEN rather than the open data catalogue. With only 2 domains left unchecked after this run (Employment/training, Planning/development) — below the "fewer than 3" threshold — proposed and added 3 more candidate domains to the list: road safety/traffic crash statistics, EPA ambient environmental monitoring (distinct from the drinking-water angle already covered), and consumer affairs/business regulation.
- 2026-07-06 (scheduled run): checked **Environment and water**. Career context re-checked against memory (`project-career-prioritized-domains`/`reference-career-path-docs`) — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual, no reordering needed. `data.sa.gov.au` was directly reachable this run (CKAN `package_search`/`package_show` both responded normally). Searched `water quality` (109 results) and `EPA` (60 results). Found and added **`datasets/sa-water-quality/`** — SA Water's **Water Quality** dataset, plain CC BY 4.0 confirmed directly via CKAN `license_id: cc-by`, and genuinely current: 97 monthly resources from May 2018 through April 2026, latest published 3 June 2026 (tagged and actually published monthly, unlike several other domains' stale "annual"-tagged datasets found in earlier runs). Downloaded and read the actual April 2026 XLSX directly rather than trusting the CKAN listing: 4,876 rows covering 6 supply regions (Metropolitan, Outer Metro, Eyre, Northern, South East, Remote Communities), 93 named water systems and 59 tested parameters, each a rolling 12-month average value and ADWG-2011 health-compliance percentage — aggregate only, no customer/property/account-level data of any kind. Also mirrored the standing `Water Quality Metadata` glossary (73-row field/parameter dictionary) and the `Water Quality Systems and Suburbs` lookup (905 towns/suburbs mapped to supplying system), both updated less frequently than the monthly performance file. Checked EPA's own CKAN listings as the other angle for this domain: found only narrow items (`epa-orders` — a single-resource statutory-orders register, infrequently updated, not water/environment monitoring data in the sense this domain implies) — didn't pursue further given SA Water's dataset was already a strong, current, statewide fit and the 2-4 search budget. National parks and EPA ambient environmental monitoring (air/water quality stations, as opposed to drinking-water compliance) remain unexplored — noted as a possible future pass rather than silently dropped. Only 3 domains remain unchecked after this run (Aged care/disability, Employment/training, Planning/development) — at the "fewer than 3" threshold, not below it, so no new domains proposed this run.
- 2026-07-06 (scheduled run): checked **Libraries**. Career context re-checked against memory — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual, no reordering needed. Used a two-angle search (data.sa.gov.au CKAN vs. wider web) to survey candidates, then independently re-fetched each promising one directly rather than trusting search summaries. Surprising finding: **"Libraries Board of South Australia Annual Report Data"** (data.sa.gov.au, CC BY 4.0, 8 annual resources through 2023-24) looked like the obvious statewide fit by name, but downloading and reading its actual 2023-24 CSV directly showed it is **not** library usage/membership data — it's the Libraries Board's standard whole-of-government annual-report disclosure template (fraud instances, whistleblower disclosures, executive employment, consultant/contractor spend, complaint categories), the same "narrow annual-report micro-dataset" pattern already documented for OCPSE. Also checked and excluded: City of Adelaide's "City Library Daily Door Count" (CC BY 4.0 but data frozen since 17 June 2018 despite a "weekly" update claim — 8 years stale), "City Library Usage for the 1st year" (CC BY 4.0, no individual data, but a fixed one-off Jan 2014–Feb 2015 snapshot, `update_freq: never`), City of Onkaparinga's "Public Libraries Location Map" (CC BY 4.0 but its only resource is a hyperlink to a council webpage, not structured data), and two discontinued State Library of SA one-off snapshots (Playford library catalogue 2015; Dewey call-number counts, whose own description says it stopped being updated when the compilation method changed). With no genuine current statewide SA-specific dataset available, fell back to a national source per this repo's standing rule: the **Australian Public Libraries Statistical Report 2022-23** (ALIA/NSLA, published October 2024), compiled from data South Australia's own Public Library Services SA supplies annually. Downloaded the actual 15.5MB PDF directly (`read.alia.org.au`, no auth needed) and read it directly rather than trusting a secondary summary — good thing, since a web search result had claimed this edition was CC BY-NC-SA 4.0, which the PDF's own cover page contradicts outright (plain CC BY 4.0, confirmed visually). Extracted South Australia's own FY2022-23 figures from the report's per-jurisdiction comparison tables (collections: 2.91m items; usage/loans: 11.93m; onsite visitors: 7.57m; website visits: 9.17m; program sessions: 49,497; program participants: 714,543) — all aggregate, no individual-level data. Noted as a real limitation that this report's per-state tables don't break out registered-member counts, branch/service-point counts, staff, or expenditure by jurisdiction (national totals only for those), so the "membership" and "branch locations" parts of this domain remain a genuine gap, not silently dropped. A newer 2023-24 edition exists (released October 2025) but is distributed only via the National Library of Australia's Trove viewer, which returned no extractable content or direct download link to either an automated fetch or a follow-up web search — used the 2022-23 edition instead since it could be fully verified by direct download. Added **`datasets/sa-public-library-statistics/`**; also added a CC BY 4.0 attribution line to `LICENSE-DATA.md` for ALIA/NSLA.
- 2026-07-06 (scheduled run): checked **Local council services (waste collection, development approvals, rates)**. Career context re-checked against memory — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual. `data.sa.gov.au` was directly reachable this run (CKAN `package_search`/`package_show` all responded normally). Surveyed waste-collection, development-application and council-rates angles: waste collection turned up only single-council polygon datasets (e.g. Port Adelaide Enfield's "Waste Collection Areas"), not a statewide dataset; council rates/finance turned up nothing SA-specific and current (only stale per-council pages and unrelated interstate results); the LGA of SA's "API Statewide Local Government Datasets" is a dead end — its only "API" resource point to `parse.com`, the Facebook-owned Parse backend-as-a-service that shut down in January 2017, so it's a defunct integration, not usable data. The legacy per-council Development Application CSV registers (City of Adelaide, City of Playford, City of Onkaparinga) have all stopped updating, mostly around 2021 — Adelaide's own dataset notes this is because DA processing moved to the state's ePlanning system. Found one genuine current statewide fit: **Land Division Applications** (Department for Housing and Urban Development), a statewide land-division/subdivision development-application register, weekly-updated (archive files last generated 28 June 2026), CC BY confirmed via CKAN `license_id: cc-by` — but actually downloading and unzipping the shapefile (135 MB, reachable in ~10s) turned up a bundled `License.txt` that explicitly states **CC BY 3.0 AU**, not the 4.0 the CKAN record implies; documented that discrepancy plainly rather than picking one silently, following the licence text bundled with the data itself (same treatment as the existing `sa-school-locations` 3.0 AU/4.0 distinction). Confirmed no individual-level fields by parsing the shapefile's own DBF header directly (its CKAN-linked metadata page, on `lms.dit.sa.gov.au`, resolves to a private `10.37.x.x` address and timed out — an internal-only host, not publicly reachable) — 559,615 records, fields limited to application/development numbers, status codes, dates and lot/piece/deposited-plan cadastral references, no applicant or owner name, no street address. Added **`datasets/sa-land-division-applications/`**. The archive decompresses to ~870 MB (full statewide shapefile duplicated across GDA94 and GDA2020 datums), so — same treatment as the large ABS shapefiles in `datasets/au-suburbs-councils/` — the raw geodata isn't mirrored whole; only the bundled licence file and a derived field dictionary are kept in `raw/`, with `fetch.sh` provided to reproduce a full download (the host wasn't blocked, this is purely a footprint call given repo precedent for large shapefiles). Documented as a known limitation that no current, open, statewide dataset exists for development applications generally (only this land-division subset) or for waste collection/council rates at all — those two angles remain a real gap, not silently dropped. Also added a CC BY 3.0 AU attribution line to `LICENSE-DATA.md`.
- 2026-07-06 (scheduled run): checked **Public transport**. Career context re-checked against memory — Rin's ASO7 SAPOL analytics track is unchanged, so proceeded top-of-list as usual, no reordering needed. Found the **Adelaide Metro General Transit Feed (GTFS)** on data.sa.gov.au, CC BY 4.0 confirmed directly via CKAN `license_id: cc-by`. The CKAN page's own listed resource is just a link to Adelaide Metro's developer API page (`gtfs.adelaidemetro.com.au`), which turned out to be a Swagger/OpenAPI spec, not a direct file — the human-facing `www.adelaidemetro.com.au/developer-info` page is behind a Cloudflare bot challenge (403 to both WebFetch and a direct `curl`, regardless of user agent). However, the OpenAPI spec itself documents an unauthenticated static-download path (`gtfs.adelaidemetro.com.au/v1/static/latest/google_transit.zip`) on a separate CloudFront/S3 host that **was** directly reachable — downloaded the current ~17 MB GTFS zip bundle (feed version 1673, valid 2026-07-03 to 2026-10-11: 727 routes, 9,167 stops, 26,763 trips, 982,304 stop_times rows across 14 operators including Adelaide Metro bus/train/tram and regional coach operators) and mirrored it whole in `raw/` rather than needing a `fetch.sh`, since the archive is the standard GTFS distribution format (not an unpackable-here quirk) and downloaded cleanly. Added **`datasets/sa-adelaide-metro-gtfs/`**. Also found and noted (but did not mirror) the companion GTFS-Realtime feed (vehicle positions/trip updates/service alerts, CC BY 4.0, same API host) — a live stream with no historical snapshot to archive, so out of scope for this repo's point-in-time mirroring pattern. No individual-level data: this is schedule/network structure only (routes, stops-as-locations, timetables), not patronage or ticketing data. Also added an Adelaide Metro attribution line to `LICENSE-DATA.md`.
- 2026-07-06 (hourly run): checked **Emergency services**. Career context re-checked against memory (`project-career-prioritized-domains`/`reference-career-path-docs`) — Rin's ASO7 SAPOL analytics track is unchanged, so no reordering needed; proceeded top-of-list as usual. `data.sa.gov.au` was directly reachable this run (confirmed via CKAN `package_show` and a direct 7.2MB file download). Surveyed CFS/SAFECOM, MFS, SES and national (data.gov.au, AFAC, Productivity Commission) sources in parallel. Every CFS/SAFECOM incident dataset (Brigade Incidents, Fire Cause by Incident Group, Regional Incident Summary, Current Incidents live feed) is licensed **CC BY-NC-ND 4.0** (non-commercial, no-derivatives) — excluded on licensing grounds, same treatment as the ACARA My School precedent; they're also stale regardless (historical ones stop at FY2016-17; the live feed has no historical archive). SES has only one open dataset (`SES Volunteer Numbers`, CC BY 4.0), a headcount series stopping at 2016-17 with no incident/operational data at all. Found and added **`datasets/sa-mfs-fire-service-incidents/`** — SA Metropolitan Fire Service's "Fire Service Incidents" dataset, plain **CC BY 4.0** confirmed directly via CKAN `license_id: cc-by`, containing 107,154 incident-level records (Ref No, Date, Brigade, Situation Found) for 1 July 2018 – 30 June 2023 across 40 MFS brigades. Mirrored the latest CSV (7.2MB) in `raw/`; no individual-level fields (verified: no names, addresses, or vehicle/personal identifiers). Documented as a known limitation that the file is ~3 years overdue against its own declared annual cadence (last resource added August 2023) — same pattern as the existing `sa-health-ed-performance` entry. No SA agency (MFS, CFS, or SES) publishes response-time data as open structured data; the closest source, the Productivity Commission's annual Report on Government Services, is a narrative statistical publication rather than a CKAN dataset, so wasn't added as its own folder — noted as a possible future pass. Also added a CFS/SAFECOM exclusion line to `LICENSE-DATA.md` alongside the existing ACARA entry.
- 2026-07-06 (hourly run, fuller pass on a previously left-open domain): re-checked **SA public sector workforce and employment**, following up on the earlier note that it was left unstruck for a fuller future pass. `data.sa.gov.au` was reachable this run (correct API path is `/data/api/3/action/...`, not `/api/3/...`). Confirmed again that OCPSE's own CKAN-published datasets (fraud, whistleblower, executives, contractors, consultants, complaints, WHS) are narrow annual-report micro-datasets stuck at FY2020-21 despite 2025 `metadata_modified` timestamps (records re-touched, not the underlying data), and that OCPSE's actual whole-of-sector workforce reporting (State of the Sector, Workforce Information Report) is published only as narrative PDFs on publicsector.sa.gov.au, with a "Workforce Information Data Dashboard" that returned HTTP 403 to both WebFetch and a direct `curl` this run — its underlying data, if any, isn't accessible. Also checked the Department of State Development's "Workforce Data" dataset (economy-wide by occupation/industry/region, last updated Jan 2022) — not public-sector-specific and stale. Did find and add a genuinely open, currently-maintained dataset one level down: **`datasets/sa-education-workforce/`** — SA Department for Education (the largest single SA public sector agency) workforce headcount, FTE, classification, full/part-time, age/gender and Aboriginal/Torres Strait Islander employee breakdowns, FY2014 through FY2025, CC BY 4.0 confirmed via CKAN `license_id`. All five data CSVs plus the source's data dictionary mirrored in `raw/`. This is agency-level, not whole-of-sector — struck the checkbox on the basis that a genuine, current, open dataset for this domain now exists in the repo, while documenting in the new dataset's README that the whole-of-sector OCPSE angle remains a real, undisclosed gap rather than treating this as fully resolving the original ask.
- 2026-07-06 (ad-hoc run, career-prioritized per Rin's request): reordered the candidate list (see "Ordering note" above) toward Rin's SA public-sector career trajectory, then checked the two domains that moved up. **SA public sector workforce and employment**: `data.sa.gov.au` search (`workforce`, `public sector employment`, `state of the sector`) turned up only (a) narrow per-agency annual-report micro-datasets (fraud/whistleblower/executives/complaints counts for OCPSE, Courts Administration Authority, etc. — not workforce composition), and (b) a single stale statewide-ish snapshot: "Classification by Appointment and Gender" / "Classification ASO6 and Above by Gender" / "Appointment Type by Age and Gender" from the (now-defunct) Government Services Group business unit of DPC, one quarter only (Dec 2014), never updated since. None of these is a genuine, current, statewide SA public sector workforce dataset — left unstruck rather than force-fitted; a fuller pass (e.g. checking whether OCPSE's "State of the Sector" report is published as open data anywhere outside CKAN) is still worth doing later. **Courts and justice**: found and added `datasets/sa-courts-at-a-glance/` — the Courts Administration Authority's "At A Glance" annual statistics (data.sa.gov.au), CC BY 4.0 confirmed via CKAN `license_id`, 9 annual CSVs from 2016-17 through 2024-25 (latest published 25 Nov 2025), covering statewide lodgement/finalisation/clearance-rate and staffing figures across every SA court (Supreme, District, Magistrates, ERD, Youth, Coroners). Mirrored only the latest file (2024-25, which itself carries a 5-year rolling window) in `raw/`. Aggregate counts only, no case/defendant-level data. Also updated the scheduled task's own SKILL.md instructions to record that the candidate-list order now reflects career prioritization, so future scheduled runs don't need to re-derive or question it.
- 2026-07-06 (hourly run): checked **Housing**. `data.sa.gov.au` was directly reachable again this run (confirmed via CKAN API `package_search`/`package_show`, and a direct file download). Surveyed the `housing` tag (23 datasets) — most are SA Housing Authority/SA Housing Trust social-housing series last updated 2015–2022 and effectively static. The **Private Rent Report** (`private-rent-report`) stood out as still actively maintained: 72 quarterly XLSX resources from June quarter 2008 through March quarter 2026 (latest published 22 May 2026), CC BY 4.0 confirmed via the CKAN record's `license_id`/`license_url` fields, published by South Australian Housing Trust from the Consumer and Business Services rental bond dataset. Added `datasets/sa-private-rental-report/` — mirrored only the latest quarter (`private-rental-report-2026-03.xlsx`) in `raw/` to keep footprint modest given the 72-file historical depth; the README links back to the source for the full series. Content is aggregated bond counts and median rents by suburb/postcode/region/LGA, with the publisher already suppressing counts of 1–5 as `*` — no individual-level fields, consistent with this repo's privacy check. Considered but did not add: `social-housing-dwellings`/`households`/`new-households-housed`/`household-members` (all last touched 2022, data only to 2020-21 — noted as a future-pass candidate) and `housing-sa-public-housing-tenantable-stock-by-bedrooms-2015`-style stock-by-bedroom pages (dataset slug from an older search returned 404 — likely retired/renamed; not pursued further this run given the 2–4 search budget).
- 2026-07-06 (hourly run): checked **Health**. Found two genuinely open, currently-published SA Health datasets on data.sa.gov.au — Emergency Department median waiting times, and 4-hour-or-less length of stay — both CC BY 4.0, confirmed via each page's `DCTERMS.License` metadata (not assumed). Added `datasets/sa-health-ed-performance/` with both CSVs mirrored in `raw/` (data.sa.gov.au was directly reachable from this sandbox this run, unlike the earlier-documented block for `sa-expiation-notices`/`sa-school-locations` — network policy or routing may have changed, or the block may be path/domain-specific; noted in that dataset's README rather than assumed to still apply everywhere). Both source series stop at FY2017-18 (last metadata update 2018-09-04) despite an "annually" tag — documented as a known limitation rather than hidden. Elective Surgery Data and the Specialist Outpatient Waiting Time Report were noted as candidates for a future pass but not pursued this run (budget: kept to 2 datasets).
- 2026-07-06 (initial build, not an hourly run): established the five items above. See git log for detail. Hourly discovery task created same day, runs on the hour, local time.
